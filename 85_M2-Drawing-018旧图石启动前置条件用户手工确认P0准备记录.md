# M2-Drawing-018 旧图石启动前置条件用户手工确认 P0 准备记录

## 结论

`TODO-049 / 旧图石启动前置条件用户手工确认 P0`
本轮已经可以按**证据节点完成**处理。

当前已经闭合的事实是：

```text
1. 旧图石启动需要 USB 狗。
2. 插狗后可以进入主界面。
3. 插狗后可以打开 SFL。
4. 不需要内网 / VPN。
5. 旧图石运行还依赖本地 HASP 安装条件。
6. 我们的新系统复刻目标不继承 USB 狗 / HASP / 网络许可依赖，
   仍保持开源、无狗。
```

所以 `TODO-049` 的问题已经从
“旧图石为什么起不来”
收敛为：

```text
旧图石的现场取证前置条件已经确认：
  USB 狗 + 本地 HASP 安装

新系统的产品边界也已经确认：
  不引入 USB 狗 / Sentinel / HASP 作为运行依赖
```

这意味着 `TODO-049` 可以写成 done，
下一步应切回旧图石**非空运行样例采集**，
而不是继续猜启动阻塞原因。

## 本轮新增用户回填（2026-06-08）

对应证据：

```text
E-RUN-004
```

用户本轮回填了四个关键结果：

1. `USB` 现在还没插；
2. **插狗后**旧图石能进主界面；
3. **插狗后**旧图石能打开 `SFL`；
4. 不需要内网，但需要本地安装 HASP 驱动和运行文件。

白话就是：

```text
旧图石不是“必须连内网才能开”。

它更像是：
  USB 狗 + 本地 HASP 环境
满足后就能启动并打开 SFL。
```

## 旧图石本地安装前置条件

用户给出的现场安装要求是：

```text
1. 安装驱动程序 HASPUserSetup.exe
2. 将 haspvlib_23250.dll 和 hasplm.ini
   拷贝到
   C:\Program Files (x86)\Common Files\Aladdin Shared\HASP\
```

## 本轮对工作区安装包目录的只读核查

本轮只做了只读检查，
没有执行安装，
没有拷贝到系统目录，
没有改系统配置。

检查目录：

```text
.\HASPUserSetup
```

实际看到的文件是：

```text
HASPUserSetup.exe
hasplm.ini
haspvlib_23520.dll
驱动安装步骤.doc
```

这里有一个需要明确记录的小点：

```text
用户消息里写的是 haspvlib_23250.dll
但工作区实际文件名是 haspvlib_23520.dll
```

当前先把它记为**现场版本差异 / 待最终安装时复核**，
不直接替用户判断谁是错的。

## 工作区安装包 hash

```text
HASPUserSetup.exe    -> 057F3B08A0BDCFC6C71C371783A2B3C8D1DD1F0D13BC73E8D07B5A3EA6A912F0
hasplm.ini           -> C5214291EBD8A27D8C7FFF73EA1DCFDB6197962205F2705F8BEB1295109CC3CC
haspvlib_23520.dll   -> 083F00E7E063B074373BCE08B1BC6BE6C28D481799C6D2A8EC18F7484B5A77B9
```

## 目标安装目录当前状态

本轮只读检查结果：

```text
C:\Program Files (x86)\Common Files\Aladdin Shared\HASP\
  -> TARGET_PATH_NOT_FOUND
```

白话就是：

```text
当前这台机器上，
旧图石所需的目标 HASP 目录还没安装出来。
```

这和用户“我没安装，只是把文件放到 HASPUserSetup 目录下了”的描述一致。

## 本轮保留的既有静态证据

### 1. 机器侧预检仍成立

对应证据：

```text
E-DEV-071
```

已确认：

```text
本机基础网络在线；
SafeNet Sentinel / Sentinel LDK 数据目录存在；
但典型本地许可服务、监听端口和环境变量未出现。
```

### 2. fallback 语义仍成立

对应证据：

```text
E-IDA-031
```

已确认：

```text
“请检查网线是否接好”
不是纯网络专用报错，
而是宽兜底许可 / 环境初始化失败文案。
```

现在再结合用户手工结果，
可以更准确地收敛为：

```text
旧图石启动前置不等于必须联网，
而是至少需要 USB 狗，
并且当前现场还需要本地 HASP 安装条件。
```

## 对新系统路线的意义

这轮最重要的架构边界没有变化：

```text
USB 狗 / HASP / Sentinel
  -> 只属于旧图石运行取证前置条件

Qt6 + OCCT 正式 app
  -> 不能把这些旧许可依赖带进来
```

所以本轮证据只用于：

```text
1. 帮我们继续采旧图石运行证据
2. 关闭“旧图石为什么起不来”的启动期歧义
```

不用于：

```text
1. 给新系统加入加密狗
2. 给新系统加入 Sentinel / HASP 运行依赖
3. 让新系统依赖内网许可
```

## 对 TODO-049 的结论

`TODO-049` 现在可以按下面口径收尾：

```text
done
```

完成原因：

```text
- 旧图石启动需要 USB 狗：已确认
- 插狗后能进主界面：已确认
- 插狗后能打开 SFL：已确认
- 不需要内网：已确认
- 当前现场还需要本地 HASP 安装：已确认
- 新系统不继承 USB 狗依赖：已确认
```

## 下一步建议

下一步不再停留在启动前置条件，
应切回：

```text
TODO-050
  -> 旧图石非空 steeljoint-line / Others 运行样例采集
```

也就是：

```text
既然旧图石在正确前置条件下已经能启动并打开 SFL，
下一轮就该去采真正的命令运行样例、
导出的 DetailNN、截图和节点内容，
而不是继续围着启动条件打转。
```

## 边界声明

本记录证明：

```text
旧图石启动需要 USB 狗。
插狗后可以进入主界面。
插狗后可以打开 SFL。
不需要内网 / VPN。
当前现场还需要本地 HASP 安装条件。
工作区已有 HASP 安装包目录。
当前机器目标 HASP 安装目录尚不存在。
新系统复刻不继承 USB 狗 / HASP 依赖。
```

本记录不证明：

```text
当前这台机器已经完成驱动安装。
agent 已执行系统安装或拷贝。
真实接头线算法已闭合。
真实 Others 几何算法已闭合。
AutoCAD L2 已通过。
旧插件接受新包。
完整工程图已完成。
golden 已采集。
```
