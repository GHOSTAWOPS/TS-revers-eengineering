# M2-Stats-001 钢筋统计 / 下料表 P0 实现记录

## 结论

本轮完成：

```text
TODO-030 / M2-Stats
钢筋统计 / 下料表 P0
```

完成范围是：

```text
按 IDA MCP 证据确认旧 StbTable / MaterialTable 写出链。
在 domain/rebar 新增 RebarScheduleService。
把 DetailWriter 的 StbRow / MatRow 写出改为消费 RebarScheduleService。
保留下料表和 StbGroup / StbGeo 同源于 SteelData 的约束。
```

本轮不是完成整个工程图输出专项，也不是完整闭合旧图石材料质量公式。

## IDA MCP 证据

新增证据：

```text
E-IDA-025
```

IDA MCP 会话：

```text
session = visualts_i64_todo030
database = VisualTS.exe.i64
Hex-Rays = ready
```

已确认 Detail / XML 侧调用链：

```text
sub_140609690
  -> sub_140606560
  -> sub_140609860
  -> sub_140607E30
       写 StbTable
       循环调用 sub_140637400 写 StbRow%d
  -> sub_140609190
       写 MaterialTable
       循环调用 sub_1406382D0 写 MatRow%d
```

和旧 Excel / 下料命令链的关系：

```text
E-IDA-019 / E-IDA-020 / E-IDA-021
  psexcel / 0x8D1C / sub_140605B20 / sub_140602F90
  确认下料表内部命令和 Excel 链字段存在。

E-IDA-025
  确认 Detail / XML writer 侧 StbTable / MaterialTable
  由 sub_140607E30 / sub_140609190 写出。
```

字段来源摘要：

```text
sub_140637400 写 StbRow：
  rsdID       = 多分支：Y%d / Z%d / %d%c / string / int
  ComponentName = 旧构件链字段
  SteelWay    = GU / LA / STEEL_NET / LASAME / XI / OTHER 分支
  diameter    = a1[8]
  length      = sub_140637070(...)
  segNum      = sub_1405FE280(v5)
  sameGrpNum  = a1[1]
  stbNumSum   = a1[3]
  lenSum      = *((double*)a1 + 5)，格式 %.2f
  stbLevel    = *(v5 + 56 + 136)
  stbLayer / stbProfile / stbUse / smallTable / mirrorType / mirrorSEFlag 继续写出

sub_1406382D0 写 MatRow：
  diameter    = sub_140637010(this) -> *(this + 4)
  lenSum      = sub_140637220(this) -> *(this + 8)
  countSum    = *(this + 0)
  singleMass  = GetDefaultValue(this)
  massSum     = sub_140637250(this) -> a1[2] * a1[1]
  stbLevel    = sub_140637200(this) -> CString(this + 32)

sub_140609190 写 MaterialTable：
  rowCount = material vector size
  Mass     = sum(sub_140637250(row)) / 1000
  Volume722 = ACIS body mass_props volume，四舍五入到 0.001
  MassNum  = 入口 a1
```

不能过度推断：

```text
sameGrpNum 的合并规则未完整闭合。
旧 material row 的构造函数和 singleMass 来源未完整闭合。
Volume722 依赖旧 ACIS body mass_props，新系统 P0 不复刻。
Excel 链 sub_140602F90 和 Detail/XML 链 sub_140607E30 / sub_140609190
不能混成同一条函数结论。
```

## 实现内容

新增：

```text
app/src/domain/rebar/RebarScheduleService.h
app/src/domain/rebar/RebarScheduleService.cpp
app/tests/unit/rebar_schedule_service_tests.cpp
```

修改：

```text
app/src/drawing/detail/DetailWriter.cpp
app/CMakeLists.txt
tools/phase1_readiness_gate/check_phase1_readiness.py
```

`RebarScheduleService::buildSchedule` 行为：

```text
输入 SteelData。
每个可解析 SteelBarGroup 输出一个 RebarScheduleRow。
RebarScheduleRow.rsdId / diameter / length / segNum / stbNumSum /
lenSum / stbLevel 等字段从同一组 group/bar/segment 派生。
MaterialRow 按 diameter + steelLevel 聚合。
有显式 legacyRaw singleMass 时计算 singleMass / massSum / Mass。
没有 singleMass 时稳定输出 mass deferred 诊断，Mass 保持 0。
只要任一 MaterialRow 的 singleMass 来源缺失或冲突，全表 Mass 和所有 MatRow.massSum
都统一 deferred 为 0，避免出现 Mass=0 但局部 MatRow.massSum 非 0 的半真输出。
```

P0 材料聚合近似：

```text
MaterialRow 当前按 diameter + steelLevel 聚合。
IDA 已确认 MatRow 字段读取链，但 material vector 的完整构造 / 分组规则未闭合。
该聚合口径只是 P0 可测试近似，后续仍需继续追 sub_140606560 /
sub_140609860 或旧图石运行下料表输出。
```

P0 未闭合字段：

```text
MassNum 当前保持 0。
IDA 已确认 MaterialTable.MassNum 来自入口参数，但该入口来源尚未闭合。
本轮不声明 MassNum 等价旧图石。
```

P0 合并口径：

```text
sameGrpNum = 1
```

原因：

```text
IDA 已确认 old row 中 sameGrpNum 来自 a1[1]，
但 a1 聚合结构的生成、同组归并规则和旧运行输出仍未闭合。
本轮不能硬猜完整合并规则。
```

## TDD

新增测试：

```text
rebar_schedule_service_tests
```

覆盖：

```text
StbRow 字段同源于 SteelBarGroup / SteelBar / SteelBarSegment。
length 是单根长度，lenSum 是 length * barCount。
stbNumSum 取 barCount。
MaterialRow 按 diameter + steelLevel 聚合。
有 singleMass 时计算 massSum 和 MaterialTable.Mass。
没有 singleMass 时输出 RS002_MATERIAL_MASS_FORMULA_DEFERRED。
缺 bar 引用时输出稳定 diagnostic，仍处理其他合法 group。
```

## 验证结果

窄测：

```text
cmake --build app/build --target rebar_schedule_service_tests detail_writer_tests
ctest --test-dir app/build -C Debug -R "rebar_schedule_service_tests|detail_writer_tests" --output-on-failure

2 / 2 pass
```

全量 CTest：

```text
16 / 16 pass
```

真实 `123.stp` 测试：

```text
step_import_probe_123 = pass
occ_selection_index_123 = pass
legacy_geometry_adapter_123 = pass
```

Readiness gate：

```text
py -3 tools/phase1_readiness_gate/check_phase1_readiness.py --strict
M1-Formal-Ready, 84 / 84 pass, 0 error, 0 warning
```

OCCT / AIS 泄漏扫描：

```text
rg -n "TopoDS_|AIS_|BRep|TopAbs_" app/src/domain/rebar app/src/drawing app/src/project
no matches
```

## xhigh 只读 Review

```text
round1 verdict = block_commit
  Critical: 多 MaterialRow 部分 deferred 时可能出现 MaterialTable.Mass = 0
            但局部 MatRow.massSum 非 0。
  Important: MaterialRow 聚合规则是 P0 近似；MassNum 来源未闭合。

main fix:
  任一材料行 deferred 后，全表 MaterialTable.Mass 和所有 MatRow.massSum
  统一 deferred 为 0。
  新增多 MaterialRow 部分 deferred 测试。
  文档补充 MaterialRow 聚合规则和 MassNum 缺口。

rerun verdict = allow_commit
  Critical: none
  Important: none
  Minor: none
```

## 边界

本轮不声明：

```text
旧图石完整下料合并规则已闭合。
旧 singleMass 材料表来源已完整闭合。
Volume722 的旧 ACIS 体积逻辑已复刻。
MassNum 入口参数来源已闭合。
Excel 输出、Dialog #427、输出文件名和覆盖策略已运行确认。
AutoCAD L2 导入已通过。
golden 已完成。
```

## 下一步

```text
TODO-031 / M2-Drawing
工程图生成专项
```
