# M1-App-020 旧线筋 / 弧筋创建算法 P0 实现记录

## 结论

本轮完成 `TODO-021 / M1-App-020`。

新增纯业务层 P0 creator：

```text
RebarGroupCreator
LegacyRebarGeometryReader
LegacySegmentCurveNormalizeRequest
LegacyRebarCurveSnapshot
```

本轮把 `TODO-020 / E-IDA-022` 的旧线筋 / 弧筋公共链落成第一批
`domain/rebar` 对象创建能力：

```text
legacy edge ref / old command parameters
  -> LegacyRebarGeometryReader 读取 legacy 曲线摘要
  -> SegmentCurveNormalizer P0 请求
  -> SteelData / SteelBarGroup / SteelBar / SteelBarSegment
  -> evidence / unresolved legacy fields
```

本轮不是完整旧 UI 复刻，不生成 AIS 显示，不写 Detail 包，不声明 golden 已闭合。

## 新增能力

### LineGroup P0

`RebarGroupCreator::createLineGroup()`：

- 输入 `RebarGroupCreationRequest` 和 `LegacyRebarGeometryReader`。
- 要求 `sourceCurve` 是 legacy edge ref。
- 先执行旧 `distanceA >= 0.002` 创建阈值。
- 读取 legacy curve snapshot。
- 要求曲线长度不低于旧 split 后最小段长 `0.01`。
- 调用 `normalizeSegmentCurve()`，把 `sub_1405D5670` 的 P0 normalizer 作为显式业务接口。
- 输出 `SteelData`，其中含一组 `SteelBarGroup -> SteelBar -> SteelBarSegment` 引用。
- group / bar / segment 均带 `E-IDA-022` evidence 和 `geometryRef.curveStableIds`。
- `sub_1405D5670.arg4`、`objA/objB/createdPayload` 仍作为 unresolved 字段保留。

### ArcGroup P0

`RebarGroupCreator::createArcGroup()`：

- 输入同样走 legacy edge ref / geometry reader。
- 按 TODO-020 证据强制旧链语义：

```text
sgroupbararc -> sub_1404D10C0(..., distance, 0.8, 1)
```

- 无论调用方传入什么 `distanceB`，都按旧入口证据强制记录 `0.8`。
- 即使请求传入 `legacyFlag = 0`，输出 old raw 字段也强制为 `1`，并记录
  `legacyFlagForcedByCommand`。
- 输出 arc 形态 segment，`SteelBarSegmentShape::Arc`。
- `sgroupbararc.uiMapping` 仍保留 `GAP-IDA-001`，不声明它已经完全对应旧 UI 的扇形筋或同心圆。

### SegmentCurveNormalizer P0 边界

本轮没有在业务层直接做 OCCT split / spline / trim。

业务层新增的是旧语义接口：

```text
LegacyRebarGeometryReader::normalizeSegmentCurve(curve, request)
```

其中 `LegacySegmentCurveNormalizeRequest` 承载 TODO-020 常量：

```text
minimumSplitLength = 0.01
nearEndpointDistance = 0.1
trimDelta = -0.03
endpointSplitStartRatio = 0.001
endpointSplitEndRatio = 0.999
trimIterationStep = 0.02
minimumSplineSamples = 5
splineSamplesPerUnitLength = 50
```

也就是说，业务层已经有 P0 normalizer 入口和证据字段；真实 topology mutation
仍由后续 adapter / transaction 层继续推进。

## 证据对齐

主要证据：

```text
E-IDA-022
57_TODO-020_IDA旧线筋弧筋链补证据记录.md
15_线配筋与弧形组专项初稿.md
16_seg_steelbargroup字段地图初稿.md
55_M1-App-018RebarDomainModelFreezeP1实现记录.md
56_M1-App-019LegacyCommandContractP1实现记录.md
```

关键对齐点：

- `distanceA < 0.002` 在读取几何前拒绝，对齐 `sub_1404D10C0`。
- 长度 `< 0.01` 拒绝，对齐 `sub_1405D5670` split 后小段规则。
- line group 使用 `Rebar.Create.LineGroup / sgroupbarline`。
- arc group 使用 `Rebar.Create.ArcGroup / sgroupbararc`。
- arc group 固化 `distanceB=0.8, flag=1` 的旧入口证据。
- `E-IDA-022` 进入 group / bar / segment evidence。
- 未闭合字段进入 `unresolvedLegacyFields`，不写成确定业务字段。

## TDD

RED 1：

```text
cmake --build app\build --target rebar_group_creator_tests
result = expected compile failure
failure = domain/rebar/RebarGroupCreator.h missing
```

GREEN 1：

```text
rebar_group_creator_tests.exe
result = pass
```

xhigh 第一轮 review 后补 RED 2：

```text
cmake --build app\build --target rebar_group_creator_tests
result = expected compile failure
failure = LegacySegmentCurveNormalizeRequest / normalizeSegmentCurve missing
```

GREEN 2：

```text
rebar_group_creator_tests.exe
result = pass
```

## 测试结果

新增测试：

```text
rebar_group_creator_tests
```

覆盖：

- line group 正常创建。
- arc group 正常创建。
- `distanceA < 0.002` 在几何查询前拒绝。
- legacy segment 长度 `< 0.01` 拒绝。
- 非 edge ref 稳定拒绝。
- evidence / unresolved 字段保留。
- arc group 强制旧 `flag=1` 和 `distanceB=0.8`。
- creator 必须调用 `SegmentCurveNormalizer P0`，并使用 normalized curve 输出 segment。

默认 CTest：

```text
10 / 10 passed
Total Test time = 126.55 sec
```

Readiness gate：

```text
decision = M1-Formal-Ready
78 / 78 pass
0 error
0 warning
```

`domain/rebar` 边界扫描：

```text
rg "TopoDS_|AIS_|BRep|TopAbs_" app\src\domain\rebar
result = no matches
```

## xhigh 只读 review

第一轮 review：

```text
Critical = none
Important =
  1. arc group 未强制 TODO-020 证据中的 flag=1。
  2. 缺少 SegmentCurveNormalizer P0 接口 / 调用，不能只写 evidence。
Verdict = block
```

处理结果：

- `createArcGroup()` 内部强制 `effectiveFlag = 1`，并记录 `legacyFlagForcedByCommand`。
- 新增 `LegacySegmentCurveNormalizeRequest`。
- 新增 `LegacyRebarGeometryReader::normalizeSegmentCurve()`。
- creator 使用 normalized curve 构造 segment。
- 补测试覆盖 normalizer 调用、旧常量和 normalized segment 输出。

最终 review：

```text
Critical = none
Important = none
Minor = ArcGroup distanceB 文档表述偏弱，已修正
Verdict = allow_commit
```

## 修改范围

```text
app/CMakeLists.txt
app/src/domain/rebar/RebarGroupCreator.h
app/src/domain/rebar/RebarGroupCreator.cpp
app/tests/unit/rebar_group_creator_tests.cpp
```

## 边界

本轮没有实现 AIS 钢筋显示。

本轮没有实现 Detail writer。

本轮没有修改 UI handler 或把 `NotImplemented` placeholder 接成可点击创建流程。

本轮没有迁移父目录 `src/rebar/*`、`EdgeToRebarFactory`、`FaceRebarGenerator`、
`PolylineRebarGenerator` 或 `RebarCreationCommandService`。

本轮没有让 `domain/rebar` 依赖 OCCT / AIS。

本轮 P0 creator 只证明业务对象创建和旧阈值 / evidence / unresolved 承载已经有
可测试入口，不等价旧图石完整线筋 / 弧筋 UI 流程、失败提示、golden 输出或裁剪编辑算法。

## 下一步建议

下一阶段建议优先做：

```text
TODO-022 / M1-App-021
AIS 钢筋显示映射
```

原因：

```text
domain SteelBarGroup 已能由旧线筋 / 弧筋 P0 creator 生成。
下一步可以把 domain 对象映射到 presentation/occ 的 AIS 显示，
但仍必须保持 domain 不知道 AIS。
```

如果要继续补业务证据，也可以先做旧图石运行确认：

```text
sgroupbararc 对应扇形筋 / 同心圆的 UI 关系
线筋 / 弧筋参数窗口、状态栏提示、失败提示
```
