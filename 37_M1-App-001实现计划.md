# M1-App-001 实现计划

> **For agentic workers:** REQUIRED CONTROL FRAMEWORK: Use `cybernetic-systems-engineering-v2` as the main engineering control framework. Use `superpowers:subagent-driven-development` or `superpowers:executing-plans` only as task execution workflow. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `【图石钢筋1比1复刻】/app` 创建正式 Qt6 + OCCT 新工程，跑通主窗口、旧命令入口、STP 导入统计，并用门禁防止 OCCT 进入钢筋领域层。

**Architecture:** 新工程只迁父目录 Qt6 / OCCT / STEP / Viewer / IO 基础设施，不迁父目录钢筋生成主链。OCCT 只能出现在 `geometry/occ`、`presentation/occ`、`io/step` 等技术层，钢筋业务通过 `LegacyGeometryAdapter` 的纯引用和几何摘要访问几何能力。

**Tech Stack:** C++20、CMake、Qt6 Widgets、OCCT 7.9.3、CTest、PowerShell、Python readiness gate。

---

## CSE v2 控制口径

本任务按 CSE v2 管理，因为它同时涉及：

- 旧图石逆向证据。
- 父目录遗留实现迁移。
- Qt6 / OCCT 新工程初始化。
- STEP 几何导入和选择 ID 稳定性。
- `domain/rebar` 与 OCCT 的硬边界隔离。

主控原则：

```text
一次只推进一个可验证切片。
每个切片都有明确输入、输出、门禁和回退触发。
父目录代码只扩展性迁移，不改写旧工程。
旧图石业务缺口只能由旧图石证据关闭。
```

M1-App-001 的控制合同：

| 字段 | 内容 |
|---|---|
| Primary Setpoint | `app` 工程可独立构建，能导入 `123.stp` 并保持 `domain/rebar` 不依赖 OCCT。 |
| Acceptance | CMake build、CTest、`123.stp` 导入统计、OCCT 边界扫描、readiness gate 全部通过。 |
| Guardrail Metrics | 不迁父目录钢筋生成主链；未实现命令返回 `NotImplemented`；`.tsrebar` 仍是源格式。 |
| Boundary | 只新增 `【图石钢筋1比1复刻】/app` 和本目录文档；不修改父目录旧工程。 |
| Rollback Trigger | `domain/rebar` 出现 `TopoDS_` / `BRep*` / `AIS_` 等关键词；或 QAction 绕过命令系统直连业务。 |
| Approximation Validity | M1 只证明工程骨架、导入和命令骨架，不声明任何钢筋业务 1:1 完成。 |

---

## 文件结构

首期创建和修改：

- Create: `app/CMakeLists.txt`
- Create: `app/src/app/main.cpp`
- Create: `app/src/app/MainWindow.h`
- Create: `app/src/app/MainWindow.cpp`
- Create: `app/src/ui/MainTabs.h`
- Create: `app/src/ui/MainTabs.cpp`
- Create: `app/src/ui/ModelTreePanel.h`
- Create: `app/src/ui/ModelTreePanel.cpp`
- Create: `app/src/command/CommandId.h`
- Create: `app/src/command/CommandRegistry.h`
- Create: `app/src/command/CommandRegistry.cpp`
- Create: `app/src/command/LegacyUiCommandMap.h`
- Create: `app/src/command/LegacyUiCommandMap.cpp`
- Create: `app/src/geometry/legacy/LegacySelectionRef.h`
- Create: `app/src/domain/rebar/SteelBar.h`
- Create: `app/src/domain/rebar/SteelBarSegment.h`
- Create: `app/src/domain/rebar/SteelBarGroup.h`
- Create: `app/src/domain/rebar/SteelData.h`
- Create: `app/tools/check_domain_rebar_boundary.py`
- Create: `app/tests/unit/test_domain_rebar_boundary.py`
- Create: `app/tests/unit/command_registry_tests.cpp`
- Create later in STEP task: `app/src/geometry/occ/import/OcctStepImportService.*`
- Create later in STEP task: `app/src/import/StepImportResult.h`

禁止首期创建：

- `app/src/rebar/EdgeToRebarFactory.*`
- `app/src/rebar/FaceRebarGenerator.*`
- `app/src/core/RebarCreationCommandService.*`
- 任何直接从 `TopoDS_Edge` / `TopoDS_Face` 创建钢筋的业务文件

---

## Task 1: 工程骨架与 CMake

**Files:**

- Create: `app/CMakeLists.txt`
- Create: `app/src/app/main.cpp`
- Create: `app/src/app/MainWindow.h`
- Create: `app/src/app/MainWindow.cpp`

- [ ] **Step 1: 创建最小 Qt6 Widgets 工程**

`app/CMakeLists.txt` 必须包含：

```cmake
cmake_minimum_required(VERSION 3.22)
project(TsRebarApp LANGUAGES CXX)

enable_testing()

set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)
set(CMAKE_AUTOMOC ON)

find_package(Qt6 6.2 REQUIRED COMPONENTS Core Gui Widgets Test)

add_executable(tsrebar_app
    src/app/main.cpp
    src/app/MainWindow.cpp
    src/app/MainWindow.h)

target_include_directories(tsrebar_app PRIVATE src)
target_link_libraries(tsrebar_app PRIVATE Qt6::Core Qt6::Gui Qt6::Widgets)
```

- [ ] **Step 2: 构建并启动最小窗口**

Run:

```powershell
cmake -S .\【图石钢筋1比1复刻】\app -B .\【图石钢筋1比1复刻】\app\build -G Ninja -DCMAKE_TOOLCHAIN_FILE=D:\Work\vcpkg\scripts\buildsystems\vcpkg.cmake
cmake --build .\【图石钢筋1比1复刻】\app\build
```

Expected:

```text
tsrebar_app build success
```

---

## Task 2: 旧图石页签和命令骨架

**Files:**

- Create: `app/src/ui/MainTabs.*`
- Create: `app/src/command/CommandId.h`
- Create: `app/src/command/CommandRegistry.*`
- Create: `app/src/command/LegacyUiCommandMap.*`
- Modify: `app/src/app/MainWindow.*`
- Create: `app/tests/unit/command_registry_tests.cpp`

- [ ] **Step 1: 定义首批 CommandId**

首批至少包含：

```cpp
enum class CommandId {
    ProjectNew,
    ProjectOpen,
    ProjectSave,
    ProjectClose,
    ImportStep,
    ExportRebarStep,
    ViewFitAll,
    ViewPan,
    ViewZoom,
    ViewRotate,
    RebarFaceCreate,
    RebarLineCreate,
    RebarArcGroupCreate,
    QueryByNumber,
    QueryCollision,
    DrawingDefineSectionPlane,
    DrawingGeneratePackage,
    DrawingGenerateBarSchedule
};
```

- [ ] **Step 2: 创建 5 个页签**

必须出现：

```text
开始
显示
钢筋
查询
工程图
```

- [ ] **Step 3: 未实现命令统一返回 NotImplemented**

CommandRegistry 的未实现命令必须返回：

```text
status = NotImplemented
```

不能静默成功。

- [ ] **Step 4: 加 C++ 单测**

测试断言：

- 五个页签定义存在。
- `ExportRebarStep` 已注册。
- 未实现命令返回 `NotImplemented`。

---

## Task 3: `domain/rebar` OCCT 边界门禁

**Files:**

- Create: `app/src/domain/rebar/SteelBar*.h`
- Create: `app/tools/check_domain_rebar_boundary.py`
- Create: `app/tests/unit/test_domain_rebar_boundary.py`
- Modify: `app/CMakeLists.txt`

- [ ] **Step 1: 创建纯 DTO 钢筋占位**

DTO 只允许使用标准库类型和纯数值类型：

```cpp
struct SteelBarSegment {
    std::string id;
    std::string segmentType;
    double lengthMm = 0.0;
};
```

- [ ] **Step 2: 创建扫描脚本**

扫描关键词：

```text
TopoDS_, TopoDS::, TopAbs_, TopExp_, BRep, BRepAdaptor_,
BRepAlgoAPI_, BRepBuilderAPI_, BRep_Tool, BRepGProp, BRepClass3d,
Geom_, Geom2d_, GeomAbs_, GCPnts_, gp_, AIS_, Standard_, Handle(Geom
```

- [ ] **Step 3: 加入 CTest**

Run:

```powershell
ctest --test-dir .\【图石钢筋1比1复刻】\app\build -R domain_rebar_boundary --output-on-failure
```

Expected:

```text
100% tests passed
```

---

## Task 4: STEP 导入统计服务

**Files:**

- Create: `app/src/import/StepImportResult.h`
- Create: `app/src/geometry/occ/import/OcctStepImportService.h`
- Create: `app/src/geometry/occ/import/OcctStepImportService.cpp`
- Create: `app/src/step/StepImportProbe.cpp`
- Modify: `app/CMakeLists.txt`

- [ ] **Step 1: 从父目录迁移最小 STEP 读取能力**

只迁：

- `STEPCAFControl_Reader`
- `XCAFDoc_ShapeTool`
- free shape 遍历
- solid / face / edge / vertex 计数
- 文件单位记录

- [ ] **Step 2: 不暴露 OCCT 到 `domain/rebar`**

`StepImportResult.h` 只包含纯摘要：

```cpp
struct StepImportResult {
    bool ok = false;
    std::string filePath;
    std::string lengthUnit;
    int solids = 0;
    int faces = 0;
    int edges = 0;
    int vertices = 0;
    std::string error;
};
```

- [ ] **Step 3: 用 `123.stp` 验证**

Run:

```powershell
.\【图石钢筋1比1复刻】\app\build\step_import_probe.exe .\123.stp
```

Expected:

```text
ok=true
solids=754
faces=3016
```

---

## Task 5: 主窗口接入 STEP 导入摘要

**Files:**

- Modify: `app/src/app/MainWindow.*`
- Create: `app/src/ui/ModelTreePanel.*`
- Modify: `app/CMakeLists.txt`

- [ ] **Step 1: 添加模型树面板**

导入后显示：

```text
123.stp
solids: 754
faces: 3016
unit: METRE
```

- [ ] **Step 2: 添加“导入 STEP”命令入口**

入口暂时可走测试路径或文件对话框。

- [ ] **Step 3: 保持命令链路**

UI action 必须通过：

```text
LegacyUiCommandMap -> CommandRegistry -> handler
```

不能 QAction 直接调用业务状态改变。

---

## Task 6: 回归门禁

**Files:**

- Modify: `34_Phase1ReadinessGate实际运行记录.md`
- Modify: `12_运行确认日志.md` when runtime output exists

- [ ] **Step 1: 跑新工程构建**

Run:

```powershell
cmake --build .\【图石钢筋1比1复刻】\app\build
```

- [ ] **Step 2: 跑 CTest**

Run:

```powershell
ctest --test-dir .\【图石钢筋1比1复刻】\app\build --output-on-failure
```

- [ ] **Step 3: 跑 Phase1 readiness gate**

Run:

```powershell
py .\【图石钢筋1比1复刻】\tools\phase1_readiness_gate\check_phase1_readiness.py --strict
```

Expected:

```json
{
  "decision": "M1-Formal-Ready",
  "errorCount": 0
}
```

---

## Self Review

- 范围只覆盖 M1-App-001，没有进入真实钢筋生成。
- STEP 导入服务落在 `geometry/occ/import`，不会污染 `domain/rebar`。
- 父目录钢筋生成主链列为禁迁，不在本计划创建。
- UI 只做功能入口和命令契约，不复刻老界面丑皮肤。
- 验收包含构建、CTest、`123.stp` 导入统计和 readiness gate。

## Run 001 结果

```text
decision = m1-app-001-pass
```

实际通过：

- CMake configure/build。
- `tsrebar_app --smoke`。
- `command_registry_tests`。
- `domain_rebar_boundary`。
- `step_import_probe_123`。
- Phase1 readiness gate strict。

关键统计：

```text
123.stp
solids=754
faces=3016
edges=9048
vertices=18096
length_unit=METRE
```

运行报告：

```text
docs/phase1/app_build_reports/m1_app_001_run_001.md
```
