# 缺口和待确认项

## 说明

本文件只记录未闭合事项。

规则：

- 低置信项必须留在这里，不得写成确定结论。
- 每项要说明为什么未确认、需要什么证据、建议下一步。

## 缺口调度规则

后续所有缺口默认补充以下调度信息。旧表暂不整体重排，避免打散已有证据；新缺口或关闭缺口时按此口径更新。

优先级：

- `P0`：阻塞 Phase 1/2/3 或会导致整体路线错误。
- `P1`：阻塞某个核心功能专项。
- `P2`：不阻塞一期主线，但影响完整度。

阻塞阶段：

- `M0`：证据闭合。
- `M1`：Qt6 + OCCT 外壳。
- `M2`：钢筋领域模型。
- `M3`：核心钢筋创建。
- `M4`：编辑/查询/检查。
- `M5`：工程图/下料。
- `M6`：老 SFL 兼容。

关闭条件：

```text
缺口关闭 = 有证据 ID + 有 Requirement ID 或明确非一期 + 有文档落点 + 有验收/运行确认方式
```

当前 P0/P1/P2 调度：

| GAP ID | 优先级 | 阻塞阶段 | 阻塞功能 | 关闭条件 |
|---|---|---|---|---|
| GAP-FUNC-001 | P1 | M3 | 剖面圈筋 | 运行确认按钮真实文字、弹窗/状态栏，并补 Evidence ID。 |
| GAP-FUNC-002 | P1 | M3 | 固中心 | 运行确认按钮真实文字、弹窗/状态栏，并补 Evidence ID。 |
| GAP-FUNC-003 | P1 | M4 | 组合开 | 运行确认对象树变化、编号变化、撤销行为，并补 Evidence ID。 |
| GAP-FUNC-004 | P1 | M4/M5 | 未割钢筋 / 未剖到钢筋 | 运行确认按钮文字和 Dialog 对应关系。 |
| GAP-FUNC-005 | P1 | M5 | 割切面 / 剖切面 | 运行确认按钮文字、Dialog #316/#485 对应关系。 |
| GAP-IDA-005 | P1 | M5 | 生成工程图 | `E-IDA-042 / E-IDA-043 / E-IDA-044 / E-RUN-006 / E-DEV-088` 已把 static stop point 收口到：`工程图 / 输出 -> {36124, 35057}`、`36124 / 0x8D1C -> psallc -> sub_140600AA0`、`36124 = 生成工程图`；TODO-066 已取得真实运行工件，确认非空 `Detail01..04.stl` 是图纸主体；多机观察显示 `Detail.xml` 均为 `<StyleRoot/>` 且生成 / CAD 导入时不更新，当前更像固定空模板 / 占位文件；同时 `sub_140600AA0` 里唯一可直接证明的 `DoModal` 已收窄到 `output_uncut_steel / Dialog 0x57C / UnCutSteel.TXT` 后置报告链。关闭仍需要字段级差距、文件覆盖策略和旧插件导入结果。 |
| GAP-IDA-006 | P1 | M5 | 下料表 | `E-IDA-020 / E-IDA-043 / E-IDA-044 / E-RUN-006 / E-DEV-088` 已确认 `35057 / 0x88F1 -> psexcel / sub_140605B20`、`工程图 / 输出` Ribbon 分组包含 `{36124, 35057}`、顶部 caption `35057 = 下料表`，且 TODO-066 已归档真实非空 `下料表.xls`；`Dialog #427` 当前已降级为 `OptionFactory` 属性页，不再作为下料表弹窗真值。关闭还需要字段级差距、Excel 公式 / 合并规则、质量公式、Volume722 等价、AutoCAD L2 / 旧插件接受度。 |
| GAP-IDA-008 | P1 | M1/M5 | 输出钢筋 | 用户截图确认 `输出钢筋` 位于 `开始 / 文件` 分组；`E-DEV-048` 已把小样本 `123.stp` 固定为旧图石钢筋几何 witness，并通过 OCCT import probe 与既有 5 轮 STEP selection gate；关闭还需要确认 `sexportbar` / `ysteelout` 与按钮的对应关系、导出窗口、输出格式选项、目标目录和复杂样本 STP。 |
| GAP-IDA-003 | P2 | M后置 | `rebarz` | 命令入口 `sub_14055C3E0` 已知，但语义未确认；暂不阻塞一期主线，后续通过 IDA 反编译和旧图石运行点击闭合。 |
| GAP-IDA-004 | P2 | M后置 | `rebarpost` | 命令入口 `sub_14055C310` 已知，但语义未确认；暂不阻塞一期主线，后续通过 IDA 反编译和旧图石运行点击闭合。 |
| GAP-DEV-001 | P1 | M2 | 新设计文件格式 | M1 入口阻塞已解除：在既有 `*.tsrebar` schema、Fixture A/B/C、negative fixture、validator、standalone Save/Open、STEP selection、Detail writer L0/L1 和 Qt6 应用 Save/Open 契约基础上，`E-DEV-022` 已补入真实 Qt6 C++ runtime gate；`app_save_open_run_001.json` 为 `qt6-app-pass`，`binding_repair_run_001.json` 为 `qt6-binding-repair-pass`。剩余工作转入 M2：把 runtime gate 的状态机迁入正式 Qt6 + OCCT 产品工程，并继续扩展真实工程样本。 |
| GAP-DEV-002 | P1 | M1/M2 | STEP 选择 ID 扩样本残余风险 | 已形成 `19_STEP选择ID稳定性Spike计划.md` 和 `31_STEP选择ID实际运行记录模板与样本清单.md`；`E-DEV-017` 已落地 STEP selection gate、unittest 和首批真实 OCCT 多轮 diff reports：`test_stp` 与 5 个 complex curve STEP 样本均 5 轮通过；`E-DEV-027` 已把 `selection-v1` 引用正式接入 app；`E-DEV-048` 已把旧图石导出钢筋 `123.stp` 固定为 geometry witness，记录 hash、import probe 与 stable refs。后续真实工程 STEP 替换时仍需跑同一 gate。 |
| GAP-DEV-008 | P1 | M1/M2/M3 | LegacyGeometryAdapter 扩能力 | `E-DEV-028` 已完成 P0，`E-DEV-029` 已完成 P1，`E-DEV-030` 已完成 P2A，`E-DEV-031` 已完成 P2B，`E-DEV-032` 已完成 P2C，`E-DEV-033` 已完成 P3A：edge split 语义和前后子段，`E-DEV-034` 已完成 P3B：edge point projection 和 split by projected point，`E-DEV-035` 已完成 P3C：endpoint inward trim summary，`E-DEV-036` 已完成 P3D：point to edge group minimum distance summary，`E-DEV-037` 已完成 P3E：point list spline rebuild summary，`E-DEV-038` 已完成 LegacyWireChain：edge refs to wire chain summary，`E-DEV-039` 已完成 offset curve preview summary，`E-DEV-040` 已完成 section / 剖切 preview summary，`E-DEV-041` 已完成 sweep / pipe preview summary。后续仍需按具体旧命令补真实 topology offset / trim / split、完整 section 行为、旧 sweep/profile/端部规则和旧图石运行对照。 |
| GAP-DEV-007 | Closed-M1 | M1 | 开发前 readiness | 已由 `E-DEV-019/E-DEV-022/E-TECH-005/E-DEV-021` 闭合：readiness gate 当前 78/78 pass、0 error，结果为 `M1-Formal-Ready`；Qt6 C++ runtime Save/Open / binding repair gate 已通过，dependency gate pass，Detail writer L0/L1 pass。后续作为回归门禁保留，不再阻塞进入 Qt6 + OCCT 工程初始化。 |
| GAP-DRAW-005 | P2 | M5 | Detail writer 输出事务 | L0/L1 离线 writer gate 已由 `E-DEV-021` 落地并通过；正式 app `DetailWriter P1` 已由 `E-DEV-047` 接入 domain rebar，覆盖首批 `StbGroup / StbGeo / StbRow / MaterialTable` 字段和安装失败回滚。AutoCAD L2 动态导入、复杂字段完整性、完整质量公式和图上钢筋一致性仍分别由 `GAP-DRAW-001/002/004` 继续跟踪。 |
| GAP-TECH-007 | P2 | M1后续 | 依赖清单 / notice / SBOM | M1 阻塞已闭合：`E-TECH-005` 已确认本机 vcpkg 候选依赖 `qtbase 6.10.3#1` 与 `opencascade 7.9.3#1`，`DEPENDENCIES.md`、`THIRD_PARTY_NOTICES.md`、`licenses/`、`sbom/` 已填实，`Build.DependencyGate` 当前 16/17 pass、0 error、1 warning，结果为 `pass`。剩余 warning 是 CMake 工程尚未创建，CMake 模块 allowlist 扫描后续继续。 |
| GAP-DEV-006 | P1 | M3 | 面配筋 | 按 `14_面配筋专项复刻说明.md` 补运行确认。 |

## 功能按钮缺口

| ID | 项目 | 当前状态 | 需要的证据 | 建议下一步 |
|---|---|---|---|---|
| GAP-FUNC-001 | 剖面圈筋 | 截图中有按钮，但字符串/命令入口未闭合 | 运行点击截图、状态栏提示、Dialog 或 IDA 命令 | 用户运行点击一次；同时继续搜别名 `剖面配筋` / `圈筋`。 |
| GAP-FUNC-002 | 固中心 | 截图中有按钮，但字符串未闭合 | 运行点击截图、状态栏提示、Dialog 或 IDA 命令 | 用户运行确认按钮文字和弹窗。 |
| GAP-FUNC-003 | 组合开 | 截图中有按钮，但可能对应组合拆分/段组分离 | 运行点击截图、右键菜单、命令 ID | 搜 `钢筋组拆分`、`段组断开`、`组合开`。 |
| GAP-FUNC-004 | 未割钢筋 | 截图文字像未割钢筋，Dialog 证据是未剖到钢筋 | 运行点击截图、状态栏、Dialog 标题 | 确认按钮真实文字和功能语义。 |
| GAP-FUNC-005 | 割切面 | 工程图截图是割切面，Dialog 证据是剖切面/转折剖面 | 运行点击截图、Dialog 标题 | 确认是割切面、剖切面还是二者不同入口。 |

## IDA 缺口

| ID | 项目 | 当前状态 | 需要的证据 | 建议下一步 |
|---|---|---|---|---|
| GAP-IDA-001 | `sub_1404DE110` | TODO-020 已用 IDA MCP 补证：确认单选对象、链式实体收集、`api_entity_entity_distance`、默认距离 `0.1`、`sub_1404D10C0(..., distance, 0.8, 1)`；公共链已追到 split / spline / trim / dirty 写回 | 运行点击扇形筋/同心圆 | 确认它对应扇形筋、同心圆还是共用入口；旧 UI 流程仍未闭合。 |
| GAP-IDA-002 | `sub_1404DE720` | TODO-020 已用 IDA MCP 补证：确认单选对象、奇数索引实体抽取、4 组端点距离、`sub_1404D10C0(..., minDistance, selectedEndpointDistance, flag)`；公共链已追到 split / spline / trim / dirty 写回 | 运行点击线配筋 | 确认线配筋选择对象类型、状态栏提示和旧 UI 流程。 |
| GAP-IDA-003 | `rebarz` | 命令入口 `sub_14055C3E0`，语义未确认 | 反编译和运行点击 | 暂列 P2，不作为一期硬实现前置。 |
| GAP-IDA-004 | `rebarpost` | 命令入口 `sub_14055C310`，语义未确认 | 反编译和运行点击 | 暂列 P2，不作为一期硬实现前置。 |
| GAP-IDA-005 | `生成工程图` 命令入口 | `E-IDA-042 / E-IDA-043 / E-IDA-044 / E-DEV-087 / E-RUN-006 / E-DEV-088 / E-DETAIL-004 / E-DEV-089 / E-DEV-090 / E-DEV-091` 已把当前边界收口到：`sub_1406F37B0` 确认 `工程图 / 输出 -> {36124, 35057}`，`36124 / 0x8D1C -> psallc -> sub_140600AA0` 是 Detail 包主写出链，顶部 caption 已静态闭合为 `36124 = 生成工程图`；TODO-066 已归档 RUN-20260609-001 真实输出；TODO-067 已完成真实字段差距报告；TODO-068 已完成当前 DetailWriter 的空 `Detail.xml`、主图 `StbTable/MaterialTable`、副图空 `StbTables` 和 StbTable 表级属性骨架纠偏；TODO-069 已补 StbRow smallTable / mirrorType / mirrorSEFlag 字段骨架；`sub_140600AA0` 里唯一可直接证明的 `DoModal` 继续只收窄到 `output_uncut_steel / Dialog 0x57C / UnCutSteel.TXT` 后置报告链 | 旧插件导入结果、rerun 覆盖行为、旧插件是否必须检查空 `Detail.xml`、StbRow 扩展属性真实公式、AutoCAD L2 | 下一步做 TODO-070，先收窄 lineStb StbGeo 字段条件化；之后再回 AutoCAD L2 和旧插件导入确认。 |
| GAP-IDA-006 | `下料表` 命令入口 | `E-IDA-019 / E-IDA-020 / E-IDA-043 / E-IDA-044 / E-DEV-087 / E-RUN-006 / E-DEV-088 / E-DETAIL-004 / E-DEV-089` 已确认 `psexcel -> 35057 / 0x88F1 -> sub_140605B20`、`工程图 / 输出` 分组包含 `{36124, 35057}`，并把顶部 caption 静态闭合为 `35057 = 下料表`；`Dialog #427` 当前已降级为 `OptionFactory` 属性页，不再作为下料表弹窗真值；TODO-066 已归档真实 `下料表.xls`，Excel COM 只读打开确认三张非空表：`钢筋表 / 钢筋汇总表 / 钢筋下料单`；TODO-067 已完成真实 Excel 三表与当前 RebarScheduleService / 当前无 Excel writer 的差距报告 | Excel 公式/合并规则、质量公式、Volume722 等价、真实 Excel writer 和旧插件/AutoCAD L2 | TODO-068 暂不实现 Excel writer；Excel 字段和公式继续作为 GAP-DRAW-008 后续专项。 |
| GAP-IDA-007 | `seg_steelbargroup` 字段语义 | TODO-020 已用 IDA MCP 成功补证 `sub_1405D5670`：确认 `api_entity_entity_distance -> api_split_curve -> api_curve_spline -> sub_140580950 -> sub_14059B980 -> sub_1405BD0C0` 链和 `0.01 / 0.1 / 5 / 50 / -0.03 / 0.001 / 0.999 / 0.02` 常量；`sub_1405BD0C0` 确认 `ENTITY::backup` 后写 `entity + 72`；`sub_1405E49D0` 确认对象树 `*` dirty 标记。但 `sub_1405D5670` 第 4 个 double 来源、`objA/objB/createdPayload` 业务名、`ptr80/ptr96/ptr88/+64` 完整语义仍未闭合 | 继续做线配筋/弧形筋运行确认、保存前后对比、父子关系对照和 Detail 映射；必要时继续 IDA 追对象构造 / 字段保存函数 | 在 `16` 和 `18` 基础上闭合 SFL 字段语义。 |
| GAP-IDA-008 | `输出钢筋` 对应入口 | 已确认导出类命令 `sexportbar`、`ysteelout` 和旧导出文本结构；用户截图确认按钮位于 `开始 / 文件`；`E-DEV-048` 已把下游消力池小样本 `123.stp` 固定为旧图石钢筋几何 witness，并记录 hash、OCCT import probe 和既有 5 轮 stable refs gate；但 `.stp` 具体对应哪个命令链、导出窗口和完整格式选项未闭合 | 继续反编译 `sub_1404D1540`、`sub_140600890`；补导出窗口截图；再导出 1 个复杂样本 STP | 区分钢筋 STP、普通钢筋文本导出、YSteelOut、工程图下料输出；`123.stp` 只能作为几何 witness / golden 辅助，不能替代旧业务参数证据。 |

## SFL 缺口

| ID | 项目 | 当前状态 | 需要的证据 | 建议下一步 |
|---|---|---|---|---|
| GAP-SFL-001 | 老 SFL 完整读取 | 不作为一期目标；但 SFL 样本证据仍是一期新格式字段校准的 P0 输入 | ACIS BinaryFile 几何解析或老软件导出 | 后置专项，不阻塞新格式 `legacyObject.raw` 和 evidence 保存。 |
| GAP-SFL-002 | `SteelJoint` / `STEELOVERLAP` 样本 | 当前 6 个 SFL 未形成明确统计证据 | 更多样本或深扫关键字 | 后续补样本扫描。 |
| GAP-SFL-003 | `steelData` 字段业务名 | `E-IDA-015/E-IDA-016` 已确认保存和读取骨架、版本阈值和字段类型组，但业务名未全闭合 | 运行钢筋式样/材料窗口字段 + SFL 保存前后差异 | 在新设计文件格式中先按 `legacyObject.raw` 保存，后续逐步命名。 |
| GAP-SFL-004 | `steelbargroup` 大字段表 | `E-IDA-015/E-IDA-016` 已确认保存和读取函数、大字段骨架、版本编码规则和 `Varies_ShangH_LZ` 特殊块，但业务字段未全命名 | Detail 字段对照 + 运行属性窗口 + 保存前后差异 | 在 `18` 字段契约基础上补 `SFL_FIELD_MAP`。 |
| GAP-SFL-005 | `steelbar / seg_steelbargroup` SFL 字段名 | 6 个样本已扫出稳定记录头；`steelbar sub_1405E1C10/sub_1405E1AB0` 与 `seg_steelbargroup sub_1405DA720/sub_1405DA5D0` 已确认 save/read 顺序；但 `steelbar ref72/ref80/ref88/int96/int100` 和 `seg ptr80/ptr96/ptr88/+64` 未命名 | 新建线配筋/弧形筋保存前后对比 + Detail StbGroup/StbGeo 对照 + 父子关系追踪 | 在 `16_seg_steelbargroup字段地图初稿.md` 和 `18` 基础上闭合字段表。 |

## 工程图缺口

- 2026-06-08 路线调整：
  `TODO-050 / TODO-061` 这条接头链路先记录、暂缓，不再作为当前 next；
  `TODO-062 / 生成工程图命令入口 / Ribbon 绑定静态补证 P0` 已完成；
  `TODO-067 / 旧图石真实 Detail 与下料表字段对照 P0` 已完成字段差距报告；
  `TODO-068 / DetailWriter 真实字段差异 P0：空 Detail.xml 与主图表格策略` 已完成格式纠偏；
  `TODO-069 / DetailWriter 真实字段差异 P1：StbRow 扩展属性骨架` 已完成；
  当前主线推进到 `TODO-070 / DetailWriter 真实字段差异 P2：lineStb StbGeo 字段条件化骨架`。

| ID | 项目 | 当前状态 | 需要的证据 | 建议下一步 |
|---|---|---|---|---|
| GAP-DRAW-001 | AutoCAD 动态导入 | 旧插件和样例包存在；`E-DEV-054` 已能输出多图纸 Detail 包；`E-DEV-055` 已生成新三图纸验证包并记录 hash；`E-DEV-058` 已生成 TODO-036 复杂字段骨架包并完成 AutoCAD 环境探测；`E-DEV-059` 已生成 TODO-037 pointStb / FaceEdge 独立包并再次记录 AutoCAD 环境缺失；`E-DEV-060` 已生成 TODO-038 section-line 独立包并保持 `autocadL2=not_run`；`E-DEV-061` 已把 section-line 包转成 TODO-039 手工 L2 确认清单并再次记录 AutoCAD 环境缺失；`E-DEV-062` 已生成 TODO-040 线容器字段骨架包并再次记录 AutoCAD 环境缺失；`E-DEV-063` 已把 line-containers 包转成 TODO-041 手工 L2 确认清单并再次记录 AutoCAD 环境缺失；`E-DEV-064` 已把 pointStb / FaceEdge 包转成 TODO-042 手工 L2 确认清单并再次记录 AutoCAD 环境缺失；`E-DEV-065` 已生成 TODO-043 Others / steeljoint-line 独立字段骨架包并再次记录 AutoCAD 环境缺失；`E-DEV-066` 已把 Others / steeljoint-line 包转成 TODO-044 手工 L2 确认清单并再次记录 AutoCAD 环境缺失。但当前本机未发现 `acad.exe / accoreconsole.exe`，AutoCAD 注册表根键存在但无版本子项，所以 AutoCAD 2020 + FDrawing 插件自动导入仍未运行 | AutoCAD 2020 + FDrawing 插件运行报告、APPLOAD 加载截图、导入结果截图 / 命令行 | 按 `69/72/73/74/75/77/78/80` 的既有手工清单在可启动 AutoCAD 的环境中确认；不要把当前状态写成 L2 pass。 |
| GAP-DRAW-002 | `DetailNN.stl` 字段完整性 | 已确认样例结构；`E-DEV-054` 到 `E-DEV-091` 已累计覆盖多图纸包、复杂容器、General-Info、pointStb、FaceEdge、section-line、line-containers、Others / steeljoint-line、生成工程图 / 下料表入口静态证据、真实 Detail / 下料表字段差距报告、TODO-068 的空 `Detail.xml` / 主图表格策略 / StbTable 表级属性骨架纠偏，以及 TODO-069 的 StbRow 三个扩展属性骨架。当前启动前置条件、symbolcutIOS 静态 producer 链、接头 handler 静态字段、生成工程图顶层入口、工程图 / 下料表命令映射、顶部 caption、真实 Detail 字段差距和真实下料表字段差距都已收敛；但按最新业务优先级，接头相关运行确认暂不作为当前主线。当前真正未闭合的是 lineStb StbGeo 字段条件化、StbRow 三个扩展属性真实公式 / 镜像语义、Excel writer、`Detail.xml + DetailNN.stl` rerun 覆盖策略、旧图石真实非空接头运行样例、AutoCAD L2、以及真实工程图算法 | 多个旧包样例、AutoCAD L2 导入结果、进一步 IDA / 旧图石运行确认 | `TODO-050` 继续 blocked，`TODO-061` 改为 pending，接头链路先记录暂缓；TODO-065 已完成模板门禁，TODO-066 已完成真实工件回填，TODO-067 已完成字段对照，TODO-068 已完成最明显旧包格式纠偏，TODO-069 已完成 StbRow 扩展属性骨架；当前主线切到非接头的 `TODO-070 / DetailWriter 真实字段差异 P2：lineStb StbGeo 字段条件化骨架`；不能声明剖切线 / 隐藏线 / 填充线 / 接头线 / Others 几何 / FaceEdge 生成算法完成。 |
| GAP-DRAW-003 | 隐藏线/剖切线/填充线保真 | 样例有字段，算法未验证 | OCCT HLR / 剖切输出对照 | 后续工程图专项。 |
| GAP-DRAW-004 | 下料表和图上钢筋一致性 | 样例字段清楚；`E-IDA-018` 已确认 `StbGroup.rsdID/groupID/stbNum/stbNumAct` 和 `Std%d/StbGeo%d` 写出链；`E-IDA-019/E-IDA-020/E-IDA-021` 已确认 Excel / 下料命令侧字段和 Ribbon 输出分组；`E-IDA-025` 已补 Detail / XML writer 侧 `sub_140607E30 -> sub_140637400` 写 `StbRow`、`sub_140609190 -> sub_1406382D0` 写 `MatRow` 的链路；`E-DEV-053` 已落地 `RebarScheduleService P0` 并让 DetailWriter 消费同一 schedule service 输出 `StbTable / MaterialTable` 首批统计字段。但完整 `sameGrpNum` 合并规则、MaterialRow 构造 / 分组规则、旧 `singleMass` 来源、`MassNum` 入口参数来源、`Volume722` ACIS 体积等价、AutoCAD L2 导入和旧图石下料 golden 仍未闭合 | AutoCAD L2 导入结果、旧图石下料输出对照、完整质量公式、`sameGrpNum` 合并编号规则、MaterialRow 构造 / 分组规则、`singleMass` 旧材料表来源、`MassNum` 入口来源、`Volume722` 新旧体积对照 | 当前已完成统计 / 下料表 P0，不代表完整下料统计规则或图上钢筋一致性闭合。 |
| GAP-DRAW-005 | Detail writer 输出事务 | L0/L1 离线 writer gate 已通过：`E-DEV-021` 覆盖 `Detail.xml` / `Detail01.stl` 生成、ID 交叉引用、negative mismatch 和失败回滚探针；正式 app `E-DEV-047` 覆盖 domain 接入、`DW003/DW004/DW008` 和安装失败恢复旧包；`E-DEV-054` 已补多 `DetailNN.stl` 输出、全部 DetailNN L0/L1 校验、成功清理旧多余 DetailNN、失败恢复旧多图纸包；`E-DEV-055` 已补 AutoCAD L2 验证包生成和环境阻塞记录；`E-DEV-056` 已补复杂字段静态证据输入；`E-DEV-057` 已补复杂字段骨架输出和测试；`E-DEV-058` 已补复杂骨架包离线检查、FDrawing hash 和 AutoCAD 环境阻塞记录；`E-DEV-059` 已补 pointStb / FaceEdge 独立包离线检查；`E-DEV-060` 已补 section-line Line/Arc/ZValue 独立包离线检查；`E-DEV-061` 已补 section-line L2 运行确认准备包和清单；`E-DEV-062` 已补 line-containers 字段骨架、独立 probe 和 done-report gate 映射；`E-DEV-063` 已补 line-containers L2 运行确认准备包、清单和 TODO-041 done-report gate 映射；`E-DEV-064` 已补 pointStb / FaceEdge L2 运行确认准备包、清单和 TODO-042 done-report gate 映射；`E-DEV-065` 已补 Others / steeljoint-line 字段骨架、独立 probe 和 TODO-043 done-report gate 映射；`E-DEV-066` 已补 Others / steeljoint-line L2 运行确认准备包、清单和 TODO-044 done-report gate 映射；不声明 AutoCAD L2 动态导入通过 | AutoCAD L2 导入报告和算法级工程图输出 | Detail writer 事务 P0 已增强为多图纸和复杂字段骨架；AutoCAD 导入走 `GAP-DRAW-001`，完整算法、公式、pointStb L2、FaceEdge 生成规则、ZValue、section-line / line-container 生成规则、Others / steeljoint-line L2、真实接头线 / Others 几何算法和旧插件 `Detail100.stl` 运行确认走 `GAP-DRAW-002/003/004`。 |
| GAP-DRAW-006 | `Detail.xml` 空 `StyleRoot` 策略 | `E-RUN-006 / E-DETAIL-004 / E-DEV-089 / E-DEV-090` 已确认 RUN-20260609-001 真实 `Detail.xml` 为 14 bytes 的 `<StyleRoot/>`，且当前 `DetailWriter` 已字节级输出 `<StyleRoot/>\r\n` | AutoCAD L2 或旧插件运行确认：旧插件是否要求 `Detail.xml` 存在、是否接受非空样式表、是否忽略空模板 | 代码侧 P0 已闭合；旧插件接受度仍走 AutoCAD L2，不把父目录/外部非空 `Detail.xml` 混入同批证据。 |
| GAP-DRAW-007 | 主图 `StbTable / MaterialTable` 输出策略与表级属性 | `E-DETAIL-004 / E-DEV-089 / E-DEV-090 / E-DEV-091` 已确认真实旧包只有 `Detail01.stl` 带 `StbTable / MaterialTable`，副图 `Detail02..04` 不带；当前 writer 已按主图输出表格、副图空 `StbTables`，补 `StbTable` 11 个表级属性骨架，并补 StbRow smallTable / mirrorType / mirrorSEFlag 三个字段骨架；表级属性公式、StbRow 三个扩展属性真实公式和镜像语义仍未闭合 | IDA 或更多旧包样例确认这些属性公式；AutoCAD L2 确认副图无表策略和默认值容忍度 | TODO-068 已闭合空模板、主图表格策略和表级属性骨架；TODO-069 已闭合 StbRow 三个扩展属性骨架；后续不把默认值当公式完成。 |
| GAP-DRAW-008 | 真实 Excel 下料表三表 writer | `E-RUN-006 / E-DETAIL-004 / E-DEV-089` 已确认真实 `下料表.xls` 含 `钢筋表 / 钢筋汇总表 / 钢筋下料单`，表头包括 `[3]20` 显示格式、单根长(cm)、总长(m)、钢筋合计(T)、混凝土(m3)、焊长(mm)、焊头(个)、单下料长(mm)、总根数(根)`；当前 app 只有内存 `RebarScheduleService` 和 XML 写出，没有 Excel workbook writer | 旧 Excel 更多样本、IDA 追 `sub_140605B20/sub_140602F90` 后续格式、运行确认公式和合并规则 | 后续单独建 Excel writer 切片；不要在 TODO-068 中混入 Excel 实现。 |

## 技术路线缺口

| ID | 项目 | 当前状态 | 需要的证据 | 建议下一步 |
|---|---|---|---|---|
| GAP-TECH-001 | Qt6 使用模块许可证清单 | 已确认 Qt6 可用 LGPL/GPL/商业许可，但项目实际模块未冻结 | 最终 Qt 模块列表、链接方式、发布包清单 | 开发方案中限制首期使用 Qt Core/Gui/Widgets/OpenGL 等基础模块，避免 GPL-only 模块。 |
| GAP-TECH-002 | OCCT 许可证和发布合规 | 已确认 OCCT 6.7.0 以后为 LGPL 2.1 with additional exception；项目发布方式未定 | OCCT 版本、链接方式、第三方 notice | 技术方案中保留 license notice 和依赖清单。 |
| GAP-TECH-003 | OCCT 替代 ACIS 的边界 | OCCT 替代的是新系统几何内核和 STEP/BRep/剖切/距离等能力；不替代 SFL 私有业务对象证据，也不负责读取完整 ACIS BinaryFile | STEP 导入样本、SFL 导出路径、OCCT 版本验证 | 一期主几何输入锁定 STEP/STP，老 SFL 作为业务证据和后置兼容专项。 |
| GAP-TECH-004 | HOOPS 替代边界 | 首期建议 OCCT AIS + Qt Viewer；大模型性能未压测 | 大 SFL/STEP 模型显示性能测试 | M1 阶段做视图帧率、选择、高亮、剖切验证。 |
| GAP-TECH-005 | 钢筋业务无现成开源替代 | 已判断没有可直接替代图石钢筋业务的开源库 | IDA、运行界面、Dialog、SFL、工程图包交叉证据 | 每个钢筋命令建立专项说明，避免误写成“OCCT 自动生成钢筋”。 |
| GAP-TECH-006 | CAD 插件分发和授权边界 | 用户有 CAD 商业版，但旧插件文件的再分发边界未确认 | AutoCAD 授权、旧插件来源、是否只在用户本机使用 | 一期先按“生成包给用户已有 AutoCAD/旧插件导入”设计，不把插件作为新程序发布物。 |
| GAP-TECH-007 | 依赖清单 / notice / SBOM | M1 阻塞已闭合：实际 `DEPENDENCIES.md`、`THIRD_PARTY_NOTICES.md`、`licenses/`、`sbom/` 已落地并填实；`probe_local_dependencies.py` 与 `dependency_probe_run_001` 已确认本机 vcpkg 候选；`Build.DependencyGate` 当前为 `pass`，readiness gate 不再报 Dependency error | 创建 CMake 后扫描 Qt 模块和 OCCT 能力边界；外部分发前补全最终 release package 文件清单 | 保持为 P2 后续风险，不再阻塞当前 M1 readiness。 |
| GAP-TECH-008 | UI / presentation 层 OCCT 枚举暴露 | 外部路线审查指出 `OccViewerWidget` public API 暴露 `TopAbs_ShapeEnum`，`MainWindow` / UI 层会知道 OCCT 选择枚举；这不违反 `domain/rebar` 硬约束，但会让 UI 与 OCCT 绑定偏强 | 后续重构前后的 UI smoke、selection-v1 稳定引用测试和 readiness gate | 建议新增项目自有 `SelectionMode { Face, Edge, Vertex }`，由 `presentation/occ` 内部翻译成 `TopAbs_*`；不在 TODO-040 工程图字段节点中混入。 |
| GAP-TECH-009 | LegacyGeometry DTO Qt 依赖 | 外部路线审查指出 `LegacyGeometryQueryResult` 使用 `QString diagnostic`，后续若 domain/rebar 直接依赖 adapter contract，可能把 Qt 类型带入业务层 | 进入更深钢筋业务前的接口审计、domain/rebar include 扫描、单测回归 | 建议逐步改为 `std::string` 或纯错误码，Qt 文本只在 UI/presentation 层转换；当前不影响 TODO-040。 |

## 开发契约缺口

| ID | 项目 | 当前状态 | 需要的证据 | 建议下一步 |
|---|---|---|---|---|
| GAP-DEV-001 | 新设计文件格式 | M1 入口阻塞已解除：`E-DEV-022` 已落地真实 Qt6 C++ runtime gate，Save/Open 与 binding repair runtime 报告通过；`*.tsrebar` 仍按 `legacyObject.raw + geometryRef + binding + evidence` 作为新工程格式主线 | M2 中把 runtime gate 状态机迁入正式 Qt6 + OCCT 工程；新增真实 STEP/SFL/Detail 样本继续跑 validator、STEP selection 和 Save/Open gate | 新格式替代 `.sfl`，但业务对象由旧图石证据驱动，几何引用由 OCCT / STEP 承载，并用 binding 明确二者关系；不沿用父目录 `.tsrproj/.vtsproj`。 |
| GAP-DEV-002 | STEP 选择 ID 稳定性扩样本 | 已形成 `19_STEP选择ID稳定性Spike计划.md` 和 `31_STEP选择ID实际运行记录模板与样本清单.md`；`E-DEV-017` 已产出真实 STEP 多轮导入 diff：`test_stp` 与 5 个 complex curve STEP 样本均通过；`E-DEV-027` 已把 `selection-v1` 正式接入 app；`E-DEV-048` 已把旧图石导出钢筋 `123.stp` 固定为 witness，当前 import probe 为 754 solids / 3016 faces / 9048 raw edges / 18096 raw vertices，既有 stable refs 为 face=3016、unique edge=4524、unique vertex=3016；当前 Phase1 seed 样本不再阻塞 M1 | 后续真实工程 STEP 样本进入时，仍需执行同一路径 3 次、复制路径 1 次、重启 1 次，并记录 hash、OCCT 版本、face/edge/vertex match rate、重复 fingerprint 和 unmatched 项 | 保持 P1 residual；新 STEP 样本未跑 gate 前不得作为正式 binding 证据。 |
| GAP-DEV-008 | LegacyGeometryAdapter 扩能力 | `E-DEV-028` 已把 OCCT EDGE / FACE P0 查询包装成 legacy 几何摘要，`E-DEV-029` 已补 P1 bbox、sample、pointAtParameter、face boundary wires、fingerprint 和错误诊断矩阵，`E-DEV-030` 已补 edge 切向、距离和最近点，`E-DEV-031` 已补 face boundary edge stableId 和 edge-face 接触/重叠代表点，`E-DEV-032` 已补 edge 参数区间和子段采样，`E-DEV-033` 已补 edge split 语义，`E-DEV-034` 已补 edge point projection 和 split by projected point，`E-DEV-035` 已补 endpoint inward trim summary，`E-DEV-036` 已补 point to edge group minimum distance summary，`E-DEV-037` 已补 point list spline rebuild summary，`E-DEV-038` 已补 edge refs to wire chain summary，`E-DEV-039` 已补 edge offset preview summary，`E-DEV-040` 已补 face-plane section preview summary 和 edge-face empty intersection set，`E-DEV-041` 已补 edge circular sweep preview summary；默认 CTest 8/8 pass | 后续按具体旧命令补 topology trim/split、真实 topology offset、完整 section 行为、旧 sweep/profile/端部规则和旧图石运行对照 | 保持 P1 residual；当前 offset / section / sweep 都是 preview summary / spike，不代表完整 ACIS 等价或 topology mutation；钢筋生成前必须把对应命令需要的几何能力逐项补齐。 |
| GAP-DEV-003 | 命令撤销/重做 | 命令契约已提出，但旧图石撤销粒度未确认 | 运行确认：创建、移动、组合并后放弃/重做行为 | 先按命令事务实现，后续对齐旧图石。 |
| GAP-DEV-004 | dirty 标记保存口径 | 已提出 Project/Rebar/Drawing/View dirty，但旧图石保存提示未确认 | 运行点击后关闭工程观察提示 | 对面配筋、移动、剖切面、显示隐藏做运行确认。 |
| GAP-DEV-005 | 内部 ID 与旧编号关系 | Detail/SFL 有 `rsdID/groupID/stbNum` 等字段，但生成规则未闭合；`E-DEV-052` 的 Rebar.Edit.Copy P0 已故意清空复制件 `rsdId / displayNumber / actualNumber / sequenceNo / legacyRaw` 并登记 unresolved，避免把源对象旧编号误继承成确定规则；`E-IDA-025` 确认 `StbRow.rsdID` 存在多分支生成，`sameGrpNum = a1[1]`，`stbNumSum = a1[3]`，但 `a1` 聚合结构如何生成仍未追完；`E-DEV-053` 只在 P0 中从现有 domain 字段派生统计字段，`sameGrpNum` 保守固定为 1 | SFL 字段地图 + 工程图包字段对照 + 旧图石复制 / 统计 / 下料表运行确认；继续 IDA 追 `sub_140606560 / sub_140609860` 中 table row 聚合结构和旧编号分支 | 当前用新系统内部 ID 保证引用一致；旧编号、显示编号、下料编号、复制后的编号生成规则和统计合并编号仍是后续缺口。 |
| GAP-DEV-009 | core target 分层偏粗 | 外部路线审查指出 `tsrebar_core` 同时包含 command、domain、legacy geometry DTO、ui/model tree 等，早期切片可接受，但长期会增加业务层被 UI/几何反向污染的风险 | CMake target 依赖图、domain include 扫描、CTest/readiness 回归 | 后续建议拆出 `tsrebar_domain / tsrebar_legacy_contract / tsrebar_command / tsrebar_ui` 或等价分层；不在 TODO-040 工程图字段节点中混入。 |
| GAP-DEV-010 | 旧图石启动阻塞提示链 | `E-DEV-069` 已确认当前 `VisualTS.exe` 启动后先弹出标题为 `提示` 的阻塞框，截图可见文本前缀 `请检查网线是否...`；`E-IDA-030 / E-DEV-070` 已静态闭合 `sub_1406BBFC0 -> sub_1406BC3B0 -> sub_14070C760(...)` 启动主链、`41 -> 许可已过期`、其他非 `0` 失败码回落到 `请检查网线是否接好`，并形成用户手工解除前置条件清单；`E-DEV-071` 已补本机预检：基础网络在线、`SafeNet Sentinel / Sentinel LDK` 数据目录存在，但 `hasplms / aksfridge` 未安装、`127.0.0.1:1947/475/6001` 未监听、相关环境变量未命中、常见 `SafeNet` 安装目录未发现；`E-IDA-031` 已补一层：`请检查网线是否接好` 是宽兜底许可初始化失败文案，许可上下文未 ready 或对象映射失败也会走这条 fallback，因此当前仍不能把问题直接缩成“只可能是网络许可故障”；`E-RUN-003 / E-RUN-004` 已进一步确认旧图石启动需要 USB 狗，插狗后可进入主界面、可打开 `SFL`、不需要内网，但现场仍需本地 HASP 安装条件，且工作区已有安装包目录；`E-DEV-072` 已把 `TODO-050` 的阻塞明确为用户现场运行条件尚未满足，而不是启动链来源不明；`E-DEV-073` 已进一步完成 symbolcutIOS 静态 producer 补证；`E-DEV-074` 已完成接头 UI/Ribbon 直接绑定静态 stop point；`E-DEV-075` 已完成接头 context menu / command dispatch 静态 stop point；`E-DEV-076` 已完成接头 handler 对象筛选静态分类；`E-DEV-077` 已完成接头 handler 动作函数字段语义深追；`E-DEV-078` 已完成 `sub_1405DB6C0` 几何重建核心静态深追；`E-DEV-079` 已进一步闭合 owner 类型层和 Dialog #428 OK 链；`E-DEV-080` 已继续闭合 JoingSegDlg message map stop point 和 `generated node+112` 的生成链边界；`E-DEV-081` 又把 `generated node+112` 的展示链收紧到旧 Excel 下料表 `焊头(个)`、`单下料长(mm)` 与状态栏 `焊接 / 绑扎 / 套筒连接` 口径；`E-DEV-082` 又确认当前工作区内并没有可直接复用的真实运行证据，现有 `1.xls` 和两张 PNG 都已排除；`E-DEV-083` 又把 TODO-061 的 runtime capture gate 补到可拒绝伪文件和路径误填的程度。也就是说，当前剩下的重点不再是纯静态链，而是用户现场运行样例采集、Excel 导出和状态栏截图确认，以及 generated node+112 最终业务名 / 旧 UI 真实 caption 等最后收口问题 | 当前机器是否已真正完成 HASP 安装、后续样例采集时的 stop point / 输出文件 | 不再围绕启动条件打转；`TODO-050` 继续 blocked，`TODO-060` 已完成候选工件排查和最小清单闭环；`TODO-061` intake/gate 已准备好，但按当前业务优先级先记录保留，不再作为当前 next；除非用户明确恢复接头优先级，否则不自动再次启动旧图石，也不自动回到接头运行确认。 |
| GAP-REB-E-001 | 钢筋移动完整旧行为 | TODO-029 已用 IDA MCP 补证 `barmove -> Input_Choice`，Move P0 已实现领域层整体平移；但旧图石 `Input_Choice` 其他 mode、ACIS topology mutation、dirty / undo / 失败提示、运行 golden 均未闭合 | 继续 IDA 查 `Input_Choice` 其他 message handler、旧图石运行确认移动后保存提示和撤销行为、SFL/Detail/STP 对比 | 当前 P0 只作为 VisualTS 证据对齐的 domain move，不声明完整旧行为。 |
| GAP-REB-E-002 | 钢筋拷贝完整旧行为 | TODO-032 已用 IDA MCP 补证 `scopy -> Input_Choice copyFlag=1 -> sub_1405989C0 -> sub_1405AA5D0`，Copy P0 已实现领域层复制后累计平移；复制件会生成新 group / bar / segment，保持引用一致，并把 binding / geometryRef 标为待重新绑定；但旧图石完整 ACIS topology clone、旧编号生成、dirty / undo、错误提示、UI handler 和运行 golden 均未闭合 | 继续 IDA 查 `sub_1405989C0` 的 `+80/+88/+96/+104` 引用链业务名、旧图石运行确认复制后编号 / 保存提示 / 撤销行为、SFL/Detail/STP 对比 | 当前 P0 只作为 VisualTS 证据对齐的 domain copy；`copyCount 1..1000` 是 P0 安全边界，不是完整旧 UI 失败口径。 |
| GAP-DEV-006 | 面配筋专项运行细节 | 已写专项说明，但 `应用/确定/取消`、预选面、自动连接等行为未运行确认 | 旧图石运行面配筋操作截图和状态栏 | 按 `14_面配筋专项复刻说明.md` 的待运行确认清单逐项补证据。 |
| GAP-DEV-007 | 开发前 readiness 实际打勾 | 已闭合 M1：`readiness_gate_run_001` 当前 78/78 pass、0 error，结果为 `M1-Formal-Ready`；`docs/phase1/readiness_run_001.md` 已同步；真实 Qt6 runtime、dependency gate、Detail writer L0/L1 均已通过 | 后续每次进入正式工程阶段仍保留 readiness gate 回归 | 关闭 M1 阻塞；后续只作为回归门禁。 |

### 开发契约缺口最新补充

- `E-DEV-020` 已落地 `tools/app_save_open_gate/`，作为 pre-runtime 状态机回归参考。
- `E-DEV-022` 已落地 `tools/qt6_runtime_gate/`、`app_save_open_run_001.json`、`binding_repair_run_001.json` 和 `binding_repair_preview_001.md`。
- 当前 app runtime gate 决策为 `qt6-app-pass` / `qt6-binding-repair-pass`，readiness gate 不再把 Qt6Application 判为 error。
- `E-DEV-021` 已落地 `tools/detail_writer_gate/` 和 `docs/phase1/detail_writer_reports/`，Detail writer L0/L1 决策为 `l0-l1-pass`。
- `E-DEV-029` 已落地 `LegacyGeometryAdapter P1`，EDGE / FACE bbox、采样、boundary loop、fingerprint 和诊断矩阵已通过真实 `123.stp` CTest。
- `E-DEV-030` 已落地 `LegacyGeometryAdapter P2A`，edge 切向、selection ref 距离和最近点对已通过真实 `123.stp` CTest。
- `E-DEV-031` 已落地 `LegacyGeometryAdapter P2B`，face boundary edge stableId 和 edge-face 接触/重叠代表点已通过真实 `123.stp` CTest。
- `E-DEV-032` 已落地 `LegacyGeometryAdapter P2C`，edge 参数区间、子段长度、子段 bbox 和子段采样已通过真实 `123.stp` CTest。
- `E-DEV-033` 已落地 `LegacyGeometryAdapter P3A`，edge split 语义和前后子段已通过真实 `123.stp` CTest。
- `E-DEV-034` 已落地 `LegacyGeometryAdapter P3B`，edge point projection 和 split by projected point 已通过真实 `123.stp` CTest。
- `E-DEV-035` 已落地 `LegacyGeometryAdapter P3C`，endpoint inward trim summary 已通过真实 `123.stp` CTest。
- `E-DEV-036` 已落地 `LegacyGeometryAdapter P3D`，point to edge group minimum distance summary 已通过真实 `123.stp` CTest。
- `E-DEV-037` 已落地 `LegacyGeometryAdapter P3E`，point list spline rebuild summary 已通过真实 `123.stp` CTest。
- `E-DEV-038` 已落地 `LegacyWireChain`，edge refs to wire chain summary 已通过真实 `123.stp` CTest。
- `E-DEV-039` 已落地 `LegacyGeometryAdapter offset preview`，edge ref 到 offset curve preview summary 已通过真实 `123.stp` CTest；该能力是 preview summary / spike，不等价于完整 ACIS offset 或 topology mutation。
- `E-DEV-040` 已落地 `LegacyGeometryAdapter section preview`，face ref + legacy plane 到 section preview summary 已通过真实 `123.stp` CTest；该能力是 preview summary / spike，不等价于完整 ACIS section、剖切面 UI 或 topology mutation。
- `E-DEV-041` 已落地 `LegacyGeometryAdapter sweep preview`，edge ref + circular radius 到 sweep / pipe preview summary 已通过真实 `123.stp` CTest；该能力是 preview summary / spike，不等价于完整 ACIS sweep、钢筋实体持久化或 topology mutation。
- `E-DEV-042` 已落地 `domain/rebar` 钢筋领域模型冻结 P1，`SteelData / SteelBarGroup / SteelBar / SteelBarSegment` 可承载 Detail 关键字段、ID 引用、legacy raw、geometryRef、binding、evidence 和 unresolved 字段；默认 CTest 9/9 pass，domain/rebar OCCT 泄漏检查通过；该能力不等价于旧图石钢筋创建算法、Detail writer 接入、UI 命令 handler 或旧 SFL 全字段语义闭合。
- `E-DEV-043` 已落地旧命令契约绑定 P1，`Rebar.Create.LineGroup / Rebar.Create.ArcGroup / RebarGroup.TrimByLine / RebarGroup.TrimByFace` 可查询、可注册稳定 `NotImplemented` placeholder；右键裁剪命令只作为 `ContextMenu` 命令契约，不渲染到 Ribbon；默认 CTest 9/9 pass，readiness gate 78/78 pass。该能力不等价于线筋 / 弧筋创建算法或裁剪业务算法。
- `E-DEV-044` 已落地旧线筋 / 弧筋创建算法 P0，`RebarGroupCreator` 可按 legacy edge ref / old parameters 输出 `SteelData -> SteelBarGroup -> SteelBar -> SteelBarSegment`，并通过 `LegacySegmentCurveNormalizeRequest` 显式承载 `sub_1405D5670` 的 P0 normalizer 常量；默认 CTest 10/10 pass，readiness gate 78/78 pass。该能力不等价于完整旧 UI、失败提示、golden 输出、Detail writer 或裁剪编辑算法。
- `E-DEV-045` 已落地 AIS 钢筋显示映射 P0，`RebarAisPresentationAdapter` 可把 domain `SteelBarGroup / SteelBarSegment` 映射成 presentation/occ 层 `AIS_Shape`，覆盖 line segment、arc segment、missing group 和 empty group 稳定诊断；默认 CTest 11/11 pass，readiness gate 78/78 pass，domain/rebar 无 AIS / OCCT 泄漏。该能力不等价于旧 HOOPS 显示样式 1:1、UI handler、Detail writer 或 golden 输出。
- `E-DEV-046` 已落地新设计文件格式 runtime P1，`TsRebarProjectRuntime` 可保存 / 读取 STEP 来源、selection-v1 refs、rebar groups、legacyObject.raw、geometryRef、binding、evidence 和 unresolved 字段；默认 CTest 12/12 pass，runtime 导出包外部 validator 为 warning-only / 0 error，readiness gate 78/78 pass，domain/rebar + project/runtime 无 OCCT / AIS 泄漏。该能力不等价于完整 UI Save/Open、Detail writer、旧 SFL 兼容、binding repair 交互或 golden 输出。
- `E-DEV-047` 已落地正式 app DetailWriter P1，`DetailWriter` 可把 domain `SteelData / SteelBarGroup / SteelBar / SteelBarSegment` 输出为 `Detail.xml + Detail01.stl` 首批字段，覆盖 `StbGroup / StbGeo / StbRow / MaterialTable` 首批映射、`DW003/DW004/DW008` 和失败恢复旧包；默认 CTest 13/13 pass，readiness gate 78/78 pass，domain/rebar + drawing + project 无 OCCT / AIS 泄漏。该能力不等价于 AutoCAD L2 动态导入、完整工程图生成、完整质量公式、旧图石 golden 或完整下料统计规则。
- `E-DEV-048` 已落地 TODO-025 旧图石输出钢筋 STP 样本入库验证，`123.stp` 固定为 `tushi_rebar_123_stp` 几何 witness，源 SFL sha256 为 `a5645635ff77346e1dacccbac57b7f594d1278d74bf70f4be762dd93ced90515`，STP sha256 为 `6a3c4f2044c2cc1f1123f0f58c61b2c869fbad3110a3585fdab49e69daf52a2a`，当前 app import probe 为 754 solids / 3016 faces / 9048 raw edges / 18096 raw vertices / METRE，既有 5 轮 STEP selection gate 为 pass。该证据不等价于旧钢筋创建算法、命令参数、统计公式、Detail 正确性或新系统已能生成同样几何。
- `E-DEV-049` 已落地 TODO-027 / M2-UI-001 旧 UI 功能入口 P1，`LegacyUiCommandMap` 覆盖 `17_一期按钮追溯与命令占位矩阵.md` 中的一期入口，Ribbon `QAction` 携带 `commandKey / legacyUiPath / evidenceIds / gapIds / implementationState` 等追溯 metadata，`tsrebar_app --smoke` 会校验这些字段并确认 ContextMenu 命令不渲染到 Ribbon；默认 CTest 13/13 pass，readiness gate 78/78 pass，xhigh 第三轮 allow_commit。该证据不等价于旧弹窗字段、状态栏提示、钢筋创建/编辑/查询/统计/出图业务算法或 golden。
- `E-DEV-050` 已落地 TODO-028 / M2-Gate-001 CSE readiness gate 扩展，`Phase1.ReadinessGate` 新增 RouteGuardrail，当前 strict 结果为 84/84 pass、0 error、0 warning；该 gate 能自动检查 domain/rebar OCCT/AIS 泄漏、保护层泄漏、父目录 rebar 业务引用、todo 状态和 done 节点报告存在性。该证据不等价于旧业务算法或输出结果 1:1。
- `E-DEV-053` 已落地 TODO-030 / M2-Stats-001 钢筋统计 / 下料表 P0，新增 `RebarScheduleService`，并让 `DetailWriter` 的 `StbTable / StbRow / MaterialTable / MatRow` 消费同一 schedule service。当前 P0 可输出首批字段和材料聚合；材料行暂按 `diameter + steelLevel` 聚合，任一材料行缺失 / 冲突 `singleMass` 时全表 `Mass` 和所有 `MatRow.massSum` 统一 deferred 为 0。`sameGrpNum` 完整合并规则、MaterialRow 构造 / 分组规则、旧 `singleMass` 来源、`MassNum` 入口来源、`Volume722` 旧 ACIS 体积等价、Excel 输出、AutoCAD L2 和 golden 仍未闭合。
- `E-DEV-054` 已落地 TODO-031 / M2-Drawing-001 DetailWriter 多图纸 P0，IDA MCP 新增 `E-IDA-026` 确认旧 `Detail01..Detail09 / Detail10+` 命名规则；正式 app 可按多个 `DrawingView` 输出多张 `DetailNN.stl`，L0/L1 校验覆盖所有生成图纸，成功安装会删除旧多余 DetailNN 并保留非 Detail 文件，安装失败恢复旧 Detail 包。完整工程图、剖切线、隐藏线、填充线、AutoCAD L2、旧插件 `Detail100.stl` 运行确认和 golden 仍未闭合。
- `E-DEV-055` 已落地 TODO-033 / M2-Drawing-002 AutoCAD L2 导入验证 P0，新增 `detail_l2_fixture_probe` 生成新 Detail 三图纸验证包，`Detail.xml / Detail01.stl / Detail02.stl / Detail03.stl` 的 hash 和根节点已记录；旧 `FDrawing.arx / FDrawingObj.dbx` 文件存在且 hash 已记录；当前本机未发现 `acad.exe / accoreconsole.exe`，所以 AutoCAD L2 自动导入未运行。该证据只证明验证包和阻塞记录齐备，不证明旧插件已接受新包。
- `E-DEV-056` 已落地 TODO-034 / M2-Drawing-003 Detail复杂字段静态证据 P0，从旧 `Detail01.stl` 样例整理 `continue-line / hidden-line / central-line / section-line / hatch-line / Others / steeljoint-line` 容器、section Line/Arc、pointStb 和 FaceEdge 字段；IDA MCP 复核 `FDrawing.arx` 中 `CViewInfo / CWSNLineDim / CWSNPointDim / CWSNSteelBarTable / CWSNMaterialTable` 等插件对象符号。该证据只证明静态字段和插件对象模型存在，不证明 AutoCAD L2 通过、旧插件容忍度、剖切线 / 隐藏线 / 填充线 / 接头线算法或 golden。
- `E-DEV-057` 已落地 TODO-035 / M2-Drawing-004 DetailWriter复杂字段骨架 P0，正式 app `DetailWriter` 输出 `PartDetailDrawing` 复杂容器骨架、`General-Info` 首批扩展默认字段和 `pointStb StbGeo shapeType=C` 字段骨架；默认 CTest 17/17 pass，strict readiness gate 84/84 pass，OCCT/AIS 泄漏扫描无匹配，xhigh review 为 allow_commit。该证据不证明 AutoCAD L2 通过、旧插件接受新包、`CompanyName` 旧默认值、FaceEdge、ZValue、剖切线 / 隐藏线 / 填充线 / 接头线算法或 golden。
- `E-DEV-058` 已落地 TODO-036 / M2-Drawing-005 AutoCAD L2复杂字段骨架导入验证 P0，`detail_l2_fixture_probe` 生成 TODO-036 三图纸复杂骨架包并离线确认 `PartDetailDrawing num=8`、复杂容器和 `General-Info` 首批扩展字段；旧 `FDrawingObj.dbx / FDrawing.arx` 文件存在且 hash 已记录；本机未发现 `acad.exe / accoreconsole.exe`，AutoCAD 注册表根键存在但无版本子项，所以 AutoCAD L2 自动导入未运行。xhigh review 指出 `pointStb` 混入会污染 L2 变量，主流程已从 TODO-036 包中移除点筋 fixture。本证据不证明旧插件接受当前包、pointStb L2、FaceEdge、完整工程图或 golden。
- `E-DEV-059` 已落地 TODO-037 / M2-Drawing-006 DetailWriter pointStb / FaceEdge 兼容字段骨架 P0，正式 app `DetailWriter` 输出 `pointStb StbGeo shapeType=C` 和显式 `FaceEdge shapeType=L/A` 字段骨架；`detail_l2_fixture_probe --fixture point-face-edge` 生成三图纸独立包并离线确认 `pointFaceEdge.passed=true`；默认 CTest 17/17 pass，strict readiness gate 84/84 pass，OCCT/AIS 泄漏扫描无匹配。该证据不证明 AutoCAD L2 通过、旧插件接受新包、点筋生成算法、FaceEdge 生成规则、剖切线 / 隐藏线 / 填充线 / 接头线算法或 golden。
- `E-DEV-060` 已落地 TODO-038 / M2-Drawing-007 DetailWriter section-line Line/Arc/ZValue 字段骨架 P0，正式 app `DetailWriter` 输出 `section-line / lines / LineN` 和 `section-line / Arcs / ArcN` 字段骨架，并保留旧样例确认的 `ZValue` 字符串；`detail_l2_fixture_probe --fixture section-line` 生成三图纸独立包并离线确认 `sectionLine.passed=true`。该证据不证明 AutoCAD L2 通过、旧插件接受新包、真实剖切线算法、隐藏线 / 填充线 / 接头线算法、`ZValue` 语义或 golden。
- `E-DEV-061` 已落地 TODO-039 / M2-Drawing-008 DetailWriter section-line AutoCAD L2 运行确认准备 P0，复用 `section-line` 独立包生成 TODO-039 三图纸 Detail 包和手工 L2 确认清单，记录 FDrawing 插件 hash，确认当前本机 `acad.exe / accoreconsole.exe` 未发现，AutoCAD registry 根键存在但无版本子项，所以 `autocadL2=not_run`。该证据不证明 AutoCAD L2 通过、旧插件接受新包、真实剖切线算法、`ZValue` 语义或 golden。
- `E-DEV-062` 已落地 TODO-040 / M2-Drawing-009 DetailWriter 线容器字段骨架 P0，正式 app `DetailWriter` 可输出 `continue-line / hidden-line / central-line / hatch-line` 下 `LineN start_x/start_y/end_x/end_y/ZValue` 字段；`detail_l2_fixture_probe --fixture line-containers` 已生成三图纸独立包并离线确认 `lineContainers.passed=true`；当前 AutoCAD 环境仍 `not_found`，所以 `autocadL2=not_run`。该证据不证明 AutoCAD L2 通过、旧插件接受新包、真实连续线 / 隐藏线 / 中心线 / 填充线算法、完整工程图或 golden。
- `E-DEV-063` 已落地 TODO-041 / M2-Drawing-010 DetailWriter line-containers AutoCAD L2 运行确认准备 P0，复用 `line-containers` 独立包生成 TODO-041 三图纸 Detail 包和手工 L2 确认清单，记录 FDrawing 插件 hash、旧样例包 hash 和新包 hash，确认当前本机 `acad.exe / accoreconsole.exe` 未发现，AutoCAD registry 根键存在但无版本子项，所以 `autocadL2=not_run`。该证据不证明 AutoCAD L2 通过、旧插件接受新包、真实连续线 / 隐藏线 / 中心线 / 填充线算法、完整工程图或 golden。
- `E-DEV-064` 已落地 TODO-042 / M2-Drawing-011 DetailWriter pointStb / FaceEdge AutoCAD L2 运行确认准备 P0，复用 `point-face-edge` 独立包生成 TODO-042 三图纸 Detail 包和手工 L2 确认清单，记录 FDrawing 插件 hash、旧样例包 hash 和新包 hash，确认当前本机 `acad.exe / accoreconsole.exe` 未发现，AutoCAD registry 根键存在但无版本子项，所以 `autocadL2=not_run`。该证据不证明 AutoCAD L2 通过、旧插件接受新包、点筋生成算法、FaceEdge 生成规则、真实工程图算法或 golden。
- `E-DEV-065` 已落地 TODO-043 / M2-Drawing-012 DetailWriter Others / steeljoint-line 字段骨架 P0，`DetailWriter` 保持输出 `Others` 空容器和 `steeljoint-line / joints` 容器骨架；`detail_l2_fixture_probe --fixture others-steeljoint` 已生成三图纸独立包并离线确认 `othersSteeljoint.passed=true`；当前 AutoCAD 环境仍 `not_found`，所以 `autocadL2=not_run`。该证据不证明 AutoCAD L2 通过、旧插件接受新包、真实接头线算法、Others 几何算法、完整工程图或 golden。
- TODO-043 xhigh 只读 review 实际返回 `needs_fix`，Critical 为 none，Important 指出 `34 / 99` 曾把 `79` 写成手工 L2 清单编号；主流程已修正为 `TODO-044` 单独生成 Others / steeljoint-line L2 清单，并关闭该子代理。
- `E-DEV-066` 已落地 TODO-044 / M2-Drawing-013 DetailWriter Others / steeljoint-line AutoCAD L2 运行确认准备 P0，复用 `others-steeljoint` 独立包生成 TODO-044 三图纸 Detail 包和手工 L2 确认清单，记录 FDrawing 插件 hash、旧样例包 hash 和新包 hash，确认当前本机 `acad.exe / accoreconsole.exe` 未发现，AutoCAD registry 根键存在但无版本子项，所以 `autocadL2=not_run`。该证据不证明 AutoCAD L2 通过、旧插件接受新包、真实接头线算法、Others 几何算法、真实工程图算法或 golden。
- `E-IDA-029 / E-DEV-068` 已落地 TODO-046 / M2-Drawing-015，当前已闭合 `JointRuler / JointDistbet / JointWeldLength` 参数链、`JointWeldLength / 2000.0` 半长公式、`pattern` raw byte `'L'`、`Others / symbolcutIOS` gate 和额外弧线 / `DrawTaoTong` 关系；旧 UI 触发、owning enum / 结构名、`Varies_ShangH_LZ` 导入覆盖语义、旧图石非空运行样例和旧插件接受度仍未闭合。
- `E-DEV-069` 已落地 TODO-047 / M2-Drawing-016，当前旧图石运行样例采集在启动期即被 `提示 / 请检查网线是否...` 阻塞；已记录 `VisualTS.exe` hash、启动截图 hash 和 stop point，但尚未进入旧图石主界面，尚未打开 `SFL`，也未导出新的 `DetailNN.stl`。
- 2026-06-05 执行 `TODO-013` 时 IDA MCP 可发现工具，但没有绑定数据库；传 `VisualTS.exe.i64` 路径返回 `Database/session not found`。本轮 P3E 没有新增 IDA 证据，旧逻辑仍以 `15/16` 已记录证据为准。
- 2026-06-05 执行 `TODO-014` 时 IDA MCP 仍没有绑定数据库。本轮 wire chain 没有新增 IDA 证据，旧 `ENTITY_LIST` 语义仍以 `35` 的 adapter 边界和既有 `15/16` 文档为准。
- 2026-06-06 执行 `TODO-015` 时 IDA MCP `idb_list` 返回空会话。本轮 offset preview 没有新增 IDA 证据，旧偏移语义仍以 `15/16/35/46` 已记录证据和 adapter 能力边界为准。
- 2026-06-06 执行 `TODO-016` 时 IDA MCP `idb_list` 返回空会话。本轮 section preview 没有新增 IDA 证据，旧剖切 / 割切面 UI 和业务语义仍需后续 IDA 或旧图石运行确认闭合。
- 2026-06-06 执行 `TODO-017` 时没有新增 IDA 证据。本轮 sweep preview 只验证 OCCT pipe 能力可在 adapter 内返回 legacy summary；旧图石钢筋实体生成规则、profile 半径来源、端部处理和导出策略仍需后续 IDA 或旧图石运行确认。
- 2026-06-06 执行 `TODO-020` 时 IDA MCP 成功打开 `VisualTS.exe.i64`，session 为 `visualts_i64_todo020`。本轮新增 `E-IDA-022`，补强旧线筋 / 弧筋公共链到可进入 P0 业务创建 spike 的证据级别；但旧 UI 流程、`sub_1405D5670` 第 4 个 double 来源和对象业务名仍保留 GAP。
- 当前 readiness gate 为 78/78 pass、0 error，M1-Formal 已可放行。
- `GAP-DEV-001` 转为 M2 正式工程迁移和样本扩展工作，不再阻塞 M1 开发入口。

## 运行界面确认清单

后续需要用户运行图石辅助确认的首批清单：

1. 点击 `剖面圈筋`，截图弹窗或状态栏提示。
2. 点击 `固中心`，截图弹窗或状态栏提示。
3. 点击 `组合开`，截图弹窗或状态栏提示。
4. 点击查询页 `未割钢筋`，确认标题是否为 `未剖到钢筋`。
5. 点击工程图片 `割切面`，确认弹窗标题和字段。
6. 点击 `生成工程图`，确认输出目录、文件名和是否生成 `Detail.xml + DetailNN.stl`。
7. 点击 `下料表`，确认是否弹 Dialog #427 或直接导出 Excel。
8. 点击 `输出钢筋`，导出 `.stp`，保留文件并记录导出窗口、路径、hash 和 OCCT 导入统计。

## 当前优先级

2026-06-08 最新业务口径：

```text
接头这块目前滞后，先记录，不作为当前开发主线。

领导已明确：
  -> 现阶段先不用考虑接头。

因此当前执行边界固定为：
  -> `TODO-050` 继续保持 blocked
  -> `TODO-061` 继续保持 pending
  -> 不再把接头运行确认、接头截图回填、接头样例采集设为 next
  -> 只保留已有证据、缺口、采样模板和恢复入口
  -> 当前主线继续按非接头方向执行 `TODO-070`
```

最新补充：

```text
TODO-064 已完成 E-IDA-044 / E-DEV-086：
  -> `36124 = 生成工程图`
  -> `35057 = 下料表`
  -> 旧资源采用 `描述/help \n caption` 模式
  -> `sub_140600AA0` 里唯一可直接证明的 DoModal
     已收窄到 `output_uncut_steel / Dialog 0x57C / UnCutSteel.TXT`
     后置报告链
  -> `Dialog #427` 已降级为 `OptionFactory` 属性页，
     不再作为下料表弹窗真值

当前 next：
  -> `TODO-070 / DetailWriter 真实字段差异 P2：lineStb StbGeo 字段条件化骨架`
```

```text
TODO-057 已完成 E-IDA-039 / E-DEV-079：
  -> dword_140993E74 / dword_140993EC8 / dword_140993E1C 已确认分别对应
     steelbar / steelbargroup / seg_steelbargroup。
  -> Dialog #428 已闭合到 sub_1405D94C0 -> sub_14045D580 -> sub_14045D720
     -> sub_1405CB160 -> sub_1405B7350 的 OK 链。
  -> child+112 已可收紧为 phase / position / 起始偏移，int mm。
  -> 这轮仍不关闭 generated node+112、standalone Apply、旧 UI caption、
     非空运行样例、AutoCAD L2、真实接头线算法、真实 Others 几何算法或 golden。

下一轮建议：
  -> TODO-058 / JoingSegDlg message map 与 child/node+112 字段收口 P1。
  -> 继续 IDA MCP，只读静态证据，不启动旧图石，不安装 HASP，不改 app 业务代码。
```

TODO-058 已完成 E-IDA-040 / E-DEV-080：

```text
  -> JoingSegDlg 的静态 message map 已闭合到：
       sub_14045D700 -> off_14075B110 -> 0x14075B090 entries
  -> entries 只稳定命中 3 个 radio handler：
       1380 -> sub_14045DA00
       1384 -> sub_14045DA10
       1381 -> sub_14045DA20
  -> 当前没有静态证据表明 JoingSegDlg 自身 message map 中存在独立 Apply。
  -> child+112 继续保持 phase / position / 起始偏移，int mm。
  -> generated node+112 已收紧为 child+88 生成链节点上的 int mm 贡献字段，
     被 sub_1405DC6C0 汇总并进入格式化 / 状态显示 / rebuild 前置判断。
  -> 这轮仍不关闭 generated node+112 最终业务调试名、
     旧中文 caption、运行期静态表外 Apply、非空运行样例、
     AutoCAD L2、真实接头线算法、真实 Others 几何算法或 golden。

下一轮建议：
  -> TODO-059 / generated node+112 展示/状态栏字符串链静态深追 P1。
  -> 继续 IDA MCP，只读静态证据，不启动旧图石，不安装 HASP，不改 app 业务代码。
```

TODO-059 已完成 E-IDA-041 / E-DEV-081：

```text
  -> sub_1405DC6C0 = sum(generated node+112) 已确认。
  -> 旧 Excel 下料表中：
       E2 = 焊头(个)      = sub_1405DC6C0(obj)
       F2 = 单下料长(mm) = sub_1405DBC20(obj, buffer)
  -> 旧状态栏中：
       sum > 0 时 pane3 显示 焊接 / 绑扎 / 套筒连接。
  -> generated node+112 当前可收紧到
     “接头 / 焊头计数展示链字段”口径，
     但最终旧调试名仍不写死。
  -> 这轮仍不关闭真实运行截图、Excel 导出样例、
     AutoCAD L2、真实接头线算法、真实 Others 几何算法或 golden。

下一轮建议：
  -> TODO-070 / DetailWriter 真实字段差异 P2：lineStb StbGeo 字段条件化骨架。
  -> 基于 TODO-067 字段差距报告和 TODO-069 字段骨架结果，
     只收窄 lineStb StbGeo 字段集合；
     不自动安装 HASP，不自动启动旧图石，不做 Excel writer，不实现真实工程图算法。
```

当前执行优先级：

```text
1. 接头链路先记录、暂缓：
   -> `TODO-050` 保持 blocked
   -> `TODO-061` 保持 pending
   -> 不再自动把接头运行确认设为 next

2. 当前主线按非接头方向推进：
   -> `TODO-070 / DetailWriter 真实字段差异 P2：lineStb StbGeo 字段条件化骨架`

3. AutoCAD L2、统计 / 下料表剩余缺口、输出钢筋 STP、剖面圈筋 / 固中心
   继续按各自专项文档后置推进，但不抢占当前 next。
```
