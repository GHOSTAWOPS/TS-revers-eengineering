# M1-App-019 Run 001

## 结论

```text
decision = m1-app-019-pass
```

本轮完成 `旧命令契约绑定 P1`。

## 通过项

RED 验证：

```text
command_registry_tests.cpp
LegacyUiCommandDefinition 缺少 uiSurface / implementationState /
interactionStates，裁剪命令未区分 ContextMenu
result = expected compile failure
```

窄测试：

```text
command_registry_tests.exe
result = pass
```

默认 CTest：

```text
CTest 9 / 9 passed
Total Test time = 123.53 sec
```

测试项：

- `command_registry_tests`
- `legacy_selection_ref_tests`
- `rebar_domain_model_tests`
- `app_smoke`
- `viewer_smoke_tests`
- `domain_rebar_boundary`
- `step_import_probe_123`
- `occ_selection_index_123`
- `legacy_geometry_adapter_123`

Readiness gate：

```text
M1-Formal-Ready
78 / 78 pass
0 error
0 warning
```

## Legacy Command Contract P1

新增 / 补强：

```text
LegacyCommandUiSurface
LegacyCommandImplementationState
LegacyCommandInteractionState
LegacyUiCommandDefinition metadata
registerLegacyUiCommandPlaceholders
```

当前能力：

```text
LineGroup / ArcGroup / TrimByLine / TrimByFace 命令契约可查询
未实现命令可注册稳定 NotImplemented placeholder
ContextMenu 命令不会被 Ribbon 构建渲染
placeholder 不覆盖已有真实 handler
evidenceIds 只包含 E-* 规范证据 ID，文档 / 实现来源放入 sourceRefs
```

## xhigh 只读 review

```text
required = true
firstVerdict = block
critical = none
important = ContextMenu placement; state naming / interaction states; evidence ID normalization
fixApplied = yes
secondVerdict = block
secondImportant = non-E source refs such as 17, 36 and M1-App-003 were still stored in evidenceIds
secondFixApplied = yes; added sourceRefs and normalized evidenceIds to E-* only
finalVerdict = allow_commit
finalMinor = sourceRefs coverage can be broadened; E-STR evidence IDs should be indexed later
agentClosed = true
```

## 边界

本轮没有实现钢筋生成。

本轮没有实现裁剪业务算法。

本轮没有迁移父目录钢筋业务主链。

本轮没有进入 `TODO-020`。

命令契约 P1 只证明入口、metadata、选择类型和 placeholder 边界可追溯，不代表旧图石业务算法已复刻。

## 下一步

```text
TODO-020：IDA MCP 旧线筋 / 弧筋链补证据。
```
