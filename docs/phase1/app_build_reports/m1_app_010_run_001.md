# M1-App-010 Run 001

## 结论

```text
decision = m1-app-010-pass
```

本轮完成 `LegacyGeometryAdapter P3B`。

## 通过项

RED 验证：

```text
legacy_geometry_adapter_tests.cpp
edgeProjectPoint / edgeSplitAtPoint missing
result = expected compile failure
```

窄测试：

```text
legacy_geometry_adapter_tests.exe ..\..\123.stp
result = pass
```

默认 CTest：

```text
CTest 8 / 8 passed
Total Test time = 122.27 sec
```

测试项：

- `command_registry_tests`
- `legacy_selection_ref_tests`
- `app_smoke`
- `viewer_smoke_tests`
- `domain_rebar_boundary`
- `step_import_probe_123`
- `occ_selection_index_123`
- `legacy_geometry_adapter_123`

Readiness gate：

```text
M1-Formal-Ready
78 / 78 pass
0 error
0 warning
```

## Adapter P3B

新增：

```text
LegacyEdgePointProjection
edgeProjectPoint
edgeSplitAtPoint
```

当前能力：

```text
edge point projection
projected parameter
projection distance
split-safe inside flag
split by projected point
endpoint projection rejection for split
wrong type / missing part diagnostics
```

## 样本

```text
sample = C:/Users/ghost/Desktop/reverse_engineering/【03】图石软件/123.stp
size = 87746388 bytes
sha256 = 6a3c4f2044c2cc1f1123f0f58c61b2c869fbad3110a3585fdab49e69daf52a2a
```

## 边界

本轮没有实现钢筋生成。

本轮没有迁移父目录钢筋业务主链。

本轮没有进入 `TODO-011`。

P3B 是点投影和按点 split 语义摘要，不是完整 topology split。

OCCT 查询仍限制在：

```text
geometry/occ/legacy_adapter
```

`domain/rebar` 边界扫描通过。

## 下一步

```text
TODO-011：LegacyGeometryAdapter P3C 端点 trim 语义。
```

也可以先补：

```text
TODO-020：IDA MCP 旧线筋/弧筋链证据。
```
