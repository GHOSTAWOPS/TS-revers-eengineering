# M1-App-013 Run 001

## 结论

```text
decision = m1-app-013-pass
```

本轮完成 `LegacyGeometryAdapter P3E`。

## 通过项

RED 验证：

```text
legacy_geometry_adapter_tests.cpp
buildSplineFromPoints missing
result = expected compile failure
```

窄测试：

```text
legacy_geometry_adapter_tests.exe ..\..\123.stp
result = pass
```

默认 CTest：

```text
CTest 8 / 8 passed
Total Test time = 171.85 sec
```

测试项：

- `command_registry_tests`
- `legacy_selection_ref_tests`
- `app_smoke`
- `viewer_smoke_tests`
- `domain_rebar_boundary`
- `step_import_probe_123`
- `occ_selection_index_123`
- `legacy_geometry_adapter_123`

Readiness gate：

```text
M1-Formal-Ready
78 / 78 pass
0 error
0 warning
```

## Adapter P3E

新增：

```text
LegacySplineCurveBuild
buildSplineFromPoints
```

当前能力：

```text
point list to OCCT BSpline rebuild summary
input point count / requested sample count / effective sample count
legacySuggestedSampleCount = max(5, sourcePolylineLength * 50)
samplePoints / length / bbox summary
insufficient point and collapsed point diagnostics
failureReason carried in DTO
```

## 样本

```text
sample = C:/Users/ghost/Desktop/reverse_engineering/【03】图石软件/123.stp
size = 87746388 bytes
sha256 = 6a3c4f2044c2cc1f1123f0f58c61b2c869fbad3110a3585fdab49e69daf52a2a
```

## 边界

本轮没有实现钢筋生成。

本轮没有迁移父目录钢筋业务主链。

本轮没有进入 `TODO-014`。

P3E 是点列到 spline 的 summary，不是旧 ACIS `api_curve_spline`
返回并写回实体的完整 topology mutation。

OCCT 查询仍限制在：

```text
geometry/occ/legacy_adapter
```

`domain/rebar` 边界扫描通过。

## IDA 状态

IDA MCP 本轮可发现工具，但没有绑定数据库：

```text
No database bound for this context.
Database/session not found: ...\VisualTS.exe.i64
```

因此 P3E 的旧逻辑证据仍来自既有 `15/16` 文档，没有新增 IDA 证据。

## 下一步

```text
TODO-014：LegacyWireChain 边链 / wire chain 语义。
```

也可以先补：

```text
TODO-020：IDA MCP 旧线筋 / 弧筋链证据。
```
