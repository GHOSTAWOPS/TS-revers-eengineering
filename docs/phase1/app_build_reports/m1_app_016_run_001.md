# M1-App-016 Run 001

## 结论

```text
decision = m1-app-016-pass
```

本轮完成 `LegacyGeometryAdapter section / 剖切能力 spike`。

## 通过项

RED 验证：

```text
legacy_geometry_adapter_tests.cpp
LegacyPlane / facePlaneSectionPreview missing
result = expected compile failure
```

窄测试：

```text
legacy_geometry_adapter_tests.exe "..\..\123.stp"
result = pass
```

默认 CTest：

```text
CTest 8 / 8 passed
Total Test time = 157.10 sec
```

测试项：

- `command_registry_tests`
- `legacy_selection_ref_tests`
- `app_smoke`
- `viewer_smoke_tests`
- `domain_rebar_boundary`
- `step_import_probe_123`
- `occ_selection_index_123`
- `legacy_geometry_adapter_123`

Readiness gate：

```text
M1-Formal-Ready
78 / 78 pass
0 error
0 warning
```

xhigh 只读 review：

```text
Critical = none
Important = none
Minor = coplanar fallback 使用采样点判断；当前 spike 不阻塞
Verdict = allow_commit
agent closed = true
```

## Adapter M1-App-016

新增：

```text
LegacyPlane
LegacySectionEdge
LegacyFacePlaneSectionPreview
facePlaneSectionPreview
```

当前能力：

```text
face ref + legacy plane to section preview summary
sectionEdges / hitCount / totalLength / bbox / samplePoints
far plane empty summary
invalid plane rejection
wrong type / missing ref diagnostics
edge-face empty intersection set coverage
```

## 样本

```text
sample = C:/Users/ghost/Desktop/reverse_engineering/【03】图石软件/123.stp
size = 87746388 bytes
sha256 = 6a3c4f2044c2cc1f1123f0f58c61b2c869fbad3110a3585fdab49e69daf52a2a
```

## 边界

本轮没有实现钢筋生成。

本轮没有迁移父目录钢筋业务主链。

本轮没有进入 `TODO-017`。

Section preview 是 legacy summary / ability spike，不是 OCCT topology mutation，
也不是旧 ACIS section API 的完整等价实现。

OCCT 查询仍限制在：

```text
geometry/occ/legacy_adapter
```

`domain/rebar` 边界扫描通过。

## IDA 状态

IDA MCP 本轮可列出工具，但没有打开的 IDB 会话：

```text
idb_list
sessions = []
count = 0
```

因此本轮没有新增 IDA 证据。

## 下一步

```text
TODO-017：LegacyGeometryAdapter sweep / 扫掠能力边界。
```

如果 IDA 数据库恢复绑定，也可以先补：

```text
TODO-020：IDA MCP 旧线筋 / 弧筋链证据。
```
