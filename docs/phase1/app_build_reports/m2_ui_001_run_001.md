# M2-UI-001 Run 001

## 结论

```text
decision = m2-ui-001-pass
```

本轮完成 `TODO-027 / 旧 UI 功能入口 1:1 复刻 P1`。

## 范围

```text
CommandId P1 UI entry expansion
LegacyUiCommandMap P1 entry coverage
QAction traceability metadata
app smoke metadata verification
command registry tests
```

本轮不实现钢筋业务算法。

## 关键实现

```text
app/src/command/CommandId.h
app/src/command/LegacyUiCommandMap.cpp
app/src/app/MainWindow.cpp
app/src/app/MainWindow.h
app/src/app/main.cpp
app/tests/unit/command_registry_tests.cpp
```

## 验证

窄测：

```text
command_registry_tests = pass
tsrebar_app --smoke = pass
```

默认 CTest：

```text
13 / 13 passed
Total Test time = 125.40 sec
```

Readiness gate：

```text
M1-Formal-Ready
78 / 78 pass
0 error
0 warning
```

OCCT / AIS 泄漏扫描：

```text
rg -n "TopoDS_|AIS_|BRep|TopAbs_" app/src/domain/rebar app/src/drawing app/src/project
result = no matches
```

git diff check：

```text
pass
```

## xhigh

```text
round1 = needs_fix
round2 = needs_fix
round3 = allow_commit
```

处理结论：

```text
Critical / Important 均已修复。
最终 allow_commit。
```

## 边界

本轮不声明：

```text
旧弹窗字段已完整复刻。
旧状态栏提示已完整复刻。
钢筋创建 / 编辑 / 查询 / 统计 / 出图业务已实现。
golden 对照已完成。
```

## 下一步

```text
TODO-028 / CSE readiness gate 扩展
```
