# M1-App-023 Run 001

## 结论

```text
decision = m1-app-023-pass
```

本轮完成 `TODO-024 / DetailWriter P1 接入 domain rebar`。

## 通过项

RED：

```text
detail_writer_tests failed to compile because drawing/detail/DetailWriter.h was missing
```

GREEN：

```text
cmake --build app\build --target detail_writer_tests
app\build\detail_writer_tests.exe
result = pass
```

默认 CTest：

```text
13 / 13 passed
Total Test time = 126.35 sec
```

测试项：

- `command_registry_tests`
- `legacy_selection_ref_tests`
- `rebar_domain_model_tests`
- `rebar_group_creator_tests`
- `rebar_ais_presentation_tests`
- `project_runtime_tests`
- `detail_writer_tests`
- `app_smoke`
- `viewer_smoke_tests`
- `domain_rebar_boundary`
- `step_import_probe_123`
- `occ_selection_index_123`
- `legacy_geometry_adapter_123`

Readiness gate：

```text
M1-Formal-Ready
78 / 78 pass
0 error
0 warning
```

`domain/rebar + drawing + project` OCCT 边界扫描：

```text
rg -n "TopoDS_|AIS_|BRep|TopAbs_" app\src\domain\rebar app\src\drawing app\src\project
result = no matches
```

`git diff --check`：

```text
pass
```

## DetailWriter P1

新增：

```text
DetailWriter
detail_writer_tests
```

当前能力：

```text
SteelData / SteelBarGroup / SteelBar / SteelBarSegment
  -> Detail.xml
  -> Detail01.stl
```

覆盖字段：

```text
StbGroup.rsdID / groupID / stbNum / stbNumAct
StbGroup.barcount / segcount / SteelWay
Std.segCount
StbGeo.segID / stbSeqNum / shapeType / start / middle / end
StbRow.rsdID / diameter / length / segNum / stbNumSum / lenSum
MaterialTable / MatRow1 首批字段
```

事务守卫：

```text
缺 required field -> DW004
缺 bar/segment ref -> DW003
MaterialTable mass formula -> DW-WARN-MATERIAL_MASS_FORMULA_DEFERRED
install failure -> DW008
失败保持 dirty
失败保留旧 Detail 包
成功不声明 AutoCAD L2
```

## IDA MCP

本轮新增旧 VisualTS Detail 写出链确认。

```text
database = visualts_i64_todo024
file = VisualTS.exe.i64
```

确认：

```text
sub_14061F830 -> StbGroups / stbGroupCount / sub_14063E910
sub_14063E910 -> StbGroup%d / Std%d / sub_14063B010
sub_14063B010 -> StbGeo%d
sub_140605B20 -> sub_140602F90
sub_140602F90 -> StbTables / StbTable / StbRow%d / MaterialTable
```

边界：

```text
不声明完整公式。
不声明 Excel / AutoCAD L2 输出闭合。
不声明完整工程图生成。
```

## xhigh 只读 review

```text
round1 = block
round1Fixed = true
round2 = block
round2Fixed = true
round3 = allow_commit
Critical = none after final fix
Important = none after final fix
agentClosed = true
```

处理：

```text
补 replacePackage 旧包恢复
补稳定 DW008 replace failure
补 bar / segment 交叉引用拒绝
补 backup failure 安全退出
补 install failure 注入测试
重新运行 detail_writer_tests / CTest / readiness / leak scan / diff check
```

## 边界

本轮没有实现：

- AutoCAD L2 动态导入。
- 完整工程图生成。
- 完整下料统计公式。
- 钢筋创建 / 编辑 / 统计专项扩展。
- 旧图石 golden 对照。

## 下一步

```text
TODO-025 / 旧图石输出钢筋 STP 样本入库验证
```
