# M1-App-011 Run 001

## 结论

```text
decision = m1-app-011-pass
```

本轮完成 `LegacyGeometryAdapter P3C`。

## 通过项

RED 验证：

```text
legacy_geometry_adapter_tests.cpp
edgeTrimEndpoint missing
result = expected compile failure
```

窄测试：

```text
legacy_geometry_adapter_tests.exe ..\..\123.stp
result = pass
```

默认 CTest：

```text
CTest 8 / 8 passed
Total Test time = 132.19 sec
```

测试项：

- `command_registry_tests`
- `legacy_selection_ref_tests`
- `app_smoke`
- `viewer_smoke_tests`
- `domain_rebar_boundary`
- `step_import_probe_123`
- `occ_selection_index_123`
- `legacy_geometry_adapter_123`

Readiness gate：

```text
M1-Formal-Ready
78 / 78 pass
0 error
0 warning
```

## Adapter P3C

新增：

```text
LegacyEdgeEndpointTrim
edgeTrimEndpoint
```

当前能力：

```text
start endpoint inward trim summary
end endpoint inward trim summary
near-endpoint no-op trim rejection
short edge / over-trim rejection
invalid endFlag diagnostics
```

## 样本

```text
sample = C:/Users/ghost/Desktop/reverse_engineering/【03】图石软件/123.stp
size = 87746388 bytes
sha256 = 6a3c4f2044c2cc1f1123f0f58c61b2c869fbad3110a3585fdab49e69daf52a2a
```

## 边界

本轮没有实现钢筋生成。

本轮没有迁移父目录钢筋业务主链。

本轮没有进入 `TODO-012`。

P3C 是端点 trim 语义摘要，不是完整 topology trim。

OCCT 查询仍限制在：

```text
geometry/occ/legacy_adapter
```

`domain/rebar` 边界扫描通过。

## 下一步

```text
TODO-012：LegacyGeometryAdapter P3D 组内最小距离检查。
```

也可以先补：

```text
TODO-020：IDA MCP 旧线筋/弧筋链证据。
```
