# M1-App-002 Run 001

## 结论

```text
decision = m1-app-002-pass
```

本轮完成正式 app 的最小 OCCT AIS Viewer 接入。

## 通过项

```text
CTest 5 / 5 passed
```

测试项：

- `command_registry_tests`
- `app_smoke`
- `viewer_smoke_tests`
- `domain_rebar_boundary`
- `step_import_probe_123`

## 真实 Viewer Smoke

命令：

```powershell
build\tsrebar_app.exe --smoke-display-step "..\..\123.stp"
```

结果：

```text
exit code = 0
```

含义：

- `123.stp` 导入成功。
- AIS Viewer 初始化成功。
- STEP shape 显示成功。
- FitAll 成功。

## 边界

本轮没有实现钢筋生成。

本轮没有迁移父目录：

- `RebarCreationCommandService`
- `EdgeToRebarFactory`
- `FaceRebarGenerator`
- `PolylineRebarGenerator`

OCCT 仍被限制在：

```text
geometry/occ
presentation/occ
io/step
```

## 下一步

```text
M1-App-003：face / edge / vertex 选择、SelectionRef 稳定 ID、选择面板摘要。
```

