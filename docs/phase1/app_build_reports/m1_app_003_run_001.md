# M1-App-003 Run 001

## 结论

```text
decision = m1-app-003-pass
```

本轮完成正式 app 的 face / edge / vertex 选择底座。

## 通过项

```text
CTest 7 / 7 passed
```

测试项：

- `command_registry_tests`
- `legacy_selection_ref_tests`
- `app_smoke`
- `viewer_smoke_tests`
- `domain_rebar_boundary`
- `step_import_probe_123`
- `occ_selection_index_123`

## 真实 Viewer Smoke

命令：

```powershell
build\tsrebar_app.exe --smoke-display-step "..\..\123.stp"
```

结果：

```text
exit code = 0
```

## 选择索引

`123.stp` 选择索引结果：

```text
faces=3016
unique_edges=4524
unique_vertices=3016
```

稳定引用格式：

```text
ref=selection-v1;entry=...;synthetic=0;syntheticOrdinal=0;type=4|6|7;index=N
```

## 边界

本轮没有实现钢筋生成。

本轮没有迁移父目录钢筋业务主链。

OCCT 仍被限制在：

```text
geometry/occ
presentation/occ
```

`domain/rebar` 边界扫描通过。

## 手动 Viewer Selection Smoke

命令：

```powershell
build\viewer_selection_tests.exe "..\..\123.stp"
```

结果：

```text
exit code = 0
```

该 smoke 需要真实 OpenGL 窗口，会弹出临时测试窗口。为避免日常测试反复弹窗，它不在默认 CTest 中执行。

## 下一步

```text
M1-App-004：LegacyGeometryAdapter P0。
```
