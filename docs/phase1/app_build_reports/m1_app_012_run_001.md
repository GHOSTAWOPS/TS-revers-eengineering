# M1-App-012 Run 001

## 结论

```text
decision = m1-app-012-pass
```

本轮完成 `LegacyGeometryAdapter P3D`。

## 通过项

RED 验证：

```text
legacy_geometry_adapter_tests.cpp
pointToEdgeGroupDistance missing
result = expected compile failure
```

窄测试：

```text
legacy_geometry_adapter_tests.exe ..\..\123.stp
result = pass
```

默认 CTest：

```text
CTest 8 / 8 passed
Total Test time = 131.42 sec
```

测试项：

- `command_registry_tests`
- `legacy_selection_ref_tests`
- `app_smoke`
- `viewer_smoke_tests`
- `domain_rebar_boundary`
- `step_import_probe_123`
- `occ_selection_index_123`
- `legacy_geometry_adapter_123`

Readiness gate：

```text
M1-Formal-Ready
78 / 78 pass
0 error
0 warning
```

## Adapter P3D

新增：

```text
LegacyEdgeGroupDistanceCandidate
LegacyEdgeGroupDistance
pointToEdgeGroupDistance
```

当前能力：

```text
single edge candidate distance
multi edge candidate nearest selection
threshold hit flag
empty group rejection
wrong type / missing ref diagnostics
```

## 样本

```text
sample = C:/Users/ghost/Desktop/reverse_engineering/【03】图石软件/123.stp
size = 87746388 bytes
sha256 = 6a3c4f2044c2cc1f1123f0f58c61b2c869fbad3110a3585fdab49e69daf52a2a
```

## 边界

本轮没有实现钢筋生成。

本轮没有迁移父目录钢筋业务主链。

本轮没有进入 `TODO-013`。

P3D 是点到 edge ref 列表的最小距离摘要，不是旧 group 链表完整复刻。

OCCT 查询仍限制在：

```text
geometry/occ/legacy_adapter
```

`domain/rebar` 边界扫描通过。

## 下一步

```text
TODO-013：LegacyGeometryAdapter P3E 点列 spline 重建。
```

也可以先补：

```text
TODO-020：IDA MCP 旧线筋/弧筋链证据。
```
