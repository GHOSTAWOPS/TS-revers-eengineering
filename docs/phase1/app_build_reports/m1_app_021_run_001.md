# M1-App-021 Run 001

## 结论

```text
decision = m1-app-021-pass
```

本轮完成 `TODO-022 / AIS 钢筋显示映射`。

## 通过项

RED：

```text
cmake --build app\build --target rebar_ais_presentation_tests
result = expected compile failure
failure = presentation/occ/RebarAisPresentationAdapter.h missing
```

GREEN：

```text
rebar_ais_presentation_tests.exe
result = pass
```

默认 CTest：

```text
11 / 11 passed
Total Test time = 124.85 sec
```

测试项：

- `command_registry_tests`
- `legacy_selection_ref_tests`
- `rebar_domain_model_tests`
- `rebar_group_creator_tests`
- `rebar_ais_presentation_tests`
- `app_smoke`
- `viewer_smoke_tests`
- `domain_rebar_boundary`
- `step_import_probe_123`
- `occ_selection_index_123`
- `legacy_geometry_adapter_123`

Readiness gate：

```text
M1-Formal-Ready
78 / 78 pass
0 error
0 warning
```

`domain/rebar` OCCT 边界扫描：

```text
rg -n "TopoDS_|AIS_|BRep|TopAbs_" app\src\domain\rebar
result = no matches
```

`git diff --check`：

```text
pass
```

## AIS 显示映射 P0

新增：

```text
RebarAisPresentationAdapter
RebarAisPresentationResult
RebarAisDisplayItem
tsrebar_occ_rebar_presentation
rebar_ais_presentation_tests
```

当前能力：

```text
Line segment -> TopoDS_Edge -> AIS_Shape
Arc segment -> circular TopoDS_Edge -> AIS_Shape
missing group / empty group stable diagnostic
presentation/occ 可使用 AIS / OCCT
domain/rebar 无 AIS / OCCT 泄漏
```

## xhigh 只读 review

```text
Critical = none
Important = none
Minor = RebarAisPresentationAdapter.cpp 建议显式补 <utility>
Route Drift = none
Verdict = allow_commit
agentClosed = true
```

处理：

```text
已补 <utility>
```

## 边界

本轮没有实现旧 HOOPS 显示样式 1:1。

本轮没有把 UI 命令 placeholder 接成真实点击创建流程。

本轮没有实现 Detail writer。

本轮没有实现新工程格式 Save/Open。

本轮没有迁入父目录 rebar 业务代码。

本轮没有让 `domain/rebar` 依赖 OCCT / AIS。

## 下一步

```text
TODO-023 / M1-App-022：新设计文件格式 runtime P1
```

也可以先补旧图石运行确认：

```text
线筋 / 弧筋 UI 流程
输出钢筋 STP 复杂样本
```
