# M1-App-024 Run 001

## 结论

```text
decision = m1-app-024-pass
```

本轮完成 `TODO-025 / 旧图石输出钢筋 STP 样本入库验证`。

## 样本

```text
sampleId = tushi_rebar_123_stp
sourceSfl = 消力池下游侧带齿槽底板结构图石钢筋模型.sfl
sourceStep = C:\Users\ghost\Desktop\reverse_engineering\【03】图石软件\123.stp
```

源 SFL：

```text
size = 563,051 bytes
sha256 = a5645635ff77346e1dacccbac57b7f594d1278d74bf70f4be762dd93ced90515
```

旧图石导出钢筋 STP：

```text
size = 87,746,388 bytes
sha256 = 6a3c4f2044c2cc1f1123f0f58c61b2c869fbad3110a3585fdab49e69daf52a2a
STEP exporter = Spatial InterOp 3D
schema = STEP AP214 / AUTOMOTIVE_DESIGN
```

## 当前导入验证

命令：

```powershell
app\build\step_import_probe.exe "..\123.stp"
```

结果：

```text
ok = true
read_ok = true
transfer_ok = true
roots = 1
free_shapes = 1
solids = 754
faces = 3016
edges = 9048
vertices = 18096
length_unit = METRE
```

## 既有 selection gate

```text
report = docs/phase1/step_selection/tushi_rebar_123_stp/step_selection_diff.json
decision = pass
probeRunCount = 5
copy-path run = included
```

稳定引用：

```text
faces = 3016, minMatchRate = 1.0, duplicate = 0
edges = 4524, minMatchRate = 1.0, duplicate = 0
vertices = 3016, minMatchRate = 1.0, duplicate = 0
```

## 默认 CTest

```text
13 / 13 passed
Total Test time = 125.71 sec
```

## Readiness gate

```text
M1-Formal-Ready
78 / 78 pass
0 error
0 warning
```

## OCCT/AIS 泄漏扫描

```text
rg -n "TopoDS_|AIS_|BRep|TopAbs_" app\src\domain\rebar app\src\drawing app\src\project
result = no matches
```

## git diff check

```text
pass
```

## xhigh

本轮未改代码、测试或构建脚本，只做只读样本 witness 和文档追溯更新。

因此未启动 xhigh 代码 review。

## 边界

本轮不声明：

- 新系统已能生成同样钢筋。
- `123.stp` 包含旧命令参数或钢筋字段。
- `sexportbar / ysteelout` 的实际 `.stp` 导出链已闭合。
- AutoCAD L2 或 Detail 输出已进一步闭合。

## 下一步

```text
TODO-027 / 旧 UI 功能入口 1:1 复刻 P1
```

说明：`TODO-026` 是 P2 golden 采集方案，用户此前说 golden 先不要；本轮把 `TODO-026` 保持 pending，不自动采集。
