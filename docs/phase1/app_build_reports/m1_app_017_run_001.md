# M1-App-017 Run 001

## 结论

```text
decision = m1-app-017-pass
```

本轮完成 `LegacyGeometryAdapter sweep / 扫掠能力边界`。

## 通过项

RED 验证：

```text
legacy_geometry_adapter_tests.cpp
edgeCircularSweepPreview missing
result = expected compile failure
```

窄测试：

```text
legacy_geometry_adapter_tests.exe "..\..\123.stp"
result = pass
```

默认 CTest：

```text
CTest 8 / 8 passed
Total Test time = 130.88 sec
```

测试项：

- `command_registry_tests`
- `legacy_selection_ref_tests`
- `app_smoke`
- `viewer_smoke_tests`
- `domain_rebar_boundary`
- `step_import_probe_123`
- `occ_selection_index_123`
- `legacy_geometry_adapter_123`

Readiness gate：

```text
M1-Formal-Ready
78 / 78 pass
0 error
0 warning
```

xhigh 只读 review：

```text
Critical = none
Important = none
Minor = effectiveSampleCount == 1 branch unreachable; shapeVertexCount not separately asserted
Verdict = allow_commit
agent closed = true
```

## Adapter M1-App-017

新增：

```text
LegacyCircularSweepPreview
edgeCircularSweepPreview
```

当前能力：

```text
edge ref + circular radius to sweep / pipe preview summary
source edge stableId / curve kind / pathLength
samplePoints / bbox
swept shape face-edge-vertex count
zero radius / non-finite radius / invalid sample rejection
wrong type / missing ref diagnostics
```

## 样本

```text
sample = C:/Users/ghost/Desktop/reverse_engineering/【03】图石软件/123.stp
size = 87746388 bytes
sha256 = 6a3c4f2044c2cc1f1123f0f58c61b2c869fbad3110a3585fdab49e69daf52a2a
```

已知导入统计：

```text
solids = 754
faces = 3016
uniqueEdges = 4524
uniqueVertices = 3016
rawEdges = 9048
rawVertices = 18096
lengthUnit = METRE
```

## 边界

本轮没有实现钢筋生成。

本轮没有迁移父目录钢筋业务主链。

本轮没有进入 `TODO-018`。

Sweep preview 是 legacy summary / ability spike，不是 OCCT topology mutation
对业务层开放，也不是旧 ACIS sweep / pipe API 的完整等价实现。

OCCT 查询仍限制在：

```text
geometry/occ/legacy_adapter
```

`domain/rebar` 边界扫描通过。

## IDA 状态

本轮没有新增 IDA 证据。

旧图石的钢筋实体生成规则、profile 半径来源、端部处理和导出策略仍需后续
IDA 或旧图石运行确认。

## 下一步

```text
TODO-018：钢筋领域模型冻结 P1。
```

如果 IDA 数据库恢复绑定，也可以先补：

```text
TODO-020：IDA MCP 旧线筋 / 弧筋链证据。
```
