# M1-App-007 Run 001

## 结论

```text
decision = m1-app-007-pass
```

本轮完成 `LegacyGeometryAdapter P2B`。

## 通过项

窄测试：

```text
legacy_geometry_adapter_tests.exe ..\..\123.stp
result = pass
```

默认 CTest：

```text
CTest 8 / 8 passed
Total Test time = 147.45 sec
```

测试项：

- `command_registry_tests`
- `legacy_selection_ref_tests`
- `app_smoke`
- `viewer_smoke_tests`
- `domain_rebar_boundary`
- `step_import_probe_123`
- `occ_selection_index_123`
- `legacy_geometry_adapter_123`

Readiness gate：

```text
M1-Formal-Ready
78 / 78 pass
0 error
0 warning
```

## Adapter P2B

新增：

```text
LegacyIntersectionPoint
LegacyIntersectionSet
edgeFaceIntersections
LegacyBoundaryLoop.edgeStableIds
```

当前能力：

```text
face boundary loop -> edge stable ids
boundary edge stable id parse
edge-face contact / overlap query
representative intersection points
edge parameter on intersection point
```

## 样本

```text
sample = C:/Users/ghost/Desktop/reverse_engineering/【03】图石软件/123.stp
size = 87746388 bytes
sha256 = 6a3c4f2044c2cc1f1123f0f58c61b2c869fbad3110a3585fdab49e69daf52a2a
```

## 边界

本轮没有实现钢筋生成。

本轮没有迁移父目录钢筋业务主链。

P2B 是接触 / 相交代表点能力，不是完整布尔 section 或裁剪。

OCCT 查询仍限制在：

```text
geometry/occ/legacy_adapter
```

`domain/rebar` 边界扫描通过。

## 下一步

```text
M1-App-008：LegacyGeometryAdapter P2C 已完成。
见 m1_app_008_run_001.md。
```
