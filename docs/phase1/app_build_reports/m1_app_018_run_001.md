# M1-App-018 Run 001

## 结论

```text
decision = m1-app-018-pass
```

本轮完成 `钢筋领域模型冻结 P1`。

## 通过项

RED 验证：

```text
rebar_domain_model_tests.cpp
SteelData / SteelBarGroup / SteelBar / SteelBarSegment P1 fields missing
result = expected compile failure
```

窄测试：

```text
rebar_domain_model_tests.exe
result = pass
```

默认 CTest：

```text
CTest 9 / 9 passed
Total Test time = 163.30 sec
```

测试项：

- `command_registry_tests`
- `legacy_selection_ref_tests`
- `rebar_domain_model_tests`
- `app_smoke`
- `viewer_smoke_tests`
- `domain_rebar_boundary`
- `step_import_probe_123`
- `occ_selection_index_123`
- `legacy_geometry_adapter_123`

Readiness gate：

```text
M1-Formal-Ready
78 / 78 pass
0 error
0 warning
```

xhigh 只读 review：

```text
Critical = none
Important = none
Minor = legacy id alias should not become future business truth; start/end radius not explicitly asserted
Verdict = allow_commit
agent closed = true
```

## Domain Model M1-App-018

新增 / 补强：

```text
RebarDomainTypes
SteelData
SteelBarGroup
SteelBar
SteelBarSegment
rebar_domain_model_tests
```

当前能力：

```text
Detail key fields can be represented in domain/rebar
group -> bar -> segment ID references are explicit
legacy raw / geometryRef / binding / evidence can be carried
low-confidence legacy fields can be marked unresolved
domain/rebar remains free of OCCT / AIS dependencies
```

## 边界

本轮没有实现钢筋生成。

本轮没有迁移父目录钢筋业务主链。

本轮没有进入 `TODO-019`。

P1 领域模型冻结只证明可编码字段边界，不代表旧图石所有字段语义完全闭合。

## 下一步

```text
TODO-019：旧命令契约绑定 P1。
```

如果 IDA 数据库恢复绑定，也可以先补：

```text
TODO-020：IDA MCP 旧线筋 / 弧筋链证据。
```
