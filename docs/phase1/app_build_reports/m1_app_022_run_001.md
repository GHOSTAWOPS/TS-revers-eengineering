# M1-App-022 Run 001

## 结论

```text
decision = m1-app-022-pass
```

本轮完成 `TODO-023 / 新设计文件格式 runtime P1`。

## 通过项

RED：

```text
project_runtime_tests failed on missing segment groupId
project_runtime_tests failed on broken rebar references being saved
project_runtime_tests failed on hardcoded sourceStepId
project_runtime_tests with relative TSREBAR_RUNTIME_FIXTURE_OUT hung at final packageHash
project_runtime_tests caught save-open-save unresolved_fields loss
project_runtime_tests covered install failure cleanup / dirty preservation
project_runtime_tests verified save result hash matches installed package hash
```

GREEN：

```text
cmake --build app\build --target project_runtime_tests
TSREBAR_RUNTIME_FIXTURE_OUT=app\build\runtime_validator_fixture.tsrebar app\build\project_runtime_tests.exe
result = pass
```

默认 CTest：

```text
12 / 12 passed
Total Test time = 125.49 sec
```

测试项：

- `command_registry_tests`
- `legacy_selection_ref_tests`
- `rebar_domain_model_tests`
- `rebar_group_creator_tests`
- `rebar_ais_presentation_tests`
- `project_runtime_tests`
- `app_smoke`
- `viewer_smoke_tests`
- `domain_rebar_boundary`
- `step_import_probe_123`
- `occ_selection_index_123`
- `legacy_geometry_adapter_123`

外部 validator：

```text
report = docs/phase1/validator_reports/runtime_m1_app_022/build_validation_001.json
decision = warning-only
errorCount = 0
warningCount = 2
```

Readiness gate：

```text
M1-Formal-Ready
78 / 78 pass
0 error
0 warning
```

`domain/rebar` + `project/runtime` OCCT 边界扫描：

```text
rg -n "TopoDS_|AIS_|BRep|TopAbs_" app\src\domain\rebar app\src\project
result = no matches
```

`git diff --check`：

```text
pass
```

## Runtime P1

新增：

```text
TsRebarProjectRuntime
project_runtime_tests
```

当前能力：

```text
保存 STEP source
topology refs / selection-v1 refs
rebar steel_data / groups / bars / segments
legacyObject.raw
geometryRef
binding
evidence / unresolved_fields
Detail mapping P1 placeholder
```

事务守卫：

```text
broken binding -> SaveFailed，旧包 hash 不变，dirty 保持
broken rebar ref -> SaveFailed，不安装正式包
resolved binding 缺 geometryPath -> LGV004
unresolvedGeometry 缺 repair/readOnly openMode -> LGV007
failed save 清理 candidate 包
relative packagePath 保存不挂住
save-open-save 保留 unresolved_fields warning
安装失败保持 dirty 并清理 candidate / replace_tmp
成功保存先算 candidate hash，避免替换后 final hash 失败
```

## xhigh 只读 review

```text
round1 = block_commit
round2 = allow_commit
round3 = block_commit
round3Fixed = true
round4 = block_commit
round4Fixed = true
round5 = allow_commit
Critical = none after final fix
Important = none after fix
agentClosed = true
```

处理：

```text
统一 TsRebarProjectRuntime.* 文件名
补齐 LGV004 / LGV007 runtime validation
SaveFailed 清理 candidate 包
规范化 runtime packagePath，修复相对路径导出卡住
恢复 readSnapshot 的 unresolved_fields round-trip
补 replaceDir / saveSnapshot 异常回滚
补 candidate hash 预计算，catch 不再二次 packageHash
重新运行 project_runtime_tests / external validator / CTest / readiness / leak scan / diff check
```

## IDA 状态

本轮没有新增 IDA 查询。

原因：本轮是保存 / 读取已有 domain 对象、binding、geometryRef 和 evidence，未新增旧钢筋业务规则判断。

## 边界

本轮没有实现：

- 完整 UI Save/Open。
- Detail writer P1。
- `.sfl` 兼容读写。
- 旧钢筋编辑 / 统计 / 出图算法。
- 旧图石 golden 对照。

## 下一步

```text
TODO-024 / M1-App-023：DetailWriter P1 接入 domain rebar
```
