# M1-App-014 Run 001

## 结论

```text
decision = m1-app-014-pass
```

本轮完成 `LegacyWireChain`。

## 通过项

RED 验证：

```text
legacy_geometry_adapter_tests.cpp
buildWireChain missing
result = expected compile failure
```

窄测试：

```text
legacy_geometry_adapter_tests.exe ..\..\123.stp
result = pass
```

默认 CTest：

```text
CTest 8 / 8 passed
Total Test time = 126.51 sec
```

测试项：

- `command_registry_tests`
- `legacy_selection_ref_tests`
- `app_smoke`
- `viewer_smoke_tests`
- `domain_rebar_boundary`
- `step_import_probe_123`
- `occ_selection_index_123`
- `legacy_geometry_adapter_123`

Readiness gate：

```text
M1-Formal-Ready
78 / 78 pass
0 error
0 warning
```

## Adapter M1-App-014

新增：

```text
LegacyWireChainEdge
LegacyWireChain
buildWireChain
```

当前能力：

```text
edge refs to ordered wire chain summary
face boundary loop closed chain
single edge open chain
disconnected edge rejection
wrong type / missing ref diagnostics
total length / endpoints / bbox / closed / connected
```

## 样本

```text
sample = C:/Users/ghost/Desktop/reverse_engineering/【03】图石软件/123.stp
size = 87746388 bytes
sha256 = 6a3c4f2044c2cc1f1123f0f58c61b2c869fbad3110a3585fdab49e69daf52a2a
```

## 边界

本轮没有实现钢筋生成。

本轮没有迁移父目录钢筋业务主链。

本轮没有进入 `TODO-015`。

Wire chain 是 legacy summary，不是 OCCT `TopoDS_Wire` topology mutation，
也不是旧 `ENTITY_LIST` 对象完整复刻。

OCCT 查询仍限制在：

```text
geometry/occ/legacy_adapter
```

`domain/rebar` 边界扫描通过。

## IDA 状态

IDA MCP 本轮仍没有绑定数据库：

```text
No database bound for this context.
```

因此本轮没有新增 IDA 证据。

## 下一步

```text
TODO-015：LegacyGeometryAdapter offset 曲线能力 spike。
```

也可以先补：

```text
TODO-020：IDA MCP 旧线筋 / 弧筋链证据。
```
