# M1-App-020 Run 001

## 结论

```text
decision = m1-app-020-pass
```

本轮完成 `TODO-021 / 旧线筋 / 弧筋创建算法 P0 版本`。

## 通过项

RED 1：

```text
cmake --build app\build --target rebar_group_creator_tests
result = expected compile failure
failure = domain/rebar/RebarGroupCreator.h missing
```

GREEN 1：

```text
rebar_group_creator_tests.exe
result = pass
```

xhigh block 后 RED 2：

```text
cmake --build app\build --target rebar_group_creator_tests
result = expected compile failure
failure = LegacySegmentCurveNormalizeRequest / normalizeSegmentCurve missing
```

GREEN 2：

```text
rebar_group_creator_tests.exe
result = pass
```

默认 CTest：

```text
10 / 10 passed
Total Test time = 126.55 sec
```

测试项：

- `command_registry_tests`
- `legacy_selection_ref_tests`
- `rebar_domain_model_tests`
- `rebar_group_creator_tests`
- `app_smoke`
- `viewer_smoke_tests`
- `domain_rebar_boundary`
- `step_import_probe_123`
- `occ_selection_index_123`
- `legacy_geometry_adapter_123`

Readiness gate：

```text
M1-Formal-Ready
78 / 78 pass
0 error
0 warning
```

`domain/rebar` OCCT 边界扫描：

```text
rg "TopoDS_|AIS_|BRep|TopAbs_" app\src\domain\rebar
result = no matches
```

`git diff --check`：

```text
pass
```

## 业务对象创建 P0

新增：

```text
RebarGroupCreator
LegacyRebarGeometryReader
LegacySegmentCurveNormalizeRequest
LegacyRebarCurveSnapshot
```

当前能力：

```text
LineGroup / ArcGroup P0 creator
distanceA >= 0.002 gate
segment length >= 0.01 gate
SegmentCurveNormalizer P0 request / call
SteelData -> SteelBarGroup -> SteelBar -> SteelBarSegment output
E-IDA-022 evidence propagation
unresolved legacy fields propagation
arc distanceB=0.8 and flag=1 evidence lock
```

## xhigh 只读 review

第一轮：

```text
Critical = none
Important =
  - arc path did not force old flag=1.
  - SegmentCurveNormalizer P0 was missing as explicit interface / call.
Verdict = block
agentClosed = true
```

处理：

```text
arc group now forces effectiveFlag=1
createdFromParameters records legacyFlagForcedByCommand
LegacySegmentCurveNormalizeRequest added
LegacyRebarGeometryReader::normalizeSegmentCurve added
creator uses normalized curve to build segment
tests cover normalizer call and normalized segment output
```

第二轮：

```text
Critical = none
Important = none
Minor = ArcGroup distanceB 文档表述偏弱，已修正
finalVerdict = allow_commit
agentClosed = true
```

## 边界

本轮没有实现 AIS 钢筋显示。

本轮没有实现 Detail writer。

本轮没有把 UI 命令 placeholder 接成真实点击创建流程。

本轮没有迁移父目录 rebar 业务代码。

本轮没有让 `domain/rebar` 依赖 OCCT / AIS。

本轮不声明完整旧图石线筋 / 弧筋 UI、失败提示、状态栏、golden 输出已闭合。

## 下一步

```text
TODO-022 / M1-App-021：AIS 钢筋显示映射
```

或者先补旧图石运行确认：

```text
sgroupbararc UI 对应
线筋 / 弧筋参数窗口
状态栏和失败提示
```
