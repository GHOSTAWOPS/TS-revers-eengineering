# 功能操作矩阵

## 说明

本文件记录用户能看到和点击的功能入口。

注意：

- 本文件只解决“旧图石有哪些按钮和操作入口”。
- Qt6、OCCT、CAD 商业版边界、商业库替代方案见 `06_技术路线与替代方案.md`。
- 钢筋业务规则不在本表里展开，后续按功能入口拆成专项复刻说明。

字段说明：

- 页签：旧图石顶部页签。
- 分组：旧 Ribbon 分组。
- 功能：按钮或命令文字。
- 证据：二进制字符串、旧文档字符串、Dialog、IDA 命令或样本证据。
- 一期：`必须`、`支撑`、`待定`。
- 状态：`已确认`、`部分确认`、`待运行确认`。

## 开始页

| 分组 | 功能 | 一期 | 状态 | 证据/说明 |
|---|---|---:|---|---|
| 文件 | 新建 | 必须 | 待补 | 运行界面截图已出现，需补二进制和命令证据。 |
| 文件 | 打开 | 必须 | 待补 | 运行界面截图已出现。 |
| 文件 | 保存 | 必须 | 待补 | 运行界面截图已出现。 |
| 文件 | 关闭 | 必须 | 待补 | 运行界面截图已出现。 |
| 文件 | 合并文件 | 支撑 | 待补 | 运行界面截图已出现。 |
| 文件 | 转换模型 | 支撑 | 待补 | 运行界面截图已出现。 |
| 文件 | 输出结构 | 支撑 | 已运行确认 | 用户截图确认位于 `开始 / 文件` 分组。 |
| 文件 | 输出钢筋 | 必须 | 部分运行确认 | 用户截图确认位于 `开始 / 文件` 分组；用户运行确认可导出钢筋 `.stp`；导出类命令含 `sexportbar` / `ysteelout`，底层对应关系仍需补证。 |
| 编辑 | 放弃 / 重做 / 删除 | 必须 | 待补 | 运行界面截图已出现。 |
| 插入参考 | 参考面 | 必须 | 部分确认 | 旧字符串含参考面；参考对象影响钢筋选择和工程图。 |
| 插入参考 | 参考线 | 必须 | 部分确认 | 旧字符串和 Dialog #317 参考线定义存在。 |
| 插入参考 | 参考体 | 支撑 | 部分确认 | 旧字符串含 `创建参考体`。 |
| 鼠标状态 | 拾取 | 必须 | 待补 | 钢筋操作选择流程依赖。 |
| 鼠标状态 | 实时转动 | 必须 | 待补 | 视图操作依赖。 |

## 显示页

| 分组 | 功能 | 一期 | 状态 | 二进制证据 |
|---|---|---:|---|---|
| 取景 | 全显 | 必须 | 已确认 | `VisualTS.exe` UTF16 offset `16740346`；`VisualTS20240723.exe` UTF16 offset `16709082`。 |
| 取景 | 实时缩放 | 必须 | 已确认 | `16740392` / `16709128`。 |
| 取景 | 实时平移 | 必须 | 已确认 | `16740444` / `16709180`。 |
| 取景 | 窗口缩放 | 必须 | 已确认 | `16740498` / `16709234`。 |
| 方位 | 轴测 | 必须 | 已确认 | `VisualTS.exe` GBK offset `7768768`，UTF16 offset `10306166`。 |
| 方位 | 轴测2 | 支撑 | 已确认 | UTF16 offset `16740570` / `16709306`。 |
| 方位 | 垂直于 | 必须 | 已确认 | UTF16 offset `10312394` / `10281162`。 |
| 方位 | 旋转 | 必须 | 已确认 | GBK offset `7745804` / `7714668`。 |
| 渲染 | 线框 | 必须 | 已确认 | UTF16 offset `10306224` / `10274992`。 |
| 渲染 | 隐线不可见 | 支撑 | 已确认 | UTF16 offset `10310870` / `10279638`。 |
| 渲染 | 隐线可见 | 支撑 | 已确认 | UTF16 offset `10310894` / `10279662`。 |
| 渲染 | 渲染 | 支撑 | 已确认 | GBK offset `7951100` / `7919948`。 |
| 渲染 | 上色 | 支撑 | 已确认 | UTF16 offset `10312512` / `10281280`。 |
| 钢筋显示 | 激活 | 必须 | 已确认 | GBK offset `7829252` / `7798100`；右键命令也有激活/钝化。 |
| 钢筋显示 | 钝化 | 必须 | 已确认 | GBK offset `7829244` / `7798092`。 |
| 切平面 | X 向剖视 | 支撑 | 已确认 | UTF16 offset `16738310` / `16707046`。 |
| 切平面 | Y 向剖视 | 支撑 | 已确认 | UTF16 offset `16738336` / `16707072`。 |
| 切平面 | Z 向剖视 | 支撑 | 已确认 | UTF16 offset `16738362` / `16707098`。 |
| 切平面 | 设置旋转 | 支撑 | 已确认 | GBK offset `7830336` / `7799184`。 |
| 窗口 | 窗口重叠 | 支撑 | 已确认 | UTF16 offset `16741774` / `16710510`。 |
| 窗口 | 窗口水平平铺 | 支撑 | 已确认 | UTF16 offset `16741798` / `16710534`。 |
| 窗口 | 窗口垂直平铺 | 支撑 | 已确认 | UTF16 offset `16741830` / `16710566`。 |

## 钢筋页

### 文件属性 / 钢筋设置

| 分组 | 功能 | 一期 | 状态 | 证据 |
|---|---|---:|---|---|
| 文件属性 | 钢筋式样 | 必须 | 已确认 | 字符串行证据；`VisualTS.exe` UTF16 offset `10312826`；Dialog #292/#293。 |
| 文件属性 | 文件设置 | 必须 | 已确认 | `VisualTS.exe` UTF16 offset `10318568`；Dialog #426。 |
| 文件属性 | 标高基准面 | 必须 | 已确认 | `VisualTS.exe` UTF16 offset `16744716`；Dialog #283。 |
| 钢筋设置 | 系统设置 | 必须 | 已确认 | `VisualTS.exe` GBK offset `7897336`，UTF16 offset `10304698`；Dialog #3011。 |

### 主要钢筋

| 功能 | 一期 | 状态 | 证据/说明 |
|---|---:|---|---|
| 面配筋 | 必须 | 已确认 | `VisualTS.exe` GBK offset `7704369`，UTF16 offset `10304830`；Dialog #265/#266/#268；SFL 小样本已见 `面配筋_x`。 |
| 线配筋 | 必须 | 已确认 | GBK offset `7786164`，UTF16 offset `10312864`；旧命令 `sgroupbarline`。 |
| 扇形筋 | 必须 | 已确认 | GBK offset `7786068`，UTF16 offset `10311204`。 |
| 剖面圈筋 | 必须 | 待运行确认 | 截图中有按钮；当前字符串需继续用别名搜索。 |
| 面周边 | 必须 | 已确认 | UTF16 offset `16740006`；旧字符串行含 `面周边布置`。 |
| 同心圆 | 必须 | 已确认 | GBK offset `7911216`，UTF16 offset `10312876`。 |
| 角度配筋 | 必须 | 已确认 | GBK offset `7785960`，UTF16 offset `16745804`。 |
| 自配筋 | 必须 | 已确认 | GBK offset `7785916`，UTF16 offset `16745092`。 |
| 钢筋配筋 | 必须 | 已确认 | GBK offset `7782496`，UTF16 offset `10312998`。 |

### 构造钢筋

| 功能 | 一期 | 状态 | 证据/说明 |
|---|---:|---|---|
| 角筋 | 必须 | 已确认 | GBK offset `7781964`，UTF16 offset `10311194`。 |
| 孔口 | 必须 | 已确认 | GBK offset `7781928`，UTF16 offset `10311216`；旧字符串含 `孔口加强筋`。 |
| 固中心 | 必须 | 待运行确认 | 截图中有按钮；当前字符串未完全闭合。 |
| 箍筋 | 必须 | 已确认 | GBK offset `7714440`，UTF16 offset `10311170`；大量旧字符串和 Dialog 证据。 |
| 插筋 | 必须 | 已确认 | GBK offset `7785972`，UTF16 offset `10312978`。 |
| 螺旋筋 | 必须 | 已确认 | GBK offset `7786092`，UTF16 offset `10312950`。 |

### 钢筋编辑

| 功能 | 一期 | 状态 | 证据/说明 |
|---|---:|---|---|
| 组合并 | 必须 | 已确认 | GBK offset `7829268`，UTF16 offset `10311292`；Dialog #500；右键命令 `0x891F`。 |
| 组合开 | 必须 | 待运行确认 | 截图中有按钮；需确认旧字符串别名是否为组合拆分。 |
| 段连接 | 必须 | 已确认 | GBK offset `7829284`，UTF16 offset `10311324`；Dialog #499；右键命令 `0x8924`。 |
| 段断开 | 必须 | 已确认 | UTF16 offset `16744690`；旧字符串行有 `段断开`。 |
| 钢筋拷贝 | 必须 | 已确认 | UTF16 offset `10311396`；Dialog #384。 |
| 钢筋移动 | 必须 | 已确认 | GBK offset `7735816`，UTF16 offset `10311382`；旧命令 `barmove`。 |
| 增加钢筋根数 | 必须 | 已确认 | UTF16 offset `10314668`；右键命令增减根数 `0x8CEB`。 |
| 增加钢筋段 | 必须 | 已确认 | UTF16 offset `10311366`；Dialog #392。 |

## 查询页

| 分组 | 功能 | 一期 | 状态 | 证据/说明 |
|---|---|---:|---|---|
| 查询钢筋 | 按编号 | 必须 | 已确认 | GBK offset `7760392`，UTF16 offset `15344854`；Dialog #303。 |
| 查询钢筋 | 按直径 | 必须 | 已确认 | GBK offset `7760415`，UTF16 offset `10312708`。 |
| 查询钢筋 | 按部位 | 必须 | 已确认 | UTF16 offset `16742854`；Dialog #486 部位查询结果。 |
| 检查 | 未割钢筋 / 未剖到钢筋 | 必须 | 部分确认 | Dialog #438 `未剖到钢筋`；Dialog #1404 `提示: 以下钢筋未剖到`；按钮文字需运行确认。 |
| 检查 | 体外钢筋 | 必须 | 已确认 | UTF16 offset `10311474`；旧字符串含 `查体外钢筋`。 |
| 测量 | 测量 | 必须 | 已确认 | GBK offset `7951260`，UTF16 offset `10306976`；Dialog #116。 |
| 测量 | 结构属性 | 必须 | 已确认 | UTF16 offset `16743050`。 |
| 测量 | 碰撞检测 | 必须 | 已确认 | UTF16 offset `10312770`；Dialog #449；旧命令 `checkoverlapsteel`。 |
| 测量 | 主筋 | 必须 | 已确认 | GBK offset `7714416`，UTF16 offset `10312784`。 |
| 测量 | 箍筋 | 必须 | 已确认 | 同钢筋页箍筋字符串；距离检查命令含 `checkgmindststeel`。 |

## 工程图页

| 分组 | 功能 | 一期 | 状态 | 证据/说明 |
|---|---|---:|---|---|
| 定义视图 | 割切面 / 剖切面 | 必须 | 部分确认 | Dialog #316 剖切面位置和尺寸；Dialog #485 定义转折剖面；按钮文字需运行确认。 |
| 定义视图 | 投影面 | 必须 | 已确认 | GBK offset `7788068`，UTF16 offset `10313200`；Dialog #269。 |
| 定义视图 | 轴测投影 | 必须 | 已确认 | GBK offset `7768768`，UTF16 offset `16745840`；Dialog #484。 |
| 输出 | 生成工程图 | 必须 | 已确认 | GBK offset `7842588`，UTF16 offset `16742744`；Detail 包证据已确认。 |
| 输出 | 下料表 | 必须 | 部分确认 | GBK offset `7842604`，UTF16 offset `16745880`；Dialog #427；Detail 包 `StbTable/MaterialTable`；`E-IDA-020` 已确认内部命令绑定 `psexcel / 0x8D1C / sub_140605B20`；`E-IDA-021` 已确认 `0x8D1C` 被挂到 `工程图 / 输出` Ribbon 分组；按钮 caption、Dialog #427 和运行输出仍需确认。 |
