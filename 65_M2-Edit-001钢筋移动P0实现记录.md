# M2-Edit-001 钢筋移动 P0 实现记录

## 结论

本轮完成 `TODO-029 / M2-Edit` 的第一个可验证编辑命令切片：

```text
Rebar.Edit.Move / 钢筋移动 P0
```

本轮不是完成整个钢筋编辑专项。

完成范围是：

```text
按 IDA MCP 证据确认 barmove 的移动原对象链路。
在 domain/rebar 新增 RebarEditMoveService。
按 copyFlag=0 / translate_transf 语义实现钢筋组整体平移 P0。
保留 group / bar / segment 身份、binding、geometryRef、evidence。
```

## IDA MCP 证据

新增证据：

```text
E-IDA-023
```

已确认调用链：

```text
barmove
  -> sub_1404D5040
  -> sub_14058B770
  -> sub_1404EED00 / Input_Choice
  -> sub_1404EF8B0
  -> sub_1404F1170
  -> sub_1405AA5D0 / sub_140447210
```

关键结论：

```text
sub_1404D5040
  选择集数量 >= 1。
  sub_1405BC870 校验选中对象类型。
  取选中包装对象 +80 指针放入 ENTITY_LIST。
  调用 sub_14058B770(..., selectedList, 0, -2)。

copy flag = 0
  对应移动原对象，不是复制。

Input_Choice + 1152
  移动方式 mode。

Input_Choice + 1160
  SPAtransf。

Input_Choice + 1272
  初始选中钢筋实体列表。

sub_1404EF8B0
  根据 mode 生成 SPAtransf。

mode 4
  两个 VERTEX 相减生成平移向量，再 translate_transf。

mode 5
  三个 double 构造 SPAvector，再 translate_transf。

sub_1404F1170
  UpdateData(TRUE) 后生成 SPAtransf。
  复制选中实体列表。
  进入 ACIS bulletin board 事务。
  移动路径调用 sub_1405AA5D0(entityList, ..., SPAtransf, 0)。
```

字符串证据：

```text
钢筋移动
选择移动方式
Z向移动距离(毫米)
平移距离
```

## 实现内容

新增：

```text
app/src/domain/rebar/RebarEditMoveService.h
app/src/domain/rebar/RebarEditMoveService.cpp
app/tests/unit/rebar_edit_move_tests.cpp
```

修改：

```text
app/CMakeLists.txt
tools/phase1_readiness_gate/check_phase1_readiness.py
```

`RebarEditMoveService::moveGroups` 行为：

```text
输入 SteelData + groupIds + translation。
先解析所有 group / bar / segment 引用。
任何引用缺失或对象 inactive，稳定失败并不做部分移动。
解析成功后统一平移 segment.startPoint / middlePoint / endPoint。
不修改 groupId / barId / segmentId。
不修改 binding / geometryRef。
追加本次移动证据。
返回 movedGroupCount / movedBarCount / movedSegmentCount。
```

事务口径：

```text
先预校验，再统一写入。
缺 group / bar / segment 不会部分移动。
```

这是对旧 `api_bb_begin -> update_from_bb -> api_bb_end` 事务思想的 P0 领域层映射。

## TDD

新增测试：

```text
rebar_edit_move_tests
```

覆盖：

```text
line segment 起点 / 中点 / 终点整体平移。
arc segment 起点 / 中点 / 终点整体平移。
group / bar / segment ID 保留。
binding state / binding items 保留。
evidence 追加。
空选择稳定拒绝。
缺 group 稳定拒绝。
缺 bar 稳定拒绝且不部分移动。
```

## 验证结果

窄测：

```text
cmake --build app/build --target rebar_edit_move_tests
app/build/rebar_edit_move_tests.exe
pass
```

全量 CTest：

```text
14 / 14 passed
```

真实 `123.stp` 测试：

```text
step_import_probe_123 = pass
occ_selection_index_123 = pass
legacy_geometry_adapter_123 = pass
```

Readiness gate：

```text
py -3 tools/phase1_readiness_gate/check_phase1_readiness.py --strict

decision = M1-Formal-Ready
84 / 84 pass
0 error
0 warning
```

说明：

```text
本机 `python` 命令当前静默返回 1。
本轮 gate 使用 `py -3` 验证通过。
```

OCCT / AIS 泄漏扫描：

```text
rg -n "TopoDS_|AIS_|BRep|TopAbs_" app/src/domain/rebar app/src/drawing app/src/project
no matches
```

## xhigh 只读 Review

本轮代码节点执行了 xhigh 只读 review。

Round 1：

```text
verdict = block
critical = none
important = 2
```

Important 已修复：

```text
RebarEditMoveService 增加 bar.groupId 与当前 group 的一致性校验。
RebarEditMoveService 增加 segment.barId 与当前 bar 的一致性校验。
新增缺 bar、串组 bar、串 bar segment 的拒绝测试。
失败路径保持不部分移动。
```

Round 2：

```text
verdict = allow_commit_after_report_update
critical = none
important = none
```

Round 2 唯一尾项是把复审结论写回 build report。
该尾项已在 `docs/phase1/app_build_reports/m2_edit_001_run_001.md/json` 中完成。

子代理生命周期：

```text
xhigh_rebar_move_reviewer = 已完成后由主流程 close_agent 关闭。
xhigh_rebar_move_reviewer_round2 = 已完成后由主流程 close_agent 关闭。
```

## 边界

本轮不声明：

```text
旧图石钢筋移动完整 1:1 行为已闭合。
旧 ACIS topology mutation 已复刻。
旧 Input_Choice 所有 mode 已复刻。
dirty / undo / redo / 保存提示已闭合。
旧图石运行 golden 已完成。
UI handler 已把钢筋移动按钮接到真实 service。
钢筋拷贝、组合并、组合开、段连接、裁剪已完成。
```

## 剩余 GAP

```text
GAP-DEV-003
  撤销 / 重做粒度仍未确认。

GAP-DEV-004
  dirty 标记和保存提示口径仍未运行确认。

GAP-REB-E-001
  钢筋移动完整旧行为仍需继续 IDA / 旧图石运行确认。
```

## 下一步

建议下一轮进入：

```text
TODO-032 / M2-Edit-002
  -> Rebar.Edit.Copy / 钢筋拷贝证据与 P0 切片
```

原因：

```text
钢筋移动已经确认 copyFlag=0。
旧拷贝和移动共用 Input_Choice 框架，下一步查 copyFlag=1 和 Dialog #384 更顺。
```
