# 面配筋专项复刻说明

## 目标

本文件把 `面配筋` 从散落证据收拢成第一份可开发样板。

结论先写清：

```text
面配筋不是用 OCCT 重新想一个算法。
面配筋要按旧图石的窗口、选择、字段、对象和输出链路逐项复刻。
OCCT 只提供面、边、点、曲线、投影、距离等几何能力。
```

## 追溯信息

- Requirement ID：`REQ-REB-C-001`
- 新 CommandId：`Rebar.Create.Face`
- 旧 UI 路径：`钢筋 / 主要钢筋 / 面配筋`
- 当前闭合状态：`ReadyForSpike`
- 主要样本：`消力池下游侧带齿槽底板结构图石钢筋模型.sfl`
- 关联缺口：`GAP-DEV-004`、`GAP-DEV-005`、`GAP-DEV-006`

## 证据清单

已确认：

- `E-DLG-265`：配筋主窗口。
- `E-DLG-266`：配筋结构信息窗口。
- `E-DLG-268`：配筋钢筋信息窗口。
- `E-SFL-001`：小样本对象树出现 `面配筋_x`。
- `E-RUN-001`：用户已打开小样本并确认面配筋属性窗口。

功能矩阵证据：

- `VisualTS.exe` GBK offset `7704369` 命中 `面配筋`。
- `VisualTS.exe` UTF16 offset `10304830` 命中 `面配筋`。

当前不足：

- 旧图石点击 `确定 / 应用 / 取消` 后的 dirty 行为未确认。
- 自动连接、快速配筋、智能分区的算法规则未闭合。
- 内部编号、`rsdID`、`groupID`、`stbNum` 生成规则未闭合。

## 旧窗口结构

面配筋属性窗口至少包含两个页签：

- 结构信息。
- 钢筋信息。

### 结构信息

字段：

- 配筋面。
- 方向。
- 直线。
- 范围：起点、终点、参考面。
- 引导线。
- 自动连接。
- 快速配筋。
- 智能分区。
- 显示钢筋。

开发映射：

| 旧字段 | 新模型字段 | 状态 |
|---|---|---|
| 配筋面 | `FaceRebarParameters.faceId` | 已确认需要 |
| 方向 | `FaceRebarParameters.direction` | 已确认需要 |
| 直线 | `FaceRebarParameters.directionLineId` | 部分确认 |
| 起点 | `FaceRebarParameters.rangeStart` | 已确认需要 |
| 终点 | `FaceRebarParameters.rangeEnd` | 已确认需要 |
| 参考面 | `FaceRebarParameters.referencePlaneId` | 已确认需要 |
| 引导线 | `FaceRebarParameters.guideLineId` | 已确认需要 |
| 自动连接 | `FaceRebarParameters.autoConnect` | 已确认字段，规则待闭合 |
| 快速配筋 | `FaceRebarParameters.fastMode` | 已确认字段，规则待闭合 |
| 智能分区 | `FaceRebarParameters.smartPartition` | 已确认字段，规则待闭合 |
| 显示钢筋 | `FaceRebarParameters.previewVisible` | 已确认字段，语义待运行确认 |

### 钢筋信息

字段：

- 式样。
- 类型，例如 III级钢。
- 直径，单位毫米。
- 间距，单位厘米。
- 保护层，单位厘米。
- 外层钢筋直径。
- 内层 / 外层。
- 起点距离、终点距离，单位厘米。
- 首端 / 尾端：斜面、外表面、延长、弯钩。
- 首末增减根数。
- 弯钩长度。
- 弯钩角度。

开发映射：

| 旧字段 | 新模型字段 | Detail 字段 | 状态 |
|---|---|---|---|
| 式样 | `RebarStyle.styleId` | `StyleRoot.Name` | 已确认 |
| 类型 | `RebarStyle.steelType` | `type` / `stbLevel` | 部分确认 |
| 直径 | `RebarStyle.diameter` | `dia` / `diameter` | 已确认 |
| 间距 | `RebarGroup.interval` | `interval` | 已确认 |
| 保护层 | `RebarStyle.coverDepth` | `depth` | 部分确认 |
| 内层 / 外层 | `RebarGroup.layer` | `stbLayer` | 部分确认 |
| 起点距离 | `RebarStyle.startCoverDepth` | `depth_start` | 部分确认 |
| 终点距离 | `RebarStyle.endCoverDepth` | `depth_end` | 部分确认 |
| 首端弯钩 | `RebarStyle.startHook` | `anchor_start` / `m_Anchor1` | 部分确认 |
| 尾端弯钩 | `RebarStyle.endHook` | `anchor_end` / `m_Anchor2` | 部分确认 |
| 首端增减根数 | `RebarStyle.addNumStart` | `m_AddNum_start` | 部分确认 |
| 末端增减根数 | `RebarStyle.addNumEnd` | `m_AddNum_end` | 部分确认 |

## 建议选择流程

当前可开发的保守流程：

```text
点击面配筋
  -> 进入 Armed
  -> 若当前已选中一个结构面，进入参数窗口
  -> 否则进入 PickingFace
  -> 选择配筋面
  -> 选择方向 / 方向线
  -> 选择范围起点 / 终点 / 参考面
  -> 可选选择引导线
  -> 打开参数窗口
  -> 应用时生成预览
  -> 确定时提交 RebarGroup
```

需要运行确认：

- 旧图石是先弹窗口再选面，还是先选面再弹窗口。
- `应用` 是否创建真实对象，还是只刷新预览。
- `取消` 是否删除预览对象。
- 修改已有 `面配筋_x` 时是否复用同一窗口。

## 新领域对象

### FaceRebarParameters

首批字段：

- `faceId`
- `direction`
- `directionLineId`
- `rangeStart`
- `rangeEnd`
- `referencePlaneId`
- `guideLineId`
- `autoConnect`
- `fastMode`
- `smartPartition`
- `previewVisible`
- `styleId`
- `diameter`
- `interval`
- `coverDepth`
- `layer`
- `startDistance`
- `endDistance`
- `startEndTreatment`
- `endEndTreatment`
- `hookLength`
- `hookAngle`
- `addNumStart`
- `addNumEnd`

约束：

- 参数对象必须能序列化进新设计文件格式，并标注旧图石证据来源。
- 参数对象必须能反向打开属性窗口。
- 参数对象必须和生成出的 `RebarGroup` 关联。

### 输出对象

确定后至少生成：

```text
RebarGroup
  -> Rebar[]
      -> RebarSegment[]
```

字段要求：

- `RebarGroup.createCommand = Rebar.Create.Face`
- `RebarGroup.sourceFaceId = FaceRebarParameters.faceId`
- `RebarGroup.createdFromParameters = FaceRebarParameters`
- `RebarGroup.styleId = FaceRebarParameters.styleId`
- `RebarGroup.interval = FaceRebarParameters.interval`

## dirty 和撤销

提交成功：

- `ProjectDirty`
- `RebarDirty`
- `DrawingDirty`

预览：

- `ViewDirty`
- 是否触发 `ProjectDirty` 待运行确认。

撤销：

- 删除本次创建的 `RebarGroup`。
- 删除关联的 `Rebar` 和 `RebarSegment`。
- 恢复选择集和模型树状态。

取消：

- 不应留下正式 `RebarGroup`。
- 如果旧图石会留下半成品，需要运行确认后再改。

## 工程图映射

面配筋生成对象后，必须能进入 Detail writer：

```text
RebarGroup
  -> StbGroup

RebarSegment
  -> StbGeo

RebarGroup / Rebar[]
  -> StbTable / StbRow

RebarStyle
  -> Detail.xml / StyleRoot
```

关键字段：

- `RebarGroup.groupId -> StbGroup.groupID`
- `RebarGroup.rsdId -> StbGroup.rsdID / StbRow.rsdID`
- `RebarGroup.interval -> StbGroup.interval`
- `RebarGroup.barCount -> StbGroup.barcount`
- `RebarSegment.segmentId -> StbGeo.segID`

暂不强行复刻：

- `rsdID` 旧算法。
- `stbNum` / `stbNumAct` 旧编号算法。

首期做法：

- 内部 ID 稳定。
- Detail writer 从内部 ID 派生字段。
- 旧编号算法后续用运行样本校准。

## OCCT 使用边界

OCCT 可以用于：

- 获取配筋面的几何边界。
- 计算面上方向线。
- 计算投影点和范围。
- 生成钢筋中心线几何。
- 判断钢筋是否在面内或体内。
- 后续做碰撞、距离、剖切。

OCCT 不负责：

- 自动决定配筋规则。
- 自动决定编号。
- 自动决定下料合并。
- 自动决定图石的自动连接、快速配筋、智能分区语义。

## Spike 验收

第一轮技术验证不追求完全复刻算法，只验证架构闭环。

输入：

- 一个导入的 STEP/STP 结构面。
- 手动指定方向、间距、直径、保护层。

输出：

- 一个 `RebarGroup`。
- 多根 `Rebar`。
- 每根至少一个 `RebarSegment`。
- 模型树出现新组。
- 查询按编号或组能定位。
- Detail writer 能生成首期可验证 `StbGroup` 和 `StbGeo`。

通过条件：

- 领域模型、命令系统、OCCT Viewer、工程图 writer 之间能串通。
- dirty 标记正确传播。
- 撤销能删除本次创建对象。

不通过条件：

- 生成对象只存在显示层，没有进入 `RebarModel`。
- 下料表和工程图使用不同 ID。
- 取消/撤销后残留对象。

## 待运行确认

优先确认：

1. 点击 `面配筋` 时旧软件是否要求预选面。
2. `应用` 和 `确定` 的区别。
3. `显示钢筋` 勾选对预览和正式对象的影响。
4. 自动连接关闭/开启后的生成差异。
5. 快速配筋和智能分区的默认状态。
6. 创建后模型树节点命名规则。
7. 创建后保存提示和工程图 dirty 行为。

## 后续输出

面配筋专项闭合后，应同步更新：

- `08_开发命令契约.md`
- `09_钢筋领域模型草案.md`
- `11_需求证据追溯矩阵.md`
- `12_运行确认日志.md`
- `13_Detail字段映射矩阵.md`
- `99_缺口和待确认项.md`
