# M2-Edit-002 钢筋拷贝 P0 实现记录

## 结论

本轮完成：

```text
TODO-032 / M2-Edit-002
Rebar.Edit.Copy / 钢筋拷贝 P0
```

完成范围是：

```text
按 IDA MCP 证据确认 scopy 的 copyFlag=1 拷贝链。
在 domain/rebar 新增 RebarEditCopyService。
按“先复制，再对复制件应用累计 SPAtransf”的旧链路实现 P0。
源 group / bar / segment 保持不变。
复制件生成新 group / bar / segment，并保持引用一致。
复制件不继承未确认旧编号、legacyRaw、source/ref 几何语义。
复制件 binding / geometryRef 标为需要重新绑定。
```

本轮不是完成整个钢筋编辑专项。

## IDA MCP 证据

新增证据：

```text
E-IDA-024
```

IDA MCP 会话：

```text
session = visualts_i64_todo032
database = VisualTS.exe.i64
Hex-Rays = ready
```

已确认调用链：

```text
scopy
  -> sub_1405A9DD0
  -> sub_14058B770(..., copyFlag=1)
  -> sub_1404EED00 / Input_Choice
  -> sub_1404EF8B0
  -> sub_1404F1170
  -> sub_1405989C0
  -> sub_1405AA5D0
```

关键结论：

```text
scopy string = 0x140768D80
handler table = 0x14095ADF8
handler = sub_1405A9DD0

sub_1405A9DD0
  筛选 sub_1405C6820 / sub_1405F17C0 通过的选择对象。
  调用 sub_14058B770(..., selectedList, 1)。

copy flag = 1
  对应复制后变换，不是移动原对象。

sub_1404F1170
  copyFlag=1 时调用 sub_1405989C0 复制源实体。
  再按累计 SPAtransf 调用 sub_1405AA5D0 应用变换。
  多份复制时，第 N 份使用叠加 N 次的同一变换。
```

不能过度推断：

```text
旧 groupID / rsdID / displayNumber / actualNumber / sequenceNo 生成规则未闭合。
旧 dirty / undo / 保存提示未运行确认。
旧错误提示文本未闭合。
旧 ACIS topology mutation 不在 P0 复刻范围。
```

## 实现内容

新增：

```text
app/src/domain/rebar/RebarEditCopyService.h
app/src/domain/rebar/RebarEditCopyService.cpp
app/tests/unit/rebar_edit_copy_tests.cpp
```

修改：

```text
app/CMakeLists.txt
tools/phase1_readiness_gate/check_phase1_readiness.py
```

`RebarEditCopyService::copyGroups` 行为：

```text
输入 SteelData + groupIds + translation + copyCount + idSets。
先解析所有源 group / bar / segment 引用。
任何引用缺失、对象 inactive、ID 映射缺失或新 ID 冲突时，稳定失败且不做部分复制。
解析成功后 staging 复制件。
复制件按 copyIndex 叠加平移向量。
统一把复制件追加到 SteelData。
```

ID 口径：

```text
P0 不猜旧图石编号规则。
调用方必须通过 RebarCopyIdSet 显式提供新 group / bar / segment ID。
旧编号、rsdID、显示编号和 legacy raw 字段被清空并登记 unresolvedLegacyFields。
```

binding / geometryRef 口径：

```text
复制件不继承源 binding item。
复制件 binding.state = UnresolvedGeometry。
复制件 geometryRef 清空。
```

原因：

```text
旧 ACIS 对象复制会生成新实体和 edge 链。
新系统 P0 当前只有 domain DTO，没有真实 topology clone。
保留源 binding / geometryRef 会误导后续保存和 Detail writer。
```

## TDD

新增测试：

```text
rebar_edit_copy_tests
```

覆盖：

```text
复制两份时，源对象不变。
第 1 份使用 1 倍平移，第 2 份使用 2 倍平移。
复制件 group -> bar -> segment 引用一致。
复制件 binding 变为 UnresolvedGeometry。
复制件 geometryRef 清空。
复制件旧编号、legacyRaw、source/ref 字段清空并登记 unresolved。
证据追加。
缺 id set 稳定拒绝。
重复 new group id 稳定拒绝。
缺 new bar id 稳定拒绝。
缺 new segment id 稳定拒绝。
重复 new bar id 稳定拒绝。
重复 new segment id 稳定拒绝。
源 segment 串 bar 时稳定拒绝。
copyCount 非 1..1000 稳定拒绝。
所有失败路径不部分复制。
```

TDD RED：

```text
cmake --build app/build --target rebar_edit_copy_tests
失败：RebarEditCopyService.h 不存在
```

GREEN：

```text
cmake --build app/build --target rebar_edit_copy_tests
app/build/rebar_edit_copy_tests.exe
pass
```

## 验证结果

窄测：

```text
rebar_edit_copy_tests = pass
```

全量 CTest：

```text
15 / 15 passed
```

真实 `123.stp` 测试：

```text
step_import_probe_123 = pass
occ_selection_index_123 = pass
legacy_geometry_adapter_123 = pass
```

Readiness gate：

```text
py -3 tools/phase1_readiness_gate/test_phase1_readiness_gate.py
10 / 10 pass

py -3 tools/phase1_readiness_gate/check_phase1_readiness.py --strict
M1-Formal-Ready
84 / 84 pass
0 error
0 warning
```

OCCT / AIS 泄漏扫描：

```text
rg -n "TopoDS_|AIS_|BRep|TopAbs_" app/src/domain/rebar app/src/drawing app/src/project
no matches
```

## xhigh 只读 Review

本轮代码节点执行了 xhigh 只读 review。

Round 1：

```text
verdict = block
critical = 2
important = 2
minor = 1
```

Critical 已处理：

```text
1. E-IDA-024 不能只出现在代码和测试里。
   处理：已补 03_IDA命令证据.md、11 追溯矩阵和本实现记录。

2. 复制件整对象拷贝会继承旧编号 / legacyRaw / source refs。
   处理：复制件清空 rsdID / displayNumber / actualNumber /
        sequenceNo / legacyRaw / createdFromParameters /
        sourceFaceId / sourceLineId / sourceCurveIds / referenceIds；
        并登记 GAP-REB-E-002 unresolvedLegacyFields。
```

Important 已处理：

```text
1. copyCount 1..1000 不是完整旧 UI 失败口径。
   处理：作为 P0 安全边界写入 GAP-REB-E-002，旧提示文本和 UI 分支待确认。

2. 缺少 bar / segment 映射失败路径测试。
   处理：新增 missing new bar id、missing new segment id、
        duplicate new bar id、duplicate new segment id 测试。
```

子代理生命周期：

```text
xhigh_rebar_copy_reviewer = 已完成后由主流程 close_agent 关闭。
```

Round 2：

```text
verdict = allow_commit
critical = none
important = none
minor = none
```

子代理生命周期补充：

```text
xhigh_rebar_copy_reviewer_round2_initial = 长上下文复审连续超时，主流程 close_agent 关闭，未采用结论。
xhigh_rebar_copy_reviewer_retry = 轻量只读复审返回 allow_commit，主流程 close_agent 关闭。
```

## 边界

本轮不声明：

```text
旧图石钢筋拷贝完整 1:1 行为已闭合。
旧 ACIS topology clone 已等价复刻。
旧编号 / rsdID / displayNumber / sequenceNo 生成规则已闭合。
dirty / undo / redo / 保存提示已闭合。
UI 钢筋拷贝按钮已接到 service。
golden 已采集。
统计、工程图、组合并、组合开、段连接、裁剪已完成。
```

## 剩余 GAP

```text
GAP-REB-E-002
  钢筋拷贝完整旧行为仍需继续 IDA / 旧图石运行确认。

GAP-DEV-003
  撤销 / 重做粒度仍未确认。

GAP-DEV-004
  dirty 标记和保存提示口径仍未运行确认。

GAP-DEV-005
  内部 ID 与旧编号关系仍未闭合。
```

## 下一步

建议下一轮进入：

```text
TODO-030 / M2-Stats
  -> 钢筋统计 / 下料表专项
```

原因：

```text
移动和拷贝两个编辑切片已经形成 P0。
统计 / 下料表会反过来要求编号、数量、长度和质量规则闭合，
能继续推动 GAP-DEV-005 / GAP-DRAW-004 的证据收敛。
```
