# 技术路线与替代方案

## 结论

功能矩阵只回答“旧软件有哪些入口、按钮和窗口”。本文件回答“新系统用什么技术把这些功能做出来，以及哪些旧商业库不再使用”。

当前路线：

```text
Qt6
  -> 主窗口 / Ribbon 类工具区 / 参数窗口 / 模型树 / 状态栏 / 命令调度

OCCT
  -> STEP 导入 / BRep 几何 / 布尔 / 剖切 / 投影 / 距离 / 显示底座

旧图石证据驱动的钢筋复刻层
  -> 钢筋对象 / 钢筋组 / 参数规则 / 编辑规则 / 查询统计 / 工程图映射

AutoCAD 2020 商业版 + 图石旧插件包格式
  -> Detail.xml + DetailNN.stl XML 包导入 / 出图 / 打印 / 二次编辑
```

这不是“用 OCCT 重新发明一个钢筋软件”的路线。

真正的一比一复刻对象是旧图石的功能操作和业务结果；Qt6 和 OCCT 只是替代旧界面库、旧几何库、旧显示库的底座。

父目录源码只能作为工程实现参考。父目录里的 Qt / OCCT / STEP / Detail writer / 钢筋模型代码，不能替代旧图石运行、IDA、SFL 和 Detail 包证据；路线边界见 `23_父目录源码参考边界与路线纠偏.md`。

## 一比一复刻的真实边界

一比一复刻要保留的是：

- 旧软件的按钮入口。
- 旧软件的参数窗口。
- 旧软件的选择顺序。
- 旧软件的默认参数、灰显、错误提示。
- 旧软件生成的钢筋对象结构。
- 旧软件查询、碰撞、距离、下料、工程图输出结果。

不要求一比一保留的是：

- MFC / Codejock 的皮肤。
- ACIS / HOOPS 的 API。
- 不把老 `.sfl` 作为首期主工程格式。
- 旧软件内部类名和内存布局。

但老 `.sfl` 不是废掉：

- `.sfl` 继续作为旧业务对象、字段顺序、对象关系和样本统计证据。
- 新设计文件格式必须吸收 `.sfl` / IDA / Detail 证据里的 `legacyObject`。
- OCCT / STEP 只负责新系统几何承载、拓扑选择和计算能力。
- 新设计文件格式必须用 `binding` 把旧业务对象和 OCCT 几何引用明确绑定，不能只保存两堆互不相干的数据。

白话说：

```text
用户操作要像图石。
业务结果要对齐图石。
底层库不需要像图石。
```

## 本地证据确认的旧商业/闭源组件

当前本地证据支持旧图石依赖以下商业或闭源组件：

- Spatial ACIS / SpaACIS / SPAX ACIS。
  - 证据关键词：`SpaACIS.dll`、`SPAAcisDs.dll`、`SPAXAcis*.dll`、`.sat`、`.sfl`。
  - SFL 样本头：`ACIS BinaryFile / ACIS 31.0 NT`。
- HOOPS Visualize 25.10。
  - 证据关键词：`hoops2510_x64_v141.dll`、`hoops_mfcu2510_x64_v141.dll`、`hoops_mvo2510_x64_v141.dll`、`hps_license.dll`。
- Codejock ToolkitPro。
  - 证据关键词：`ToolkitPro1501vc141x64.dll`。
- MFC / VC141 x64 老 Windows 桌面栈。
  - 证据关键词：`mfc140`、`vc141x64`。
- AutoCAD 2020 图石旧插件链。
  - 证据关键词：`FDrawing.arx`、`FDrawingObj.dbx`、`Detail.xml`、`Detail01.stl`。

本地证据没有确认旧图石使用 Qt。Qt6 只作为新系统替代方案出现。

替代关系：

```text
MFC / Codejock       -> Qt6
ACIS / SpaACIS       -> OCCT
HOOPS Visualize      -> OCCT AIS / Qt Viewer
老 .sfl 主保存格式    -> 新设计文件格式
DWG/DXF 直接写出      -> 暂不做，优先旧 AutoCAD 插件包
```

其中“新设计文件格式”不是单纯替换扩展名。

它的真实内容是：

```text
SFL / IDA / Detail 反推的 legacyObject.raw
  + OCCT / STEP 承载的 geometryRef
  + 二者之间的 binding
  + 保存事务、证据追溯、缺口治理
```

## 旧技术和新替代

### MFC / Codejock -> Qt6

旧图石是典型 Windows 桌面程序，界面形态接近 MFC + Ribbon/工具栏体系。

新系统建议用 Qt6，原因是：

- Qt6 是当前仍在维护的 C++ 桌面框架，不是过时路线。
- Qt Widgets 适合复杂桌面软件、参数窗口、树、表格、停靠面板。
- Qt6 可以和 OCCT Viewer 集成。
- 对老 MFC 软件来说，Qt Widgets 比纯 Web 或 Qt Quick 更稳妥。

注意事项：

- 优先使用 Qt Core / Gui / Widgets / OpenGL 相关基础模块。
- 不随便引入 Qt GPL-only 模块。
- 如果用 LGPL 方案，工程上倾向动态链接，并保留许可证合规清单。
- 若后续公司决定完全规避 LGPL 合规成本，再评估 Qt 商业授权。
- 实际允许模块、禁止模块、动态链接和 notice 要按 `21_QtOCCT依赖许可证门禁.md` 执行。

官方依据：

- Qt 官方许可页：`https://doc.qt.io/qt-6/licensing.html`
- Qt LGPL obligations：`https://www.qt.io/licensing/open-source-lgpl-obligations`

### ACIS -> OCCT

旧 `.sfl` 样本显示为：

```text
ACIS BinaryFile / ACIS 31.0 NT
```

这说明旧图石几何底层很可能依赖 ACIS 或 ACIS 兼容保存链路。

新系统不建议继续依赖 ACIS，原因是：

- ACIS 是商业几何内核。
- 当前没有 ACIS 授权。
- 直接解析 ACIS BinaryFile 会把一期目标拖入高风险兼容工程。

OCCT 可以替代的部分：

- STEP/STP 导入。
- BRep 拓扑和几何。
- 布尔运算。
- 曲线、边、面、实体建模。
- 投影、剖切、距离计算。
- HLR/隐藏线类工程图基础能力。
- AIS 三维显示。

OCCT 不能自动替代的部分：

- 图石的面配筋规则。
- 图石的线配筋规则。
- 图石的扇形筋、箍筋、孔口、插筋、螺旋筋等业务生成逻辑。
- 图石钢筋组的编号、分段、合并、断开、统计规则。
- 旧 `.sfl` 里的全部业务字段语义。

白话说：

```text
OCCT 管“几何怎么算”。
图石复刻层管“钢筋为什么这么生成”。
```

官方依据：

- OCCT 官方许可页：`https://dev.opencascade.org/resources/licensing`
- OCCT 官方概览：`https://dev.opencascade.org/doc/overview/html/index.html`
- OCCT 商业组件列表：`https://dev.opencascade.org/resources/components`

### HOOPS -> OCCT AIS / Qt Viewer

旧软件很可能使用 HOOPS 或类似商业显示库承担三维视图。

新系统建议首期用 OCCT AIS + Qt Viewer，原因是：

- 和 OCCT 几何对象天然衔接。
- 支持选择、高亮、显示模式、剖切辅助。
- 足够支撑“显示页”和“钢筋编辑选择流程”的首期复刻。

后续如果 OCCT AIS 在大模型性能上不够，再评估专门显示库。

首期不要为了显示效果引入新的商业 3D SDK。

### AutoCAD / DWG -> 保留 CAD 商业版，输出旧插件包

用户已说明 CAD 有商业版，其他商业库没有。

所以工程图路线是：

```text
新系统生成 Detail.xml + DetailNN.stl XML 包
  -> 旧 AutoCAD 2020 图石插件导入
  -> AutoCAD 内完成 DWG 图纸、打印、二次编辑
```

首期不直接写 DWG/DXF，原因是：

- DWG 是复杂商业格式。
- 直接写 DWG 会引入 ODA/RealDWG 等新商业依赖风险。
- 当前目录已有旧图石 AutoCAD 插件和 `Detail.xml` / `Detail01.stl` 样例，可作为更快闭环。

注意：

- 这里的 `DetailNN.stl` 不是三角网格 STL，而是 XML 结构。
- 工程图 writer 要按旧插件包字段生成，不按普通 STL 生成。
- 工程图 writer 的临时写入、校验、覆盖和回滚策略见 `20_DetailWriter输出事务契约.md`。

## 非商业库替代候选

### 首选组合

```text
Qt6 + OCCT + 旧图石证据驱动的钢筋复刻层 + 旧 AutoCAD 插件包 writer
```

这是当前最快路线。

### 其他候选

- FreeCAD：基于 OCCT 的完整 CAD 应用，适合参考架构，不适合直接嵌入复刻图石。
- SALOME：工程平台能力强，但体量重，不适合作为最快复刻底座。
- CGAL：几何算法强，但不是完整 BRep CAD 内核，不适合作为 ACIS 的主替代。
- VTK：显示和科学可视化强，但不是 CAD 建模内核；可作为后续显示增强候选。
- OpenSceneGraph / Ogre：偏渲染引擎，不能替代钢筋业务和 CAD 几何内核。
- QCAD / LibreCAD：偏 2D CAD，不适合三维结构配筋主流程。

结论：

```text
更接近 ACIS 的开源替代是 OCCT。
更接近 HOOPS 显示的开源替代可先用 OCCT AIS。
更接近图石钢筋模块的开源库目前没有发现。
钢筋业务必须靠逆向证据 + 运行确认 + 旧图石证据驱动的复刻层还原。
```

## 最快复刻分层

### M1：可操作外壳

目标：

- Qt6 主窗口。
- 顶部页签和按钮。
- 命令注册表。
- 模型树。
- 属性面板。
- 状态栏提示。
- OCCT Viewer。
- STEP/STP 导入和显示。
- LegacyGeometryAdapter：把 OCCT EDGE / FACE 查询包装成旧 VisualTS 可用的几何语义。

这个阶段先让软件“像图石一样能点、能选、能看模型”。

截至 `M1-App-009`，正式 app 已经完成 STEP 导入、AIS 显示、`selection-v1` 选择引用，以及 EDGE / FACE 的 bbox、采样、boundary loop、fingerprint、诊断矩阵、edge 切向、selection ref 距离、最近点对、face boundary edge stableId、edge-face 接触/重叠代表点、edge 参数区间、子段采样和 edge split 语义。

### M2：钢筋对象和核心创建

目标：

- 钢筋对象。
- 钢筋组对象。
- 钢筋段对象。
- 面配筋、线配筋、扇形筋等核心创建命令。
- 参数窗口和默认值按旧图石复刻。

注意：

这里不是拍脑袋重写钢筋算法，而是按以下证据逐个还原：

- 运行界面。
- IDA 命令入口。
- Dialog 字段。
- 旧 `.sfl` 样本对象。
- 工程图输出字段。

也不能因为父目录已有钢筋对象、创建服务或测试，就直接视为旧图石算法已经闭合。父目录只能提供写法参考，业务规则必须回到旧图石证据链。

### M3：钢筋编辑、查询、检查

目标：

- 组合并 / 组合开。
- 段连接 / 段断开。
- 钢筋移动 / 拷贝。
- 增加钢筋根数 / 增加钢筋段。
- 按编号 / 直径 / 部位查询。
- 体外钢筋、碰撞检测、最小距离。

OCCT 在这一层主要提供：

- 距离计算。
- 相交检测。
- 剖切辅助。
- 投影辅助。

业务判断仍由复刻层完成。

### M4：工程图闭环

目标：

- 剖切面 / 投影面 / 轴测投影。
- 生成工程图。
- 下料表。
- 输出 `Detail.xml + DetailNN.stl` XML 包。
- 用 AutoCAD 2020 + 旧插件动态导入验证。

### M5：老 SFL 兼容专项

目标：

- 评估是否需要读取老 `.sfl`。
- 如果需要，优先利用旧软件导出 STEP/工程图包/钢筋文本；用户已运行确认 `开始 / 文件 / 输出钢筋` 可导出钢筋 `.stp`。
- 不优先直接解析 ACIS BinaryFile。

原因：

```text
直接解析老 SFL = ACIS BinaryFile + 图石私有对象字段
风险远高于先复刻功能操作。
```

## 验收方式

每个功能不能只看“按钮做出来了”，要至少过四层验收：

1. 界面验收：按钮、窗口、字段、默认值和旧图石一致。
2. 操作验收：选择顺序、状态栏提示、错误提示一致。
3. 对象验收：生成的钢筋对象、组、段、编号、参数可查询。
4. 输出验收：工程图包、下料表、查询结果能和旧图石样例对齐。

没有加密狗或不能长期运行旧软件时，先用 IDA、样本文件和现有截图建立离线基线；等旧软件可运行时再补 golden 对照。

## 当前风险

- Qt6 LGPL/GPL/商业许可边界需要按实际使用模块做清单。
- OCCT LGPL 2.1 with exception 需要保留合规说明。
- OCCT 不能读取所有 ACIS BinaryFile；老 `.sfl` 不作为一期主输入。
- 钢筋业务规则没有现成开源替代库，逆向和运行确认仍是关键路径。
- 工程图包 writer 必须按旧 AutoCAD 插件样例验证，不能按普通 STL 理解。
- Detail writer 未通过 AutoCAD 插件动态导入前，不能宣称工程图闭环。
- Qt / OCCT 依赖清单、notice 和 SBOM 未落地前，不能进入可发布包阶段。
- 父目录技术路线偏向 OCCT 重新开发钢筋软件，不能作为本项目 1 比 1 复刻路线；见 `23_父目录源码参考边界与路线纠偏.md`。
