# 1 比 1 复刻实施路线

## 2026-06-11 路线状态

本文件记录的是早期“旧图石 / VisualTS 功能操作 1 比 1 复刻”路线，当前已不再作为 P0/P1 主执行路线。

新的主线是：

```text
STEP-only + RebarSmart 钢筋生成逻辑证据 + 图石 Detail 包兼容导出
```

后续实施顺序改为：

```text
M0 仓库治理与路线切换
  -> ADR / roadmap / README / 46 / todo / AGENTS 对齐

M1 Detail 包格式 POC
  -> DetailPackage 数据模型
  -> DetailPackageReader
  -> DetailPackageWriter round-trip
  -> 极简 Detail 包生成
  -> 旧 AutoCAD 插件人工导入验证

M2 RebarSmart 纯算法层
  -> GenerateRebarData
  -> RebarSmartIniLoader
  -> SpaceListParser
  -> Distribution
  -> GuideCurveZoneCalculator

M3 OCCT 几何层 + FixDistance / FixNumber
  -> IGeometryEngine
  -> OcctGeometryEngine
  -> FixDistanceGenerator
  -> FixNumberGenerator

M4 Qt / OCCT 操作闭环
  -> STEP 打开、选择、输入参数、生成钢筋、AIS 显示、保存 .tsrebar

M5 Detail 接 RebarModel
  -> RebarModel / DrawingModel / ScheduleModel 导出 Detail 包
```

权威新路线见：

- `docs/adr/ADR-20260611-step-only-rebarsmart-detail.md`
- `docs/roadmap/STEP_only_RebarSmart_Detail_refactor_plan.md`
- `46_CSE_v2Goal执行目标与Todo说明.md`

以下内容保留为历史路线和证据背景，不能再单独驱动 goal next。

## 历史目标

本文件回答“先做什么、后做什么、每阶段怎么验收”。

当前目标不是先做一个简化版钢筋工具，而是按旧图石功能操作逐项复刻：

```text
证据闭合
  -> UI / 命令 / 参数窗口
  -> 钢筋对象和核心生成
  -> 钢筋编辑 / 查询 / 检查
  -> 工程图包输出
  -> 老 SFL 兼容专项
```

## 总原则

- 功能入口先按 `01_功能操作矩阵.md` 对齐。
- 参数窗口先按 `02_界面窗口参数矩阵.md` 对齐。
- 命令和业务入口先按 `03_IDA命令证据.md` 对齐。
- 旧工程样本按 `04_SFL样本证据.md` 作为对象结构证据。
- 工程图输出按 `05_Detail工程图包证据.md` 对齐。
- 技术底座按 `06_技术路线与替代方案.md` 执行。
- 开发命令按 `08_开发命令契约.md` 执行。
- 钢筋领域对象按 `09_钢筋领域模型草案.md` 起步。
- 证据和验收按 `10_证据追溯与验收基线.md` 记录。
- 需求、证据和缺口按 `11_需求证据追溯矩阵.md` 串联。
- 运行确认按 `12_运行确认日志.md` 记录。
- 工程图字段映射按 `13_Detail字段映射矩阵.md` 执行。
- 面配筋专项按 `14_面配筋专项复刻说明.md` 执行。
- 线配筋和弧形组技术 spike 按 `15_线配筋与弧形组专项初稿.md` 执行。
- `seg_steelbargroup` 字段和几何规则按 `16_seg_steelbargroup字段地图初稿.md` 执行。
- 一期按钮到命令占位按 `17_一期按钮追溯与命令占位矩阵.md` 执行。
- 新设计文件格式按 `18_新设计文件格式替代SFL策略.md` 执行。
- STEP 选择 ID 稳定性按 `19_STEP选择ID稳定性Spike计划.md` 验证。
- Detail writer 输出事务按 `20_DetailWriter输出事务契约.md` 执行。
- Qt / OCCT 依赖许可证门禁按 `21_QtOCCT依赖许可证门禁.md` 执行。
- 开发前 readiness 按 `22_开发前Readiness审计.md` 审计。
- 父目录源码参考边界按 `23_父目录源码参考边界与路线纠偏.md` 执行。
- 新设计文件格式 schema 与 fixture 草案按 `24_新设计文件格式Schema与Fixture草案.md` 执行。
- SFL + OCCT 联合开发任务按 `25_新设计文件格式SFL_OCCT联合开发任务拆解.md` 执行。
- Fixture / validator 执行清单按 `26_首期可验证联合格式Fixture与Validator执行清单.md` 执行。
- Qt6 + OCCT 开发入口门禁按 `27_Qt6_OCCT开发入口门禁与首批工件清单.md` 执行。
- 未闭合项统一进 `99_缺口和待确认项.md`。

白话说：

```text
不是先自由发挥做新软件。
而是每做一个功能，都要能说清旧图石证据在哪里。
父目录源码只能参考工程写法，不能替代旧图石证据。
```

## Phase 0：证据闭合

目标：

- 把旧图石能看到的按钮、窗口、字段、默认值全部列清。
- 把 IDA 命令入口、Dialog ID、字符串证据和运行界面对应起来。
- 把 6 个 SFL 样本里的钢筋对象、钢筋组、工程图对象继续补全。
- 把 AutoCAD 工程图包字段和旧插件导入行为补全。

验收：

- `01_功能操作矩阵.md` 中一期必须项没有明显漏按钮。
- `02_界面窗口参数矩阵.md` 中核心钢筋窗口有 Dialog 或运行证据。
- `99_缺口和待确认项.md` 中每个缺口都有下一步。

进入条件：

- 已建立文档目录。
- 已锁定一期范围：开始、显示、钢筋、查询、工程图。
- 已确认 `.sfl` 只作证据，不作一期主工程格式。

退出条件：

- 每个一期必须按钮至少有 `Requirement ID`、`CommandId`、`GAP ID` 或明确占位，落点见 `17_一期按钮追溯与命令占位矩阵.md`。
- 核心证据进入 `11_需求证据追溯矩阵.md`。
- 运行确认任务进入 `12_运行确认日志.md`。

阻塞项：

- 明显按钮缺漏。
- 缺口没有关闭条件。

## Phase 1：Qt6 可操作外壳

目标：

- Qt6 主窗口。
- 顶部页签：开始、显示、钢筋、查询、工程图。
- 按钮分组和按钮文字按旧图石。
- 模型树、属性面板、状态栏、命令调度。
- OCCT Viewer 显示 STEP/STP。
- 基础选择、高亮、视图缩放、平移、旋转。

不在本阶段强求：

- 钢筋算法完全正确。
- 工程图输出完整。
- 老 SFL 完整打开。

验收：

- 用户能按旧图石入口找到功能。
- STEP 模型可导入、显示、选择。
- 每个按钮至少能进入对应命令状态、弹参数窗口或给出明确未实现提示。

进入条件：

- `01_功能操作矩阵.md` 已能支撑 Ribbon 壳。
- `17_一期按钮追溯与命令占位矩阵.md` 已能支撑 Command 注册表。
- `08_开发命令契约.md` 已定义全局命令状态机。
- `09_钢筋领域模型草案.md` 已定义 `ProjectDocument` 和 `StructureModel`。
- `18_新设计文件格式替代SFL策略.md` 已定义替代 `.sfl` 的保存策略和旧证据驱动字段规则。
- `19_STEP选择ID稳定性Spike计划.md` 已定义验证方法和通过标准。
- `21_QtOCCT依赖许可证门禁.md` 已冻结一期允许模块和禁止模块。
- `22_开发前Readiness审计.md` 已明确可做 / 不可做边界。
- `26_首期可验证联合格式Fixture与Validator执行清单.md` 已明确 Fixture A/B/C、negative fixture、validator、Save/Open 回滚测试和 STEP diff 工件。
- `27_Qt6_OCCT开发入口门禁与首批工件清单.md` 已明确 UI demo、M1-Prep 和正式 Phase 1 的区别。

补充口径：

```text
本节 Phase 1 入口不能单独理解。
正式 Phase 1 开工以 22 / 26 / 27 的增强门禁为准。
只有 Qt 窗口、按钮和 STEP 显示，只能叫 UI demo 或 M1-Prep。
```

退出条件：

- Qt6 主窗口能显示 5 个一期页签。
- 所有一期按钮有 `CommandId` 或明确占位。
- STEP/STP 可导入 OCCT Viewer。
- 选择集、状态栏、模型树能被命令系统访问。

阻塞项：

- OCCT Viewer 集成失败。
- STEP 选择对象无法稳定传给命令系统。
- 把父目录已有 UI / viewer / STEP 实现当作旧图石 1 比 1 验收结果。
- 没有实际 `DEPENDENCIES.md / THIRD_PARTY_NOTICES.md / fixture / validator / STEP diff`，却宣称正式 Phase 1 已具备开工门禁。

## Phase 2：钢筋领域模型

目标：

- 建立新系统内部钢筋模型。
- 支持钢筋、钢筋段、钢筋组、编号、直径、级别、用途、部位。
- 支持激活/钝化、显示/隐藏、属性查看。
- 支持保存到新设计文件格式。

关键点：

OCCT 不负责这层业务模型。OCCT 只提供曲线、点、面、实体等几何能力。

新系统内部钢筋模型可以借鉴父目录的代码组织，但字段、编号、组、段、统计和工程图映射必须由旧图石证据反推，不能直接继承父目录对象语义。

验收：

- 新模型能表达 SFL 样本中已经确认的 `steelbar`、`steelbargroup`、`steelData` 核心信息。
- 工程图包和下料表能引用同一套钢筋 ID。

进入条件：

- `09_钢筋领域模型草案.md` 已定义首批对象。
- `13_Detail字段映射矩阵.md` 已明确 `StbGroup/StbGeo/StbRow` 到领域模型的映射。
- 新设计文件格式字段策略完成，并能追溯旧图石证据。

退出条件：

- 能创建、保存、恢复 `RebarGroup/Rebar/RebarSegment/RebarStyle`。
- 能把同一组钢筋同时用于查询和 Detail writer。
- 内部 ID 不因移动、拷贝、工程图生成而丢失。

阻塞项：

- `RebarGroup` 与 `StbGroup` 映射无法稳定。
- STEP 面/边选择 ID 不稳定导致配筋参数无法保存。

## Phase 3：核心钢筋创建

目标：

- 面配筋。
- 线配筋。
- 扇形筋。
- 面周边。
- 同心圆。
- 角度配筋。
- 自配筋。
- 钢筋配筋。
- 角筋、孔口、箍筋、插筋、螺旋筋。

执行方式：

- 每个命令单独建专项说明。
- 先记录旧图石参数窗口和选择流程。
- 再用 IDA 入口和 SFL 样本反推对象结构。
- 最后用运行软件截图或样本输出做校验。

验收：

- 参数窗口字段和默认值对齐。
- 选择流程和提示对齐。
- 生成对象可编辑、可查询、可进入工程图包。

进入条件：

- Phase 2 的领域模型可保存恢复。
- `面配筋` 专项说明已完成到可 spike 状态。
- 至少一个样本可作为运行基线。
- 父目录钢筋创建代码只作为实现参考，不作为旧图石算法证据。

退出条件：

- 面配筋完成第一条可验收闭环。
- 线配筋和弧形组入口至少完成技术 spike。
- 创建出的钢筋能进入查询、下料、工程图 writer。

阻塞项：

- 面配筋选择流程无法确认。
- 生成算法缺少足够旧图石行为证据。

## Phase 4：钢筋编辑 / 查询 / 检查

目标：

- 组合并 / 组合开。
- 段连接 / 段断开。
- 钢筋移动 / 钢筋拷贝。
- 增加钢筋根数 / 增加钢筋段。
- 按编号、按直径、按部位查询。
- 体外钢筋。
- 碰撞检测。
- 平行钢筋最小距离。

OCCT 可承担：

- 几何相交。
- 距离计算。
- 投影、剖切辅助。

复刻层必须承担：

- 哪些对象参与检查。
- 检查结果如何分组和展示。
- 错误提示和查询结果格式。
- 钢筋组编辑后编号、段、下料信息如何更新。

验收：

- Dialog #449、#450 等检查窗口行为对齐。
- 查询结果能定位并高亮对应钢筋。
- 编辑后模型树、工程图 dirty 状态、下料信息同步更新。

进入条件：

- 至少一种钢筋创建命令能生成稳定 `RebarGroup`。
- dirty 标记规则已接入命令系统。
- 查询结果模型已定义。

退出条件：

- 移动、拷贝、组合并、段连接至少完成一轮样本验证。
- 按编号查询能反选并高亮钢筋。
- 碰撞和最小距离检查能输出结果列表。

阻塞项：

- ID 和编号规则导致编辑后下料/工程图断链。

## Phase 5：工程图和下料闭环

目标：

- 剖切面 / 割切面。
- 投影面。
- 轴测投影。
- 生成工程图。
- 下料表。
- 输出 `Detail.xml + DetailNN.stl` XML 包。

执行方式：

- 新系统内部先生成工程图中间模型。
- 再写旧 AutoCAD 插件包 writer。
- 用 AutoCAD 2020 + 旧插件导入验证。

验收：

- `Detail.xml` 能被旧插件识别样式。
- `DetailNN.stl` 的 `DrawingRoot`、`StbGroups`、`StbTable` 等字段有效。
- 下料表和图上钢筋编号一致。

进入条件：

- `RebarModel` 可供 Detail writer 使用。
- `13_Detail字段映射矩阵.md` 中首批必需字段已能生成。
- `20_DetailWriter输出事务契约.md` 中 L0/L1 writer 门禁可执行。
- AutoCAD 2020 + 旧插件运行环境可验证。

退出条件：

- 新系统生成首期可验证 `Detail.xml + Detail01.stl` XML 包。
- 旧插件能导入首期可验证包。
- `StbTable`、`MaterialTable`、`StbGroup` 引用同一套领域 ID。
- writer 失败时不覆盖旧包，并能保留 audit 记录。

阻塞项：

- 旧插件对字段缺失容忍度不明。
- `DetailNN.stl` 命名和多图纸规则未闭合。
- writer 输出事务和失败回滚未实现。

## Phase 6：老 SFL 兼容专项

目标：

- 评估是否必须读取老 `.sfl`。
- 如果必须，先通过旧图石导出 STEP、钢筋 STP、钢筋文本或工程图包；当前已运行确认 `开始 / 文件 / 输出钢筋` 可导出钢筋 `.stp`。
- 最后才考虑直接解析 ACIS BinaryFile 和图石私有字段。

原因：

```text
老 SFL = ACIS BinaryFile + 图石私有对象。
直接兼容成本高，不适合作为一期主线。
```

验收：

- 明确老 SFL 是否进入新系统主流程。
- 如果进入，必须有字段地图和样本对照。

进入条件：

- 一期功能操作复刻已有可运行闭环。
- 用户明确需要老 SFL 导入进入新系统主流程。

退出条件：

- 明确选择一种兼容策略：旧软件导出中间格式、字段级导入、或放弃直接导入。
- 如果做导入，必须建立字段地图和样本对照。

阻塞项：

- ACIS BinaryFile 无授权解析。
- 图石私有对象字段无法闭合。

## 历史推荐下一步（已被正式 app 阶段取代）

本节保留早期路线判断，作为为什么不能“直接写完整钢筋算法”的历史依据。

当前项目已经进入正式 `app` 长期 goal 阶段，不再停留在“进入 Qt6 + OCCT 外壳开发之前”。实时下一步以 `46_CSE_v2Goal执行目标与Todo说明.md` 和 `todo.csv` 的 `status=next` 为准。

早期阶段曾建议：

父目录源码参考边界先按 `23_父目录源码参考边界与路线纠偏.md` 冻结，避免把“OCCT 重新开发钢筋软件”的路线混进本项目。

历史推荐顺序：

1. 继续用 IDA 和 SFL 样本闭合新设计文件格式里的旧对象字段。
2. 闭合 `剖面圈筋`、`固中心`、`组合开`、`未割钢筋`、`割切面` 这几个按钮缺口。
3. 用 IDA 深看 `sgroupbarline`、`sgroupbararc`、`rebar`、`rebars` 的入口流程。
4. 以 `面配筋` 为样板，补完整功能追溯、命令契约、领域模型映射。
5. 按 IDA 证据推进线配筋/弧形组技术 spike，但不要声明完整复刻。
6. 再进入 Qt6 + OCCT 外壳开发。

这样能避免一开始就陷入“自己想象钢筋算法”的错误路线。

当前执行状态：

- Qt6 + OCCT 正式 app 已进入 M1/M2 长期迭代。
- LegacyGeometryAdapter、domain/rebar、DetailWriter、新工程格式、旧 UI 入口、编辑、统计和工程图字段骨架已有多个已完成节点。
- 后续不得按本节的早期顺序倒退执行；只按 `todo.csv` 和 `46` 的当前 goal 执行。
