# 父目录源码参考边界与路线纠偏

## 目标

本文件专门记录一次关键纠偏：

```text
父目录源码只能参考。
父目录原技术路线不能作为本项目路线。
本项目目标是旧图石功能操作和业务结果 1 比 1 复刻。
不是基于 OCCT 重新开发一套新钢筋软件。
```

后续开发、审计、缺口关闭都必须按这个口径执行。

## 为什么要单独写清

父目录 `C:\Users\ghost\Desktop\reverse_engineering` 已经存在一套 Qt / OCCT / STEP / 工程保存 / Detail writer / 钢筋模型相关源码。

这些源码有参考价值，但它的核心路线偏向：

```text
用 OCCT 做几何底座
  -> 自己设计一套钢筋建模软件
  -> 以新系统内部模型和测试为主线推进
```

这不是当前项目的真实目标。

当前项目的目标是：

```text
旧图石有什么按钮
  -> 旧图石怎么选对象
  -> 旧图石弹什么参数窗口
  -> 旧图石怎么生成钢筋对象
  -> 旧图石查询、下料、工程图输出什么结果
  -> 新系统按这些行为逐项复刻
```

白话说：

```text
父目录代码可以教我们“Qt6 + OCCT 怎么写”。
旧图石证据才决定“钢筋功能到底怎么做”。
```

## 权威证据顺序

本项目判断功能真相时，证据优先级如下：

1. 旧图石运行界面和用户实际点击确认。
2. IDA 里的命令字符串、入口函数、Dialog、保存/恢复函数。
3. 6 个 `.sfl` 样本里的旧对象结构和字段痕迹。
4. AutoCAD 2020 图石旧插件包里的 `Detail.xml + DetailNN.stl` 字段。
5. 旧图石截图、旧 zip 里的字符串/窗口/架构分析。

父目录源码不属于“功能真相”的权威证据。

父目录源码只能进入下一节“工程实现参考”。
它可以帮我们少写样板代码，但不能决定旧图石业务字段、算法和验收口径。

## 父目录哪些可以参考

可以参考的内容：

- Qt6 / CMake 工程组织方式。
- Qt Widgets 主窗口、dock、action、状态栏组织方式。
- OCCT Viewer 集成方式。
- STEP/STP 导入和 XCAF 经验。
- face / edge 选择引用的技术思路。
- JSON 工程保存、schema 校验、失败回滚测试思路。
- Detail writer 的 XML 写法、包结构组织和单元测试思路。
- CTest、golden、失败注入测试的工程习惯。

这些内容属于“工程实现参考”。

## 父目录哪些不能采纳为路线

不能采纳的内容：

- 不能把父目录 README、DEVELOPMENT_PLAN 或源码结构当本项目权威路线。
- 不能把父目录 `LegacyUiCommandMap` 当旧图石完整按钮表。
- 不能把父目录钢筋模型当旧图石钢筋对象模型。
- 不能把父目录钢筋生成算法当旧图石算法。
- 不能把父目录静态测试或 golden 当旧图石动态 golden。
- 不能把父目录工程文件格式静默作为本项目新设计文件格式。
- 不能因为父目录已有 Detail writer，就关闭 AutoCAD 旧插件动态导入缺口。
- 不能因为父目录已有 STEP 选择引用，就关闭本项目 STEP 选择 ID 稳定性缺口。

一句话：

```text
父目录能减少工程从零搭建成本。
父目录不能替代旧图石逆向证据。
```

## 技术路线纠偏

本项目仍建议使用：

```text
Qt6 + OCCT + 旧图石证据驱动的钢筋复刻层 + Detail 工程图包 writer
```

但这里的“钢筋复刻层”不是自由自研。

正确含义是：

- 用旧图石按钮和 Dialog 定义命令入口。
- 用旧图石选择流程定义命令状态机。
- 用 IDA 和 SFL 定义对象结构、字段、阈值和几何规则。
- 用旧 Detail 包定义工程图字段和 ID 映射。
- 用用户运行确认补齐缺失行为。

错误含义是：

- 觉得 OCCT 有几何能力，就重新设计钢筋生成逻辑。
- 觉得父目录已经有钢筋对象，就直接拿来当旧图石复刻。
- 觉得能生成看起来像钢筋的模型，就认为 1 比 1 完成。

## 新设计文件格式边界

本项目仍然采用“新设计文件格式替代老 `.sfl` 主保存格式”的方向。

```text
老 .sfl
  -> 证据源
  -> 反推旧图石对象、字段、关系和保存行为

新设计文件格式
  -> 新系统主保存格式
  -> 承载复刻后的钢筋对象和工程图对象
```

边界是：

- 不直接复刻老 `.sfl` 二进制格式。
- 不把 ACIS BinaryFile 解析作为一期主线。
- 不把父目录已有工程格式当本项目默认格式。
- 不沿用父目录 `.tsrproj/.vtsproj` 作为本项目默认扩展名。
- 新格式字段要尽量贴旧图石对象结构，而不是为了 OCCT 重新设计一套业务模型。

开发期工作名暂用 `*.tsrebar`。

正式命名可以在 Phase 1 初始化前冻结，但字段和对象关系必须先由旧图石证据驱动，并用 OCCT / STEP 承载几何引用。

具体策略见 `18_新设计文件格式替代SFL策略.md`。

## 缺口关闭规则

涉及父目录源码时，缺口关闭必须满足：

```text
父目录源码存在
  + 本目录旧图石证据匹配
  + 本目录 Requirement / GAP 更新
  + 本目录验收方式明确
  = 可以降低风险或进入实现
```

不能这样关闭：

```text
父目录有实现
  = 本项目已完成
```

尤其以下缺口不能靠父目录单独关闭：

- 旧图石按钮、菜单、右键入口。
- 参数窗口字段、默认值、灰显状态。
- 面配筋、线配筋、弧形组、箍筋等钢筋生成规则。
- 组合开、组合并、段连接、移动、拷贝等编辑规则。
- 查询、碰撞、最小距离、未割钢筋等结果展示。
- 生成工程图、下料表、Detail 包 AutoCAD 动态导入。
- STEP face / edge 选择 ID 稳定性。
- 新设计文件格式字段契约。

## 开发时的使用方式

后续开发可以这样使用父目录：

1. 先从本目录文档拿 `Requirement ID / CommandId / GAP ID`。
2. 再从旧图石证据确认按钮、窗口、选择流程和输出。
3. 如果需要 Qt / OCCT / writer 代码样例，再看父目录。
4. 参考父目录实现时，只借工程结构和技术细节。
5. 业务字段、算法阈值、默认值必须回到旧图石证据校验。

推荐工作流：

```text
本目录需求证据
  -> 旧图石运行 / IDA / SFL / Detail 交叉确认
  -> 父目录找可参考实现
  -> 新项目按 1:1 契约编码
  -> 本目录验收基线关闭缺口
```

## 当前结论

父目录不是废料，有参考价值。

但父目录不是本项目的路线答案。

后续所有文档和开发都按以下硬边界执行：

```text
Qt6 / OCCT 替代旧商业底座。
旧图石证据决定钢筋业务。
父目录源码只作实现参考。
未被旧图石证据证明的内容，必须留在 GAP。
```
