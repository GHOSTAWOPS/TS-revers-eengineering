# 证据追溯与验收基线

## 目标

本文件规定后续每个功能如何记录证据、如何判断是否能进入开发、如何验收。

核心原则：

```text
没有证据的功能，不写成已确认。
没有验收口径的功能，不进入正式开发。
```

## 证据类型

### E-RUN：运行界面证据

来源：

- 用户打开旧图石后的截图。
- 点击按钮后的弹窗。
- 状态栏提示。
- 错误提示。
- 输出文件。

适用：

- 按钮文字。
- 参数窗口。
- 选择流程。
- 默认值和灰显。
- 运行结果。

### E-IDA：IDA / 二进制证据

来源：

- 命令字符串。
- 命令表。
- 入口函数。
- Dialog ID。
- 反编译流程。

适用：

- 命令入口。
- 可用性函数。
- 旧对象保存/读取函数。
- 字符串和提示语。

### E-SFL：SFL 样本证据

来源：

- 当前目录 6 个 `.sfl` 样本。
- 对象统计。
- 对象树截图。
- 字符串和字段扫描。

适用：

- 真实工程对象是否存在。
- 对象数量级。
- `steelbar` / `steelbargroup` / `seg_steelbargroup`。
- 剖切面 / 投影面。
- 图层 / 坝段 / 特征。

限制：

- SFL 不作为一期主输入格式。
- SFL 头部是 ACIS BinaryFile，不直接等于新系统保存格式。

### E-DETAIL：工程图包证据

来源：

- `Detail.xml`。
- `DetailNN.stl` XML。
- AutoCAD 2020 图石旧插件导入结果。

适用：

- 工程图 writer。
- 下料表。
- 材料表。
- 视口。
- 钢筋详图。

### E-DOC：旧文档 / 压缩包证据

来源：

- `reverse_engineering.zip` 内旧分析文件。
- 已有 md 文档。

适用：

- 旧架构。
- 字符串清单。
- Dialog 清单。
- 历史分析结论。

## 置信度等级

### C0：未确认

只有猜测或口头推断。

处理：

- 只能写入 `99_缺口和待确认项.md`。

### C1：单一证据

只有一种证据支持。

例子：

- 只有截图有按钮。
- 只有字符串有名称。

处理：

- 可以进入功能矩阵，但状态必须是 `部分确认` 或 `待运行确认`。

### C2：交叉证据

至少两种证据支持。

例子：

- 字符串 + Dialog。
- SFL 样本 + 运行截图。
- 命令表 + Dialog。

处理：

- 可以作为开发输入。
- 仍需记录缺口。

### C3：可验收证据

具备旧软件运行结果或输出文件，可用于对照新系统。

处理：

- 可作为 golden 基线。
- 可进入自动化或半自动验收。

## 功能进入开发的最低门槛

### UI 壳命令

最低门槛：

- 按钮文字。
- 页签和分组。
- 命令状态。
- 未实现提示。

证据等级：

- C1 即可。

### 参数窗口

最低门槛：

- Dialog ID 或运行截图。
- 字段清单。
- 默认值和单位，若未确认需标缺口。

证据等级：

- C2 优先。
- C1 可做占位，但不得宣称完整。

### 钢筋生成命令

最低门槛：

- 旧按钮入口。
- 参数窗口。
- 选择流程。
- 输出对象结构。
- dirty 标记。
- 验收样本。

证据等级：

- 至少 C2。

### 工程图 writer

最低门槛：

- Detail 包字段。
- AutoCAD 插件导入方式。
- 首期可导入包。
- 下料表和图上钢筋 ID 映射。
- 输出事务、失败回滚和 L0/L1/L2 门禁。

证据等级：

- C3 才能宣称闭环。

离线 writer 只能达到 C2。

只有 AutoCAD 2020 + 旧插件真实导入通过，才能升级为 C3。

## 运行截图收集规范

推荐命名：

```text
YYYYMMDD_页签_分组_按钮_步骤.png
```

例子：

```text
20260604_钢筋_主要钢筋_面配筋_属性窗口.png
20260604_工程图_输出_生成工程图_输出目录.png
```

每张截图旁边要记录：

- 打开的 SFL 或 STEP 文件。
- 当前选中的对象。
- 点击的按钮。
- 弹窗标题。
- 状态栏文字。
- 是否点击确定。
- 是否生成文件。

## 首批验收基线

### 基线 1：小样本面配筋

样本：

- `消力池下游侧带齿槽底板结构图石钢筋模型.sfl`

用途：

- 面配筋参数窗口。
- 对象树层级。
- 小规模钢筋显示。
- 首期工程图可验证包。

验收项：

- 能在旧图石看到 `面配筋_x`。
- 新系统能创建等价 `RebarGroup`。
- 参数字段能映射到 `RebarStyle` / `RebarGroup`。

### 基线 2：边墩复杂样本

样本：

- `边墩1107...替换后.sfl`

用途：

- 钢筋组数量较多。
- 剖切面和投影面都有。
- 工程图 writer 字段覆盖更全。

验收项：

- 对象数量统计可对齐。
- `HFSECTIONPLANE` / `HFPROJECTPLANE` 可映射到 `DrawingModel`。

### 基线 3：最大钢筋数量样本

样本：

- `尾水调压室闸墩结构图石钢筋模型_替换前.sfl`

用途：

- 性能基线。
- 显示和查询压力。
- 下料表规模测试。

验收项：

- 大量 `steelbar` 对象时选择、显示、查询不失控。

### 基线 4：工程图包样例

样本：

- `autocad2020/Detail.xml`
- `autocad2020/Detail01.stl`

用途：

- 旧插件工程图导入。
- 下料表字段。
- 视口字段。
- 钢筋详图字段。

验收项：

- 新系统生成的首期可验证包能被旧插件导入。
- `DetailNN.stl` 是 XML，不是网格 STL。
- writer 失败时不覆盖旧 Detail 包。
- `StbTable` 和 `StbGroups` 的 ID 能互相追溯。

## 追溯表模板

后续每个功能按此模板补：

```text
FeatureId:
旧按钮:
新 CommandId:
证据:
  - E-RUN:
  - E-IDA:
  - E-SFL:
  - E-DETAIL:
置信度:
输入:
输出:
验收:
缺口:
```

## 当前最高优先级追溯项

1. `面配筋`：已有 Dialog + 截图 + SFL，应补成首个完整功能追溯样板。
2. `线配筋`：已有字符串 + IDA 命令，缺选择流程和参数窗口对应。
3. `生成工程图`：已有按钮 + Detail 包，缺命令入口和动态导入验证。
4. `下料表`：已有 Dialog + Detail 字段，缺新模型映射规则。
5. `组合开`：按钮有运行截图证据但命令入口未闭合，是编辑链明显缺口。
