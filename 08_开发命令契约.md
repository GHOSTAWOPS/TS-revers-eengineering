# 开发命令契约

## 目标

本文件把旧图石按钮转换成新系统可实现的命令契约。

功能矩阵回答：

```text
旧软件有哪些按钮？
```

命令契约回答：

```text
每个按钮进入什么状态？
需要用户选择什么？
弹什么参数窗口？
改哪些对象？
产生什么 dirty 状态？
怎么验收？
```

## 命令统一结构

每个命令都按以下结构实现和验收：

```text
CommandId
  -> 所属页签 / 分组 / 按钮文字
  -> 触发条件
  -> 选择流程
  -> 参数窗口
  -> 输入对象
  -> 输出对象
  -> 影响的 dirty 标记
  -> 错误提示
  -> 撤销 / 重做
  -> 证据来源
  -> 验收方式
```

### M1-App-019 命令契约 P1 状态

`TODO-019 / M1-App-019` 已把首批旧钢筋命令落到正式 `app` 命令契约：

```text
Rebar.Create.LineGroup
  -> Ribbon
  -> legacyCommand = sgroupbarline
  -> implementationState = NotImplemented

Rebar.Create.ArcGroup
  -> Ribbon
  -> legacyCommand = sgroupbararc
  -> implementationState = NotImplemented

RebarGroup.TrimByLine
  -> ContextMenu
  -> legacyContextCommandId = 0x8939
  -> implementationState = NotImplemented

RebarGroup.TrimByFace
  -> ContextMenu
  -> legacyContextCommandId = 0x8CE5
  -> implementationState = NotImplemented
```

本阶段只证明命令入口、旧 UI 路径、旧命令字符串 / 右键命令号、输入选择类型、证据 / 缺口和状态机字段可追溯。

本阶段不证明线筋、弧筋、裁剪算法已经实现。

`ContextMenu` 命令只能注册到 `CommandRegistry`，不能被 Ribbon / Toolbar 构建逻辑渲染成顶部按钮。

## 命令状态机

首期命令状态建议统一为：

```text
Idle
  -> Armed
  -> Picking
  -> ParameterEditing
  -> Preview
  -> Applying
  -> Completed
```

异常路径：

```text
Picking / ParameterEditing / Preview
  -> Cancelled

Applying
  -> Failed
```

状态含义：

- `Idle`：没有命令运行。
- `Armed`：按钮已触发，命令准备进入选择或弹窗。
- `Picking`：等待用户选择面、线、点、钢筋组等。
- `ParameterEditing`：参数窗口打开。
- `Preview`：显示预览钢筋或剖切/投影结果。
- `Applying`：提交变更。
- `Completed`：对象已创建或修改。
- `Cancelled`：用户取消，不能留下半成品对象。
- `Failed`：执行失败，必须给出错误提示并回到安全状态。

## Dirty 标记

新系统至少需要以下 dirty 标记：

- `ProjectDirty`：工程文件需要保存。
- `GeometryDirty`：结构模型或参考对象改变。
- `RebarDirty`：钢筋对象、钢筋组、钢筋段改变。
- `DrawingDirty`：剖切、投影、工程图、下料表需要重新生成。
- `SelectionDirty`：当前选择集改变。
- `ViewDirty`：视图显示状态改变，但不一定需要保存。

首期规则：

- 创建、删除、移动、拷贝钢筋：`ProjectDirty + RebarDirty + DrawingDirty`。
- 修改钢筋属性：`ProjectDirty + RebarDirty + DrawingDirty`。
- 创建或修改剖切面/投影面：`ProjectDirty + DrawingDirty`。
- 查询、测量、碰撞检测：默认不改 `ProjectDirty`。
- 显示/隐藏、激活/钝化是否保存，需要运行确认；未确认前进入缺口。

## 开始页命令

### Project.New

旧入口：

- 开始 / 文件 / 新建。

输入：

- 工程模板或空工程。

输出：

- 新工程。
- 空模型树。
- 空钢筋集合。

验收：

- 当前工程未保存时提示保存。
- 新建后命令状态回到 `Idle`。

状态：

- 功能矩阵为待补，需继续补旧图石运行证据。

### Project.OpenTsRebar

旧入口：

- 开始 / 文件 / 打开。

首期口径：

- 打开新设计文件格式 `*.tsrebar`。
- 不把老 `.sfl` 当一期主保存格式。
- 打开时必须恢复 `legacyObject.raw + geometryRef + binding + evidence`。

输入：

- `*.tsrebar` 工程包。

输出：

- `ProjectDocumentState`。
- `RebarModelSnapshot`。
- `ValidationReport`。
- `BindingRecoveryReport`。

Dirty：

- 正常打开不 dirty。
- 打开后进入 binding repair preview 不 dirty。
- repair apply 后进入 `repairAppliedPendingSave`，需要保存。

验收：

- 正向 fixture 能进入 `OpenedFormal / OpenedWarning`。
- requiredFiles 缺失进入 `OpenBlocked`。
- binding 断裂进入 `OpenedRepairRequired` 或 `OpenBlocked`，不能静默改绑。
- 详细应用状态机见 `33_Qt6应用SaveOpen与Binding修复契约.md`。

### Project.OpenStep

旧入口：

- 开始 / 文件 / 打开。
- 开始 / 文件 / 转换模型。

首期口径：

- 优先打开 STEP/STP。
- 老 `.sfl` 不作为一期主输入。

输入：

- `.step` / `.stp`。

输出：

- `StructureModel`。
- `GeometryShape`。
- 模型树结构。

Dirty：

- 打开已有模型默认不 dirty。
- 导入到当前工程则 `ProjectDirty + GeometryDirty + DrawingDirty`。

验收：

- 能加载 6 个 SFL 样本引用的 STEP 路径对应模型。
- 能在 OCCT Viewer 中显示、全显、选择面边点。

### Project.Save

旧入口：

- 开始 / 文件 / 保存。

首期口径：

- 保存新设计文件格式，不保存老 `.sfl`。
- 开发期工作名暂用 `*.tsrebar` 工程包，正式扩展名 Phase 1 前冻结。
- 工程包必须同时保存旧图石证据驱动的 `legacyObject.raw`、OCCT / STEP 驱动的 `geometryRef`、二者之间的 `binding` 和 evidence。

输出：

- 新系统工程文件。
- 包含结构引用、参考对象、钢筋领域模型、工程图设置。

缺口：

- 新设计文件格式按 `18_新设计文件格式替代SFL策略.md`、`24_新设计文件格式Schema与Fixture草案.md`、`25_新设计文件格式SFL_OCCT联合开发任务拆解.md` 继续细化，字段和绑定关系必须能追溯旧图石证据与 OCCT 几何引用。
- Qt6 应用内保存、打开、回滚和 binding 修复的状态机见 `33_Qt6应用SaveOpen与Binding修复契约.md`。

### Rebar.Export.SteelStepOrLegacy

旧入口：

- 开始 / 文件 / 输出钢筋。
- IDA 命令候选：`sexportbar`、`ysteelout`。

首期口径：

- 用户截图确认 `输出钢筋` 位于 `开始 / 文件` 分组。
- 用户运行确认旧图石 `输出钢筋` 可导出钢筋 `.stp`。
- 新系统必须把“导出钢筋 STP”单独作为导出命令契约，不再只按文本或 SAT 占位描述。
- 导出的 STP 可作为后续旧图石 golden 样本，用于对比新系统生成钢筋的几何、数量、长度和分组。

输入：

- 当前工程钢筋集合。
- 选中钢筋组或全部钢筋，具体范围规则待运行确认。
- 导出路径和格式选项，具体窗口待运行确认。

输出：

- `.stp` / `.step` 钢筋导出文件。
- 若旧入口同时支持文本结构导出，则另保存旧文本导出样本。

Dirty：

- 只导出文件，不应修改 `ProjectDirty`。
- 是否写入最近导出路径或项目状态待运行确认。

验收：

- 旧图石至少导出 1 个小样本和 1 个复杂样本钢筋 STP。
- 导出文件能被 OCCT STEP reader 打开。
- 记录导出文件 hash、单位、roots、solids、faces、edges、曲线类型统计。
- 和同一 SFL 的对象树、Detail 包、钢筋统计建立 Evidence 关联。

缺口：

- `sexportbar` 与 `ysteelout` 哪个对应 STP 导出仍需 IDA 或运行确认。
- 导出窗口、文件名规则、选中范围、覆盖提示、错误提示未闭合。
- 旧 STP 是钢筋实体、中心线，还是混合表达，需要样本解析确认。

## 显示页命令

显示页首期分为三类：

- 相机命令：全显、实时缩放、实时平移、窗口缩放、旋转、轴测、垂直于。
- 渲染命令：线框、隐线不可见、隐线可见、渲染、上色。
- 剖视命令：X/Y/Z 向剖视、设置旋转。

### View.FitAll

旧入口：

- 显示 / 取景 / 全显。

输入：

- 当前视图。

输出：

- 相机范围更新。

Dirty：

- `ViewDirty`。
- 不应触发 `ProjectDirty`。

验收：

- 大模型和小模型均能回到完整可见状态。

### View.SectionX / View.SectionY / View.SectionZ

旧入口：

- 显示 / 切平面 / X 向剖视。
- 显示 / 切平面 / Y 向剖视。
- 显示 / 切平面 / Z 向剖视。

输入：

- 当前模型包围盒。
- 剖视方向。

输出：

- 临时显示剖视状态。

Dirty：

- 未确认。
- 暂按 `ViewDirty`，不写入工程图剖切面。

缺口：

- 需要运行确认显示页剖视和工程图页剖切面是否共用对象。

## 钢筋创建命令

### Rebar.Create.Face

旧入口：

- 钢筋 / 主要钢筋 / 面配筋。

旧证据：

- Dialog #265 / #266 / #268。
- SFL 小样本对象树出现 `面配筋_x`。
- 用户截图确认 `面配筋属性` 包含结构信息、钢筋信息。

选择流程：

1. 选择配筋面。
2. 选择方向或方向线。
3. 设置范围起点、终点、参考面。
4. 设置引导线。
5. 编辑钢筋信息。
6. 预览。
7. 确定。

参数窗口：

- 结构信息：配筋面、方向、范围、参考面、引导线、自动连接、快速配筋、智能分区、显示钢筋。
- 钢筋信息：式样、类型、直径、间距、保护层、内外层、起终点距离、首尾端、弯钩、首末增减根数。

输入对象：

- `StructureFace`。
- `RebarStyle`。
- `RebarCreateParameters`。
- 可选 `ReferencePlane` / `GuideLine`。

输出对象：

- `RebarGroup`。
- 多个 `Rebar`。
- 多个 `RebarSegment`。

Dirty：

- `ProjectDirty + RebarDirty + DrawingDirty`。

撤销：

- 删除本次创建的 `RebarGroup` 和关联 `Rebar` / `RebarSegment`。

验收：

- 参数窗口字段和旧图石一致。
- 生成对象进入模型树配筋部位。
- 查询、下料、工程图能引用同一组对象。

缺口：

- 具体布筋边界、避让、自动连接、智能分区规则未闭合。

### Rebar.Create.LineGroup

旧入口：

- 钢筋 / 主要钢筋 / 线配筋。

旧证据：

- 旧命令 `sgroupbarline -> sub_1404DE720`。
- 功能矩阵确认按钮。
- TODO-071 / E-IDA-045 已复核：旧入口要求单选；选中对象通过 `sub_1405C6820 / sub_1405DA020`；内部子项至少 2 个；抽取奇数索引 child 到临时 `ENTITY_LIST`；计算 4 个端点距离候选；初始最小距离候选为 `10.0`；随后调用 `sub_1404D10C0(entityList,objA,objB,minDistance,selectedEndpointDistance,flag)`。

选择流程：

- IDA 静态入口契约已收口到 TODO-071；旧 UI 状态栏提示、选择对象业务名和运行点击流程仍待旧图石运行确认。

输入对象：

- `GuideLine` 或结构边线。
- `RebarStyle`。
- 布置参数。

输出对象：

- `RebarGroup`。
- `Rebar`。
- `RebarSegment`。

Dirty：

- `ProjectDirty + RebarDirty + DrawingDirty`。

缺口：

- `sub_1404DE720` 入口契约已深看并落到 `RebarGroupCreator` P0 raw/evidence。
- TODO-072 已完成命令 handler P0：`Rebar.Create.LineGroup` 现在通过 `RebarLineGroupCommandHandler` 接收 `LegacySelectionRef`，经 `LegacyRebarGeometryReader` 调用 `RebarGroupCreator`，并以事务方式追加到 `SteelData`；`edge-p0-surrogate` 只是 P0 选择代用，不等价旧 VisualTS 完整 `ENTITY_LIST` 选择对象语义。
- TODO-073 已完成 UI 可见反馈 / AIS 显示 P0：`Rebar.Create.LineGroup` 成功后由 `MainWindow` 调用 `RebarAisPresentationAdapter`，再交给 `OccViewerWidget::displayRebarPresentation` 显示新增 `SteelBarGroup`；失败命令不刷新显示。
- 下一步 TODO-074 只做线配筋参数窗口 P0，先把旧 UI 字段占位、默认值和 handler 参数接入做成可测链路；不声明完整旧线配筋算法、undo/dirty 或 golden。

### Rebar.Create.ArcGroup

旧入口：

- 钢筋 / 主要钢筋 / 扇形筋 或 同心圆，具体对应关系未完全闭合。

旧证据：

- 旧命令 `sgroupbararc -> sub_1404DE110`。
- 字符串含 `扇形筋`、`同心圆`。

缺口：

- 需要确认 `sgroupbararc` 是扇形筋、同心圆还是两者共用入口。

## 钢筋编辑命令

### Rebar.Edit.Move

旧入口：

- 钢筋 / 钢筋编辑 / 钢筋移动。

旧证据：

- 旧命令 `barmove -> sub_1404D5040`。

输入：

- 选择的 `RebarGroup` 或 `Rebar`。
- 移动方向或两点。
- 距离。

输出：

- 更新几何位置。

Dirty：

- `ProjectDirty + RebarDirty + DrawingDirty`。

验收：

- 移动后查询和工程图引用不丢失 ID。

### Rebar.Edit.Copy

旧入口：

- 钢筋 / 钢筋编辑 / 钢筋拷贝。

旧证据：

- Dialog #384。

输入：

- 选择的钢筋对象。
- 方向线、端点、距离、倍数、份数。

输出：

- 新的 `RebarGroup` 或同组新增对象，需运行确认。

Dirty：

- `ProjectDirty + RebarDirty + DrawingDirty`。

缺口：

- 拷贝后编号规则未确认。

### Rebar.Group.Merge

旧入口：

- 钢筋 / 钢筋编辑 / 组合并。

旧证据：

- Dialog #500。
- 右键命令 `0x891F`。

输入：

- 两个或多个 `RebarGroup`。

输出：

- 合并后的 `RebarGroup`。

Dirty：

- `ProjectDirty + RebarDirty + DrawingDirty`。

缺口：

- 合并后 groupID、编号、下料合并规则未确认。

### Rebar.Group.Split

旧入口：

- 钢筋 / 钢筋编辑 / 组合开。

状态：

- 待运行确认。

缺口：

- 按钮文字、Dialog、命令入口、输出对象均未闭合。

### RebarGroup.TrimByLine

旧入口：

- 钢筋 / 右键菜单 / 与线裁剪。

旧证据：

- 右键命令 `0x8939`。

输入：

- 选择的 `RebarGroup`。
- 裁剪用线或线对象。

输出：

- 待 IDA / 旧图石运行确认。

Dirty：

- 预期 `ProjectDirty + RebarDirty + DrawingDirty`，需运行确认。

当前状态：

- M1-App-019 已绑定为 `ContextMenu` 命令契约和 `NotImplemented` placeholder。
- 未实现裁剪业务算法。

### RebarGroup.TrimByFace

旧入口：

- 钢筋 / 右键菜单 / 与面裁剪。

旧证据：

- 右键命令 `0x8CE5`。

输入：

- 选择的 `RebarGroup`。
- 裁剪用面或面对象。

输出：

- 待 IDA / 旧图石运行确认。

Dirty：

- 预期 `ProjectDirty + RebarDirty + DrawingDirty`，需运行确认。

当前状态：

- M1-App-019 已绑定为 `ContextMenu` 命令契约和 `NotImplemented` placeholder。
- 未实现裁剪业务算法。

## 查询和检查命令

### Rebar.Query.ByNumber

旧入口：

- 查询 / 查询钢筋 / 按编号。

旧证据：

- Dialog #303。

输入：

- 钢筋编号。
- 查询范围：全部 / 当前组。

输出：

- 查询结果。
- 选择并高亮对应钢筋。
- 可选修改直径、类型、间距。

Dirty：

- 单纯查询不 dirty。
- 如果应用修改：`ProjectDirty + RebarDirty + DrawingDirty`。

验收：

- 查询结果和模型树对象一致。
- 修改后下料表和工程图同步。

### Rebar.Check.Overlap

旧入口：

- 查询 / 测量 / 碰撞检测。

旧证据：

- `checkoverlapsteel -> sub_14059BB80 -> Dialog #449`。

输入：

- 集合1。
- 集合2。

输出：

- 碰撞结果列表。
- 高亮冲突对象。

Dirty：

- 不改工程，默认不 dirty。

OCCT 用途：

- 几何相交检测。

复刻层用途：

- 决定哪些钢筋进入集合。
- 决定结果展示、过滤、定位。

### Rebar.Check.MinDistance

旧入口：

- 查询 / 测量 / 主筋。
- 查询 / 测量 / 箍筋。

旧证据：

- `checkmindststeel -> sub_14059BB00 -> Dialog #450`。
- `checkgmindststeel -> sub_14059BA80 -> Dialog #450`。

输入：

- 钢筋集合。
- 最小距离规则。

输出：

- 距离不足结果。

Dirty：

- 不改工程，默认不 dirty。

缺口：

- 主筋和箍筋分别使用哪条命令需要运行确认。

## 工程图命令

### Drawing.DefineSectionPlane

旧入口：

- 工程图 / 定义视图 / 割切面 或 剖切面。

旧证据：

- Dialog #316 剖切面位置和尺寸。
- Dialog #485 定义转折剖面。

输入：

- 剖切面位置。
- X/Y/Z、宽度、高度、中心点、半剖。
- 可选转折剖切线。

输出：

- `SectionPlane`。
- `DrawingDirty`。

缺口：

- 按钮真实文字和入口关系未确认。

### Drawing.DefineProjectPlane

旧入口：

- 工程图 / 定义视图 / 投影面。

旧证据：

- Dialog #269。
- SFL 样本存在 `HFPROJECTPLANE`。

输入：

- 剖切面。
- 投影面。
- 被剖切面。
- 投影面保护层。
- 标题备注。
- 剖面到投影面距离。

输出：

- `ProjectPlane`。

Dirty：

- `ProjectDirty + DrawingDirty`。

### Drawing.GeneratePackage

旧入口：

- 工程图 / 输出 / 生成工程图。

旧证据：

- Detail 工程图包。
- `E-IDA-042 / E-IDA-043`：`36124 / 0x8D1C -> psallc -> sub_140600AA0` 对齐到 Detail 包主写出链。
- Dialog #274 当前只能保守降级为 `Set2Dpage` 属性页，不能再直接写成“生成工程图设置窗口”。

输入：

- `SectionPlane` / `ProjectPlane` / `AxonView`。
- `RebarGroup`。
- 2D 设置 / 视图设置类参数（旧弹窗仍待继续补证）。

输出：

- `Detail.xml`。
- `DetailNN.stl` XML。
- writer audit 记录。

Dirty：

- 成功生成后可清除 `DrawingDirty`，但不清除 `ProjectDirty`。
- 生成失败不能清除 `DrawingDirty`。

验收：

- AutoCAD 2020 + 旧插件能导入。
- L0 / L1 / L2 writer 门禁见 `20_DetailWriter输出事务契约.md`。

### Drawing.GenerateBarSchedule

旧入口：

- 工程图 / 输出 / 下料表。

旧证据：

- Dialog #427。
- Detail 包 `StbTable` / `MaterialTable`。
- `E-IDA-019`：`sub_140605B20 -> sub_140602F90` 写出 `StbTable / MaterialTable`。
- `E-IDA-020 / E-IDA-043`：内部命令绑定纠偏为 `psexcel / 0x88F1 / 35057 / sub_140605B20`。
- `E-IDA-021 / E-IDA-042`：Ribbon 构造函数确认 `工程图 / 输出` 分组包含 `{36124, 35057}`；结合命令表纠偏后，`35057` 才是当前高置信下料表入口。

内部命令：

```text
psexcel -> 0x88F1 / 35057 -> sub_140605B20
```

注意：

- `psexcel` 内部绑定已确认。
- `工程图 / 输出` 分组到 `35057` 的下料表候选绑定已由命令表纠偏补强。
- 顶部 UI `下料表` caption、Dialog #427 和运行输出路径仍需运行确认。

输入：

- 定尺长度。
- 焊接长度。
- 接头相间距离。
- 当前钢筋领域模型。

输出：

- `StbTable`。
- `MaterialTable`。
- 下料表中间模型，供 `Drawing.GeneratePackage` 统一写包。

验收：

- 下料表和图上钢筋编号一致。
- 不能生成一套和 `StbGroups` 脱节的独立编号。

## 后续补全规则

每闭合一个命令，都要补齐：

- 旧按钮文字。
- 新 `CommandId`。
- Dialog ID。
- IDA 入口函数。
- 选择流程。
- 参数字段。
- 输入对象。
- 输出对象。
- dirty 标记。
- 撤销/重做策略。
- 验收方式。

缺任一项时，必须在 `99_缺口和待确认项.md` 留缺口，不写成已确认。
