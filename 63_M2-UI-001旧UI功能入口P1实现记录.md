# M2-UI-001 旧 UI 功能入口 P1 实现记录

## 结论

本轮完成 `TODO-027 / M2-UI`。

正式 `app` 已把 `17_一期按钮追溯与命令占位矩阵.md` 中的一期旧图石功能入口接入：

```text
CommandId
LegacyUiCommandMap
CommandRegistry placeholder
Qt6 Ribbon QAction
QAction traceability metadata
app --smoke metadata gate
```

本轮只做 UI 入口和命令契约，不实现新的钢筋创建、编辑、查询、统计或工程图业务算法。

## 新增证据

```text
E-DEV-049
  -> TODO-027 / M2-UI-001 旧 UI 功能入口 P1
  -> app/src/command/CommandId.h
  -> app/src/command/LegacyUiCommandMap.cpp
  -> app/src/app/MainWindow.cpp
  -> app/tests/unit/command_registry_tests.cpp
  -> docs/phase1/app_build_reports/m2_ui_001_run_001.md
```

## 新增能力

新增或补齐一期入口：

```text
开始:
  Project.New / Open / Save / Close
  Rebar.Export.SteelStepOrLegacy
  Edit.Undo / Redo / DeleteSelection
  Selection.PickMode
  Reference.CreatePlane / CreateLine

显示:
  View.FitAll / PanRealtime / ZoomRealtime / OrbitRealtime
  View.ZoomWindow / Iso / PerpendicularToSelection / Rotate
  View.Render.Wireframe
  Rebar.View.Activate / Deactivate

钢筋:
  RebarStyle.OpenManager
  Project.Settings / SetElevationDatum
  App.SystemSettings
  Rebar.Create.Face / LineGroup / ArcGroup / SectionRing
  Rebar.Create.FaceBoundary / ConcentricCircle / Angle / Custom / Generic
  Rebar.Create.FixedCenter / Stirrup / Corner / Opening / Insert / Spiral
  Rebar.Edit.Move / Copy / AddBarCount / AddSegment
  Rebar.Group.Merge / Split
  Rebar.Segment.Connect / Disconnect

查询:
  Rebar.Query.ByNumber / ByDiameter / ByPart
  Rebar.Check.Overlap / MinDistance / GroupMinDistance
  Rebar.Check.UncutOrUnsectioned / OutsideStructure
  Measure.Generic / StructureProperty

工程图:
  Drawing.DefineSectionPlane / DefineProjectPlane / DefineAxonometricProjection
  Drawing.GeneratePackage / GenerateBarSchedule
```

右键菜单仍保持 `ContextMenu`：

```text
RebarGroup.TrimByLine
RebarGroup.TrimByFace
```

它们不会渲染到 Ribbon。

## QAction 追溯元数据

`MainWindow::buildCommandTabs()` 现在给每个 Ribbon `QAction` 写入：

```text
commandKey
legacyUiPath
legacyUiSurface
implementationState
legacyCommand
legacyContextCommandId
evidenceIds
sourceRefs
gapIds
inputSelectionTypes
```

`tsrebar_app --smoke` 会调用 `verifyLegacyUiActionMetadata()` 校验这些字段，并确认 `ContextMenu` 命令没有出现在 Ribbon。

## TDD

RED：

```text
新增 testLegacyUiCommandCoverageP1 后，CommandId 中缺少 EditUndo / ReferenceCreatePlane 等枚举，编译失败。
```

GREEN：

```text
补齐 CommandId、LegacyUiCommandMap 和 QAction metadata 后：
command_registry_tests = pass
tsrebar_app --smoke = pass
```

后续两轮 xhigh 指出覆盖不足，本轮继续补测试直到覆盖 `17` 的一期入口。

## IDA MCP 使用情况

本轮打开 IDA MCP：

```text
database = visualts_i64_todo027
input = VisualTS.exe.i64
```

中文字符串批量查询未直接命中，未形成新的 IDA Evidence ID。

处理口径：

```text
有既有 IDA 证据的入口继续挂 E-IDA-*。
没有 IDA/运行闭合的入口只作为 NotImplemented / GAP 占位。
不把缺证入口写成业务已确认。
```

## 验证结果

窄测：

```text
command_registry_tests = pass
tsrebar_app --smoke = pass
```

默认 CTest：

```text
13 / 13 passed
Total Test time = 125.40 sec
```

Readiness gate strict：

```text
decision = M1-Formal-Ready
78 / 78 pass
0 error
0 warning
```

OCCT / AIS 泄漏扫描：

```text
rg -n "TopoDS_|AIS_|BRep|TopAbs_" app/src/domain/rebar app/src/drawing app/src/project
result = no matches
```

`git diff --check`：

```text
pass
```

## xhigh 只读 review

本轮代码节点执行了三轮 xhigh 只读 review，子代理只审查、不修改。

第一轮：

```text
Verdict = needs_fix
Critical:
  QAction metadata smoke 覆盖不足。
Important:
  ExportRebarStep 缺 E-IDA-014。
  Undo / Redo 缺按钮级 GAP。
  显示路径从旧“取景”改成“视图”，需恢复或说明。
```

处理：

```text
补 QAction metadata 完整校验。
补 ContextMenu 不渲染到 Ribbon 检查。
补 E-IDA-014 / E-DEV-048。
补 GAP-BTN-START-005 / 006。
恢复旧“显示/取景”路径。
```

第二轮：

```text
Verdict = needs_fix
Important:
  对照 17 矩阵，仍缺 23 个一期入口。
  Project.New/Open/Save/Close 和 Drawing.DefineSectionPlane 追溯不完整。
```

处理：

```text
补齐 17 矩阵一期入口。
补 Project.* 按钮级 GAP。
补 Drawing.DefineSectionPlane 的 E-SFL-002 / GAP-FUNC-005。
```

第三轮：

```text
Verdict = allow_commit
Critical = none
Important = none
Minor = none
```

三个子代理完成后均已关闭，减少代理负担。

## 边界

本轮没有：

```text
实现新的钢筋创建业务算法。
实现钢筋编辑、统计、检查或工程图输出业务。
迁入父目录 rebar 业务。
让 domain/rebar、drawing、project 依赖 OCCT / AIS。
关闭剖面圈筋、固中心、组合开、未割钢筋等运行确认 GAP。
采集 golden。
```

## 下一步

本轮完成后只把 `todo.csv` 的下一项标为 `next`，不继续实现。

建议下一阶段：

```text
TODO-028 / CSE readiness gate 扩展
```

原因：

```text
当前 UI 入口覆盖已经扩大。
下一步应把路线护栏自动化，防止后续再次偏到父目录 OCCT 直接重写钢筋路线。
```
