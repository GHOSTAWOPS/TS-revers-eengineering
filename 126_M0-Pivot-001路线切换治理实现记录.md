# TODO-089 / M0-Pivot-001 路线切换治理实现记录

## 目标

把正式仓库的总路线事实源从旧 VisualTS 全量 1:1 复刻，调整为：

```text
STEP-only + RebarSmart 钢筋生成逻辑证据 + 图石 Detail 包兼容导出
```

## 完成内容

- 新增正式 ADR：`docs/adr/ADR-20260611-step-only-rebarsmart-detail.md`。
- 新增正式 roadmap：`docs/roadmap/STEP_only_RebarSmart_Detail_refactor_plan.md`。
- 更新 `AGENTS.md`：RebarSmart / VisualTS / 3DE / ACIS / HOOPS / Codejock 不得进入运行时。
- 更新 `README.md`：项目定位切到 STEP-only + RebarSmart evidence + Detail compatibility。
- 更新 `00_总览.md`、`06_技术路线与替代方案.md`、`07_1比1复刻实施路线.md`：旧 VisualTS 1:1 路线降级为历史证据。
- 更新 `11_需求证据追溯矩阵.md`：新增 `E-ADR-001 / E-ROADMAP-001`。
- 更新 `34_Phase1ReadinessGate实际运行记录.md`：记录 gate 在新路线下的保留价值和后续升级方向。
- 更新 `46_CSE_v2Goal执行目标与Todo说明.md`：长期 goal 和下一轮短期 goal 改为 `TODO-090 / DetailPackage 数据模型 P0`。
- 更新 `99_缺口和待确认项.md`：登记新路线缺口。
- 更新 `todo.csv`：`TODO-088` 降为 pending，新增 `TODO-089 done`、`TODO-090 next`、`TODO-091..099 pending`。

## 边界

本节点只做路线治理和文档事实源切换。

未做：

- 未修改 app 代码。
- 未迁入 RebarSmart 二进制或资源包。
- 未调用 VisualTS、3DE、ACIS、HOOPS、Codejock。
- 未删除旧文档。
- 未开始 DetailPackage 数据模型实现。

## 结论

本节点完成后，后续 goal 模式应从 `TODO-090 / M1-Detail-001 DetailPackage 数据模型 P0` 开始。

旧 VisualTS UI / 操作流程全量 1:1 不再作为当前 P0/P1 主线。
