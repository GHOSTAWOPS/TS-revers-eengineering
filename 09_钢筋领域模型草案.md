# 钢筋领域模型草案

## 目标

本文件定义新系统首期需要的领域对象骨架。

它不是最终数据库 schema，也不是老 `.sfl` 字段全量复刻。它的作用是让后续 Qt6 + OCCT 开发有稳定对象边界。

## 领域分层

```text
ProjectDocument
├── StructureModel
├── ReferenceObjects
├── RebarModel
├── DrawingModel
├── StyleCatalog
└── Settings
```

### ProjectDocument

表示一个新系统工程。

首批字段：

- `projectId`
- `filePath`
- `sourceModelPath`
- `sourceModelFingerprint`
- `units`
- `createdAt`
- `modifiedAt`
- `dirtyFlags`

约束：

- 首期保存为新设计文件格式。
- 不保存为老 `.sfl`。
- 可记录原始 STEP/STP 路径。

### StructureModel

表示结构模型。

首批字段：

- `modelId`
- `name`
- `sourcePath`
- `occtShapeRef`
- `bbox`
- `faces`
- `edges`
- `vertices`
- `layers`

旧证据：

- SFL 样本中存在 `HEFACE`、`HEEDGE`、`HEVERTEX`、`__layer`。
- 样本引用 STEP/STP 文件路径。

约束：

- OCCT shape 是几何实现细节，不能直接替代领域对象 ID。
- 面、边、点需要稳定选择 ID，否则钢筋参数无法可靠保存。

缺口：

- STEP 重新导入后面/边 ID 如何稳定，需要开发时专项验证。

### ReferenceObjects

表示参考面、参考线、参考体。

首批对象：

- `ReferencePlane`
- `ReferenceLine`
- `ReferenceBody`

首批字段：

- `referenceId`
- `type`
- `name`
- `geometry`
- `sourceSelection`
- `visible`
- `locked`

旧证据：

- 功能矩阵包含参考面、参考线、参考体。
- Dialog #317 参考线定义存在。
- 钢筋配筋结构信息包含参考面、引导线。

## RebarModel

### RebarModel 根对象

首批字段：

- `groups`
- `bars`
- `styles`
- `steelData`
- `selectionSets`

约束：

- `RebarGroup` 是编辑、查询、工程图、下料的主要单位。
- `Rebar` 是具体一根钢筋。
- `RebarSegment` 是一根钢筋内的直线段、圆弧段或点状表达。

旧证据：

- SFL 样本统计包含 `steelbar`、`steelbargroup`、`seg_steelbargroup`、`steelData`、`SteelStyle`。
- Detail 包包含 `StbGroup`、`Std`、`StbGeo`、`StbTable`。

### Rebar 对象三类锚点

首期领域对象不能只保存新系统字段。

每个核心钢筋对象都要有三类锚点：

```text
LegacyAnchor
  -> legacyObject.raw
  -> 来自 SFL / IDA / Detail 的旧对象类型、字段顺序、编号和证据

GeometryAnchor
  -> geometryRef
  -> 来自 OCCT / STEP 的 face / edge / curve / fingerprint

BindingAnchor
  -> binding
  -> 说明旧业务对象与新几何引用如何对应
```

落到对象上：

- `RebarGroup` 必须能追回旧 `steelbargroup`，并绑定来源面、线或参考对象。
- `Rebar` 必须能追回旧 `steelbar`，并绑定它使用的段或曲线集合。
- `RebarSegment` 必须能追回旧 `seg_steelbargroup`，并绑定 OCCT 曲线、边或派生曲线。
- `SteelData` 没有直接几何引用时，可以没有 `geometryRef`，但仍必须有 `legacyObject.raw` 和 evidence。

绑定状态：

- `resolved`：旧对象和几何引用都可恢复。
- `unresolvedGeometry`：旧对象可恢复，但 STEP 缺失或拓扑匹配失败。
- `pendingLegacyEvidence`：几何引用可恢复，但旧字段语义还没闭合。

开发约束：

- 纯 OCCT 对象不能直接进入正式 `RebarModel`。
- 纯旧字段 raw 也不能在正式保存验收里假装几何已恢复。
- 绑定失败时对象必须进入修复或只读状态，不能静默挂到相似面/边。

### M1-App-018 P1 冻结状态

`TODO-018 / M1-App-018` 已把首批钢筋领域对象落到正式 `app/src/domain/rebar`：

```text
RebarDomainTypes.h
SteelData.h
SteelBarGroup.h
SteelBar.h
SteelBarSegment.h
```

当前 P1 代码边界：

- `SteelData` 承载 `steelDataId / level / gradeName / diameterSet / defaultHook / defaultBend`，并携带 `legacyRaw / binding / unresolvedLegacyFields / evidence`。
- `SteelBarGroup` 承载 `groupId / rsdId / displayNumber / actualNumber / componentName / projectSteelName / createCommand / steelDataId / diameter / interval / barCount / segmentCount / steelWay / rebarType / offsetInOut` 等 Detail 和旧组字段。
- `SteelBar` 承载 `barId / groupId / sequenceNo / displayNumber / diameter / steelLevel / segmentIds / length / shapeType`。
- `SteelBarSegment` 承载 `segmentId / barId / sequenceNo / shapeType / startPoint / endPoint / middlePoint / startRadius / endRadius / offset / length`，并提供 `detailShapeTypeCode()` 映射 `L / A / C`。
- `RebarDomainTypes` 承载 `DomainPoint3d / LegacyRawBlock / GeometryReference / BindingAnchor / RebarEvidenceRef / UnresolvedLegacyField` 等纯领域 DTO。

P1 约束：

- `domain/rebar` 不依赖 `TopoDS_ / AIS_ / BRep* / TopAbs_`。
- `legacyObject.raw + geometryRef + binding + evidence` 必须显式存在，不能只保存新字段。
- 低置信字段必须进入 `unresolvedLegacyFields`，不能写成已确认业务语义。
- `id / legacyCommand / segmentType / lengthMm` 等早期薄骨架字段暂时保留为兼容 alias；后续业务真相以 `groupId / barId / segmentId / sequenceNo / length` 等 P1 字段为准。

本冻结只说明字段已经可编码，不说明旧图石钢筋创建、编辑、统计或出图算法已经复刻。

### SteelData

用途：

- 记录工程级钢筋材料或类型数据。

已知证据：

- SFL 样本存在 `steelData`。
- 数量通常与 `SteelStyle` 数量接近，但小样本为 0。

首批字段：

- `steelDataId`
- `level`
- `gradeName`
- `diameterSet`
- `defaultHook`
- `defaultBend`

置信度：

- 字段名是新系统草案，不是已确认旧字段名。

缺口：

- `steelData` 真实保存字段和业务名未闭合。

### RebarStyle

对应旧软件钢筋式样。

旧证据：

- Dialog #292 / #293。
- Detail.xml `StyleRoot`。

首批字段：

- `styleId`
- `name`
- `diameter`
- `steelType`
- `jointType`
- `coverDepth`
- `startCoverDepth`
- `endCoverDepth`
- `startAnchor`
- `endAnchor`
- `startHook`
- `endHook`
- `extendStart`
- `extendEnd`
- `addNumStart`
- `addNumEnd`

Detail 对应字段：

- `Name`
- `dia`
- `type`
- `joint_type`
- `depth`
- `depth_start`
- `depth_end`
- `m_Anchor1`
- `m_Anchor2`
- `extend1`
- `extend2`
- `m_AddNum_start`
- `m_AddNum_end`

### RebarGroup

用途：

- 表示一组由同一规则生成、一起编辑、一起统计的钢筋。

首批字段：

- `groupId`
- `rsdId`
- `name`
- `componentName`
- `createCommand`
- `styleId`
- `diameter`
- `interval`
- `barCount`
- `segmentCount`
- `steelLevel`
- `layer`
- `profile`
- `use`
- `active`
- `visible`
- `sourceFaceId`
- `sourceLineId`
- `referenceIds`
- `barIds`
- `createdFromParameters`

旧证据：

- SFL 样本 `steelbargroup` 数量。
- Detail `StbGroup` 字段含 `rsdID`、`groupID`、`diameter`、`interval`、`barcount`、`segcount`、`stbLevel`、`stbLayer`、`stbProfile`、`stbUse`、`ComponentName`、`SteelWay`。

状态：

- `active / inactive` 对应旧图石激活/钝化。
- `visible / hidden` 对应显示/隐藏。
- `selected / unselected` 只属于运行时选择状态，不建议保存进工程。

缺口：

- `rsdID` 和 `groupID` 的生成规则未确认。
- `stbLayer`、`stbProfile`、`stbUse` 的业务枚举未确认。

### Rebar

用途：

- 表示一根钢筋。

首批字段：

- `barId`
- `groupId`
- `sequenceNo`
- `displayNo`
- `diameter`
- `steelLevel`
- `segments`
- `length`
- `shapeType`
- `active`
- `visible`

旧证据：

- SFL 样本 `steelbar` 数量。
- Detail `Std` 下存在多个 `StbGeo`。

缺口：

- 旧软件中 `steelbar` 和 `Std` 是否一一对应未确认。

### RebarSegment

用途：

- 表示钢筋的一段几何。

首批字段：

- `segmentId`
- `barId`
- `segSeqNo`
- `shapeType`
- `startPoint`
- `endPoint`
- `middlePoint`
- `startRadius`
- `endRadius`
- `offset`
- `length`

Detail 对应字段：

- `segID`
- `stbSeqNum`
- `shapeType`
- `start_x/y/z`
- `end_x/y/z`
- `middle_x/y/z`
- `start_r`
- `end_r`
- `offset_x/y/z`

形状类型：

- `L`：线段。
- `A`：圆弧。
- `C`：点筋 / 点状钢筋表达。

缺口：

- `C` 的完整语义未确认。

## DrawingModel

### SectionPlane

旧证据：

- SFL 样本 `HFSECTIONPLANE`。
- Dialog #316。
- Dialog #485。

首批字段：

- `sectionPlaneId`
- `name`
- `origin`
- `normal`
- `topDirection`
- `width`
- `height`
- `halfSection`
- `breakLine`

### ProjectPlane

旧证据：

- SFL 样本 `HFPROJECTPLANE`。
- Dialog #269。

首批字段：

- `projectPlaneId`
- `sectionPlaneId`
- `origin`
- `normal`
- `title`
- `coverDepth`
- `distanceToSection`
- `cutFaceIds`

### DrawingPackage

对应旧 AutoCAD 插件导入包。

首批字段：

- `styleRoot`
- `drawingRoots`
- `viewPorts`
- `stbTables`
- `materialTable`
- `stbGroups`

约束：

- `DetailNN.stl` 必须写 XML。
- 下料表和图上钢筋必须共用 `RebarGroup` / `Rebar` / `RebarSegment`。
- 输出事务、临时目录、覆盖、回滚和 audit 见 `20_DetailWriter输出事务契约.md`。

## ID 和映射规则

首期必须建立以下映射：

```text
RebarGroup.groupId
  -> Detail.StbGroup.groupID
  -> Detail.StbTable.rsdID / group 相关字段

Rebar.barId
  -> 查询结果
  -> 高亮定位

RebarSegment.segmentId
  -> Detail.StbGeo.segID
```

约束：

- 内部 ID 必须稳定。
- 显示编号可以变化，但内部 ID 不应因移动、拷贝、工程图生成而丢失。
- 删除对象后，是否复用编号需按旧图石运行确认。

缺口：

- 旧图石编号空号/加撇规则见 Dialog #426，但具体算法未闭合。

## 保存格式约束

新设计文件格式替代 `.sfl` 的策略见 `18_新设计文件格式替代SFL策略.md`。

当前 `18` 已补入首批字段契约：

- `steelData -> RebarSteelData`
- `steelbar -> Rebar`
- `steelbargroup -> RebarGroup`
- `seg_steelbargroup -> RebarSegmentGroup / SegmentCurveRule`
- `Detail StbGroup / StbGeo -> Drawing Mapping`

这些字段分两类保存：

```text
legacyObject
  -> 旧图石 SFL / IDA / Detail 证据驱动的业务字段
  -> 首期包含 legacyObject.raw，按旧 save/read 顺序保真

geometryRef
  -> OCCT / STEP / BRep 驱动的几何引用和拓扑选择 ID
  -> 承载 face / edge / curve / fingerprint，不替代旧业务编号

binding
  -> legacyObject.raw 与 geometryRef 的绑定关系
  -> 说明旧对象依附哪个 face / edge / curve，以及失败时如何处理
```

新格式工作名：

```text
*.tsrebar
```

这只是开发期称呼，不是旧图石证据。
正式扩展名在 Phase 1 初始化前冻结。
不沿用父目录 `.tsrproj/.vtsproj`。

首期新设计文件格式至少要保存：

- 工程元信息。
- STEP/STP 源文件引用和指纹。
- `manifest / project / geometry / rebar / drawing / evidence` 包结构。
- 参考对象。
- 钢筋样式。
- 钢筋组。
- 钢筋。
- 钢筋段。
- `legacyObject.raw` 和 `geometryRef` 的绑定关系。
- `binding.state`、`binding.items` 和绑定证据。
- 字段证据 ID 和 `pendingLegacyEvidence` 状态。
- 剖切面。
- 投影面。
- 出图设置。
- 文件设置。

不保存：

- 老 ACIS BinaryFile。
- HOOPS 显示状态私有数据。
- AutoCAD DWG。

## 待验证清单

- STEP 重新导入后选择 ID 的稳定性。
- `steelData` 真实字段。
- `steelbargroup` 大字段表业务含义。
- `RebarGroup` 和 `StbGroup` 的一一对应关系。
- `steelbar` 和 `Std` / `StbGeo` 的对应关系。
- 激活/钝化是否需要保存。
- 显示/隐藏是否需要保存。
- 编号空号/加撇规则。
- 拷贝、组合并、组合开后的 ID 和编号规则。
