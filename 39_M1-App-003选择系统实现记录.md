# M1-App-003 选择系统实现记录

## 目标

本文件记录 M1-App-003 的实现边界和实际结果。

目标是：

```text
在正式 app 中实现 face / edge / vertex 选择底座。
把 OCCT STEP 子形状映射为稳定 LegacySelectionRef。
把选择模式和选择摘要接入 Qt6 UI。
继续禁止钢筋业务层直接依赖 OCCT。
```

本阶段仍然不是钢筋业务复刻。

## 路线边界

本轮复用父目录的是：

- `selection-v1` 稳定引用协议。
- XCAF label entry 作为 part 稳定身份的思路。
- `type + index` 的子形状引用方式。
- `AIS_Shape::SelectionMode(shapeType)` 的 viewer 选择模式用法。

本轮没有迁移父目录的钢筋生成主链：

```text
RebarCreationCommandService
RebarCreationCommandSession
ProjectRebarMutationService
EdgeToRebarFactory
FaceRebarGenerator
PolylineRebarGenerator
RebarFaceOffsetService
RebarLocalMoveService
```

原因：这些属于父目录“OCCT 直接重写钢筋”的路线，不能作为旧 VisualTS 业务真相。

## 新增模块

```text
app/src/geometry/legacy/LegacySelectionRef.h
app/src/geometry/legacy/LegacySelectionRef.cpp
app/src/geometry/occ/selection/OccSelectionIndex.h
app/src/geometry/occ/selection/OccSelectionIndex.cpp
```

职责：

- `LegacySelectionRef`：纯 legacy 选择引用，不包含 `TopoDS_` / `AIS_` / `BRep*`。
- `OccSelectionIndex`：在 OCCT 层为导入 STEP 建立 face / edge / vertex 索引。
- `OccViewerWidget`：显示、选择模式、按 stableId 恢复当前选择和属性摘要。
- `MainWindow`：新增 `选面 / 选边 / 选点` 命令，属性 dock 显示选择摘要。

## 稳定引用格式

当前格式：

```text
ref=selection-v1;
entry=<XCAF entry>;
synthetic=<0|1>;
syntheticOrdinal=<n>;
type=<TopAbs enum>;
index=<1-based subshape index>
```

示例：

```text
ref=selection-v1;entry=0:1:1:1/0:1:1:1:1;synthetic=0;syntheticOrdinal=0;type=4;index=1
```

约束：

- 不允许出现瞬态 `part=`。
- 非 synthetic part 的 `syntheticOrdinal` 必须为 `0`。
- face / edge / vertex 使用旧 TopAbs 数值：`Face=4`、`Edge=6`、`Vertex=7`。
- 当前 `123.stp` 的可选择唯一子形状统计为：
  - faces = 3016
  - edges = 4524
  - vertices = 3016

注意：`StepImportResult` 的 raw edges / vertices 仍保留原始遍历统计：

```text
raw edges = 9048
raw vertices = 18096
```

选择索引使用 `TopTools_IndexedMapOfShape` 去重后数量，这是和前期 STEP selection gate 对齐的口径。

## 实现内容

完成项：

- `LegacySelectionRef` 编码 / 解析 / 拒绝 transient part id。
- `OccSelectionIndex::fromDocument()`。
- `OccSelectionIndex::refs(Face/Edge/Vertex)`。
- `OccSelectionIndex::resolve()` / `resolveStableId()`。
- `OccSelectionIndex::refForShape()`，供 viewer 鼠标命中后反查 stable ref。
- `ViewSelectFace / ViewSelectEdge / ViewSelectVertex` 命令。
- `OccViewerWidget::selectByStableId()`。
- `currentSelectionStableId()` / `currentSelectionSummary()` / `currentSelectionDetails()`。
- 属性 dock 显示当前选择 stableId。
- viewer selection 测试覆盖真实 `123.stp` 显示和 stableId 恢复。

## 验证结果

构建：

```text
success
```

CTest：

```text
7 / 7 passed
```

通过项：

- `command_registry_tests`
- `legacy_selection_ref_tests`
- `app_smoke`
- `viewer_smoke_tests`
- `domain_rebar_boundary`
- `step_import_probe_123`
- `occ_selection_index_123`

真实 Viewer 显示 smoke：

```powershell
build\tsrebar_app.exe --smoke-display-step "..\..\123.stp"
```

结果：

```text
exit code = 0
```

真实 Viewer 选择 smoke：

```powershell
build\viewer_selection_tests.exe "..\..\123.stp"
```

结果：

```text
exit code = 0
```

说明：

- 该命令会短暂打开 `viewer_selection_tests` 窗口。
- `123.stp` 较大，测试主线程导入/显示时 Windows 可能显示空白或“未响应”。
- 为避免干扰日常开发，它不再放进默认 `ctest`。

## 测试平台说明

`viewer_selection_tests.exe` 需要真实 OpenGL 窗口平台。

原因：

```text
QT_QPA_PLATFORM=minimal 下 OpenGl_Window::CreateWindow 会 ChoosePixelFormat failed。
```

所以：

- `app_smoke` / `viewer_smoke_tests` 继续使用 `QT_QPA_PLATFORM=minimal`。
- `viewer_selection_tests.exe` 使用 Windows 正常窗口平台，只设置 `QT_QPA_PLATFORM_PLUGIN_PATH`。
- 默认 `ctest` 不运行该测试，避免反复弹出空白临时窗口。

## 当前未完成

- 鼠标点选后的 AIS 高亮同步还未做完整验收。
- 多选 / Shift / Ctrl 切换规则还未实现。
- 选择摘要还只是 stableId + 简单类型；EDGE / FACE 几何摘要已由 M1-App-004 P0 提供，尚未接入属性面板展示。
- `LegacyGeometryAdapter` 已完成 P0；bbox、采样、fingerprint、boundary wires 等仍在 P1。
- 钢筋生成仍未开始。

## 下一步

```text
M1-App-004：LegacyGeometryAdapter P0。

EDGE:
  length / type / endpoints / sample / fingerprint

FACE:
  type / normal / area / boundaries / fingerprint
```

这个阶段才开始给旧 VisualTS 钢筋逻辑提供“像老 ACIS/HOOPS 那样的几何能力”，但仍然不直接让 OCCT 写钢筋业务。

状态：已完成，见 `40_M1-App-004LegacyGeometryAdapterP0实现记录.md`。
