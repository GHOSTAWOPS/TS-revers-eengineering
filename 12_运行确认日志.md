# 运行确认日志

## 目标

本文件记录旧图石软件运行确认过程。

只要用户能打开旧图石并点击功能，就把结果记到这里。后续开发不能只说“看过截图”，必须能反查：

- 哪个版本的旧软件。
- 打开的哪个样本。
- 点了哪个按钮。
- 弹了什么窗口。
- 状态栏说了什么。
- 生成了什么文件。
- 对应哪个 Evidence ID / GAP ID。

## Run ID 规则

```text
RUN-YYYYMMDD-序号
```

例子：

```text
RUN-20260604-001
```

## 记录模板

```text
Run ID:
日期:
旧软件版本:
输入文件:
操作步骤:
观察结果:
截图/视频路径:
输出文件:
关联 Evidence ID:
关联 Requirement ID:
关联 GAP ID:
结论:
下一步:
```

## 已记录运行确认

### RUN-20260604-001：小样本打开和面配筋属性

日期：

- 2026-06-04

旧软件版本：

- 用户本机旧图石软件，具体版本号待补。

输入文件：

- `消力池下游侧带齿槽底板结构图石钢筋模型.sfl`

操作步骤：

1. 打开旧图石软件。
2. 打开小样本 SFL。
3. 查看对象树。
4. 打开 `面配筋属性`。

观察结果：

- 对象树包含 `Root`、STEP 引用、参考特征、剖面、多个配筋部位。
- 层级中确认 `配筋部位 -> 面配筋_x`。
- `面配筋属性` 有两个页签：结构信息、钢筋信息。
- 结构信息包含配筋面、方向、范围、参考面、引导线、自动连接、快速配筋、智能分区、显示钢筋。
- 钢筋信息包含式样、类型、直径、间距、保护层、内外层、起终点距离、首尾端、弯钩、首末增减根数等字段。

截图/视频路径：

- 待用户或后续整理补充具体截图路径。

输出文件：

- 无。

关联 Evidence ID：

- E-RUN-001
- E-SFL-001
- E-DLG-265
- E-DLG-266
- E-DLG-268

关联 Requirement ID：

- REQ-REB-C-001

关联 GAP ID：

- GAP-DEV-005

结论：

- 面配筋可以作为第一批专项复刻样板。

下一步：

- 补截图路径。
- 补旧软件版本号。
- 补点击“确定/应用/取消”后的 dirty 和对象变化。

### RUN-20260605-001：开始页文件分组和输出钢筋 STP

日期：

- 2026-06-05

旧软件版本：

- 用户本机旧图石软件，具体版本号待补。

输入文件：

- `消力池下游侧带齿槽底板结构图石钢筋模型.sfl`

操作步骤：

1. 打开旧图石软件。
2. 查看顶部 Ribbon。
3. 切到 `开始` 页签。
4. 查看 `文件` 分组。
5. 点击或确认 `输出钢筋`。
6. 导出格式选择或确认 `.stp`。

观察结果：

- 用户截图确认 `输出结构`、`输出钢筋` 位于 `开始 / 文件` 分组。
- 用户运行确认旧图石 `输出钢筋` 可以导出钢筋 `.stp`。

截图/视频路径：

- 用户已在对话中提供截图，需后续整理为本地 evidence 文件。

输出文件：

- `C:\Users\ghost\Desktop\reverse_engineering\【03】图石软件\123.stp`
- 文件大小：87,746,388 bytes。
- SHA256：`6a3c4f2044c2cc1f1123f0f58c61b2c869fbad3110a3585fdab49e69daf52a2a`。
- STEP 头部：`FILE_NAME('123.stp','2026-06-05T02:02:12',...,'Spatial InterOp 3D',...)`。
- OCCT 单次导入：read ok、transfer ok、1 root、1 free shape、1 assembly、754 references、754 simple shapes、754 solids、3016 faces、9048 edges、18096 vertices、length unit `METRE`。
- 5 轮稳定性 gate：`docs/phase1/step_selection/tushi_rebar_123_stp/step_selection_diff.md`，decision `pass`。
- 稳定引用统计：faces 3016、edges 4524、vertices 3016，min match rate 均为 1.000000，duplicate 0。

关联 Evidence ID：

- E-RUN-002
- E-IDA-014
- E-DEV-023

关联 Requirement ID：

- REQ-PROJ-005

关联 GAP ID：

- GAP-IDA-008
- GAP-DEV-002

结论：

- `输出钢筋` 的 UI 位置已运行确认。
- 旧图石存在钢筋 STP 导出能力。
- 该 STP 可以成为后续钢筋几何 golden 来源之一。
- `123.stp` 是下游消力池小样本 SFL 导出的钢筋 STEP，已被 OCCT 成功导入并通过 5 轮稳定性 gate。
- 导入结果显示 754 个 solid，初步判断更像钢筋扫掠实体，而不是单纯中心线；最终表达语义仍需结合可视化和旧图石统计确认。
- 当前尚未确认 `sexportbar` / `ysteelout` 哪条命令链实际负责 `.stp` 导出。

下一步：

- 继续导出一个复杂样本钢筋 `.stp`。
- 对比 `123.stp` 与同一 SFL 的对象树、Detail 包和钢筋统计。
- 在可视化端确认 `123.stp` 的 754 个 solid 是否对应旧图石钢筋根数或分段实体。

## 待运行确认任务

### RUN-TODO-001：剖面圈筋

目标：

- 闭合 GAP-FUNC-001。

操作：

1. 打开小样本或边墩复杂样本。
2. 点击 `钢筋 / 主要钢筋 / 剖面圈筋`。
3. 记录弹窗标题、字段、状态栏提示。
4. 如果需要选择对象，记录选择顺序。

预期输出：

- 新 Evidence ID：`E-RUN-剖面圈筋`。

### RUN-TODO-002：固中心

目标：

- 闭合 GAP-FUNC-002。

操作：

1. 打开小样本。
2. 点击 `钢筋 / 构造钢筋 / 固中心`。
3. 记录弹窗、状态栏或错误提示。

预期输出：

- 新 Evidence ID：`E-RUN-固中心`。

### RUN-TODO-003：组合开

目标：

- 闭合 GAP-FUNC-003。

操作：

1. 打开含多个钢筋组的样本。
2. 选择一个可组合的钢筋组。
3. 点击 `组合开`。
4. 记录对象树变化、编号变化、是否可撤销。

预期输出：

- 新 Evidence ID：`E-RUN-组合开`。

### RUN-TODO-004：未割钢筋 / 未剖到钢筋

目标：

- 闭合 GAP-FUNC-004。

操作：

1. 切到查询页。
2. 点击疑似 `未割钢筋` 的按钮。
3. 记录真实按钮文字、弹窗标题、提示内容。

预期输出：

- 新 Evidence ID：`E-RUN-未割钢筋`。

### RUN-TODO-005：割切面 / 剖切面

目标：

- 闭合 GAP-FUNC-005。

操作：

1. 切到工程图片。
2. 点击疑似 `割切面` 的按钮。
3. 记录真实按钮文字、弹窗标题、字段。

预期输出：

- 新 Evidence ID：`E-RUN-割切面`。

### RUN-TODO-006：生成工程图

目标：

- 闭合 GAP-DRAW-001 和 GAP-IDA-005 的运行部分。
- 当前静态前提已知：
  - `36124 = 生成工程图`
  - `36124 / 0x8D1C -> psallc -> sub_140600AA0`
  - `output_uncut_steel / Dialog 0x57C / UnCutSteel.TXT`
    目前只算后置报告候选，不能直接当成前置设置窗。

操作：

1. 打开带剖切面/投影面的样本。
2. 点击 `生成工程图`。
3. 记录是否出现前置弹窗；若无前置弹窗，明确记 `无`。
4. 记录输出目录、生成文件名和是否出现 `UnCutSteel.TXT`。
5. 记录重复生成时是否覆盖旧 `Detail.xml / DetailNN.stl`。
6. 导出前后分别记录目录 listing / hash / 最后修改时间。
7. 用 AutoCAD 2020 + 旧插件导入生成包。
8. 记录导入后是否出现钢筋图形、下料表、材料表。

建议落点：

```text
docs/phase1/runtime_capture/todo_065_generate_package_and_schedule/
```

预期输出：

- 新 Evidence ID：`E-RUN-生成工程图`。
- 闭合或更新 `GAP-DRAW-001`、`GAP-DRAW-005`。

### RUN-TODO-007：下料表

目标：

- 闭合 GAP-IDA-006 的运行部分。
- 验证顶部 UI `下料表` caption 和真实运行输出，补足 `E-IDA-020 / E-IDA-043 / E-IDA-044` 尚未覆盖的运行部分。
- 当前静态前提已知：
  - `35057 = 下料表`
  - `35057 / 0x88F1 -> psexcel / sub_140605B20`
  - `Dialog #427` 当前只保守记为 `OptionFactory` 属性页，
    不能先入为主写成“下料表弹窗”。

操作：

1. 打开含钢筋的样本。
2. 点击 `下料表`。
3. 记录是否弹额外运行时窗口；若弹出，截图并记录标题。
4. 记录生成结果或 Detail 包字段变化。
5. 记录下料表是否写入 `DetailNN.stl` 的 `StbTable / MaterialTable`。
6. 记录下料表编号是否能和图上钢筋对应。
7. 记录是否生成 Excel、Detail 包或二者同时生成。
8. 记录输出目录、文件名、覆盖提示和失败提示。
9. 导出前后分别记录目录 listing / hash / 最后修改时间。

建议落点：

```text
docs/phase1/runtime_capture/todo_065_generate_package_and_schedule/
```

预期输出：

- 新 Evidence ID：`E-RUN-下料表`。
- 闭合或更新 `GAP-IDA-006`、`GAP-DRAW-004`。

### RUN-TODO-008：输出钢筋 STP 样本留存

目标：

- 补强 `E-RUN-002`。
- 闭合或更新 `GAP-IDA-008`。
- 给后续钢筋几何 golden 提供旧图石导出基线。

操作：

1. 分别打开小样本和复杂样本。
2. 点击 `开始 / 文件 / 输出钢筋`。
3. 截图导出窗口、格式选项、默认文件名和覆盖提示。
4. 导出 `.stp`。
5. 记录导出路径、文件大小、hash。
6. 用 OCCT 导入并记录 roots / solids / faces / edges / units / 曲线类型。
7. 若导出支持选择范围，分别记录“全部钢筋”和“选中钢筋组”的差异。

预期输出：

- 新 Evidence ID：`E-RUN-输出钢筋STP`。
- 旧图石导出的 `.stp` 文件。
- OCCT 导入统计报告。
- 更新 `31_STEP选择ID实际运行记录模板与样本清单.md` 的真实工程 STEP 样本。

### RUN-TODO-009：generated node+112 状态栏 / Excel 运行回填

目标：

- 闭合 `TODO-061` 的真实运行部分。
- 为 `E-RUN-005` 留下最小但可追溯的旧图石运行证据。
- 验证静态推断在运行时是否确实表现为：
  - 状态栏 `pane3 = 焊接 / 绑扎 / 套筒连接`
  - Excel 下料表包含 `焊头(个)` / `单下料长(mm)`

操作：

1. 打开一个确定带接头的旧图石样本。
2. 选中一个确定有接头的对象。
3. 截状态栏 `pane3`，保留完整主界面上下文。
4. 执行一次 `下料表` 导出。
5. 保留真实 Excel 文件，或者至少截出：
   - `焊头(个)`
   - `单下料长(mm)`
6. 记录：
   - 样本文件名
   - 对象编号
   - 操作步骤
   - 输出路径
   - 文件 hash

建议落点：

```text
docs/phase1/runtime_capture/todo_061_generated_node112/
  pane3_statusbar_*.png
  barschedule_*.xls[x]
  capture_notes.md
```

预期输出：

- 新 Evidence ID：`E-RUN-005`
- 至少 1 张真实 `pane3` 截图
- 至少 1 份真实 Excel 文件或字段截图
- 对应路径、对象编号和 `SHA256`

## RUN-20260609-001：生成工程图 / 下料表真实工件回填

Evidence：

```text
E-RUN-006
E-DEV-088
```

归档目录：

```text
docs/phase1/runtime_capture/todo_065_generate_package_and_schedule/run_20260609_001/
```

来源：

```text
用户现场旧图石运行回填。
本轮 agent 未启动旧图石，未安装 HASP，未修改系统目录。
```

输入样本：

```text
消力池下游侧带齿槽底板结构图石钢筋模型.sfl
SHA256 = 6706F10836CA43D1D3275ACD09B373CAC5AF08C0D7A66B1C7025D30006C4B007
```

生成工程图输出：

```text
Detail.xml
Detail01.stl
Detail02.stl
Detail03.stl
Detail04.stl
```

Detail.xml 说明：

```text
本轮 Detail.xml = 14 bytes，内容为 <StyleRoot/>。
它是本次回填工件，但不是非空样式表，也不是本轮业务字段载体。
用户补充：换了几台电脑生成结果都一样，Detail.xml 都是 <StyleRoot/>；
生成工程图和 CAD 导入时该文件修改时间不更新。
当前更倾向判断它是固定空模板 / 占位文件。

C:\Users\ghost\Desktop\reverse_engineering\Detail.xml
是父目录/外部历史样式样例，含 3 个 Style，可能来自其他项目，
不能并入 RUN-20260609-001 当作同一次导出结果。
```

关键 hash：

```text
Detail.xml  = CCBD220D75D7F9C7E26E2540D639FA5956A369A31D7902F75ED36461F778F271
Detail01.stl = 444BE32ED907C0393104F415639CED2FB698F21C93DF7F27F9ED830A70E40BE6
Detail02.stl = 478D166A5DCE69A3BD8042E0118443780C8C7FBBDB93E7AC910EEB2CCB4E32F6
Detail03.stl = 2531D11A15184909CC9FB3AA8CC96866F765E8254E443422854C176453A8FB43
Detail04.stl = 03617911FBFCC2D53BCC310F515F8C375B3C6B78640C638CB2591DA1494D4D0E
```

截图确认：

```text
PixPin_2026-06-09_09-49-51.png
  -> 工程图页签，能看到 生成工程图 / 下料表。

PixPin_2026-06-09_09-50-13.png
  -> 运行提示：剖切结束,请进入AutoCAD读取工程图!
```

下料表输出：

```text
下料表.xls
SHA256 = A9CF511EF0513DDB45E975A822C19833009AB237C9B0C7E1CA320BC2B5BF5D1E
```

Excel 只读预览：

```text
钢筋表        rows=9,  cols=6
钢筋汇总表    rows=4,  cols=6
钢筋下料单    rows=16, cols=8
```

本轮结论：

```text
旧图石真实输出了非空 Detail01..04.stl 图纸主体和非空 下料表.xls。
本轮 Detail.xml 存在但为空 StyleRoot；
结合多机观察，它更像固定空模板 / 占位文件。
这关闭了“是否能拿到真实运行工件”的当前节点。
```

剩余缺口：

```text
1. 缺少 rerun before/after 两次目录 listing，覆盖行为仍未完全闭合。
2. AutoCAD L2 未运行，不能声明旧 FDrawing 插件已读取该包。
3. 新系统输出和旧图石真实输出还未做字段级对照。
4. 旧插件是否要求 Detail.xml 这个空占位文件必须存在仍未确认。
5. 下料表完整合并规则、质量公式和 Volume722 等价仍未闭合。
```

## RUN-20260609-001 字段对照补充

Evidence：

```text
E-DETAIL-004
E-DEV-089
```

本轮 `TODO-067` 只做字段对照，不启动旧图石、不安装 HASP、不改 app 业务代码。

已确认字段差距：

```text
1. 真实 Detail.xml = <StyleRoot/>；
   当前 DetailWriter 会写非空 StyleRoot / Styles / Style1。

2. 真实 Detail01.stl 有 StbTable / MaterialTable；
   Detail02.stl 到 Detail04.stl 无 StbTable / MaterialTable。
   当前 DetailWriter 每张图都写表格。

3. 真实 StbTable 有 HeightValue0 / HeightValueCount / Volume1225 /
   NumCombineGoJians / SteelNetArea / GJTAOTNumber / GJTAOTVolue /
   LinkTop / LinkDown / DCGQSJ / HYLJJ；
   当前 DetailWriter 只写 count。

4. 真实 StbRow 有 smallTable / mirrorType / mirrorSEFlag；
   当前还未实现。

5. 真实 Excel 有 钢筋表 / 钢筋汇总表 / 钢筋下料单 三张表；
   当前 app 尚无真实 Excel workbook writer。
```

本轮新增缺口：

```text
GAP-DRAW-006
GAP-DRAW-007
GAP-DRAW-008
```
