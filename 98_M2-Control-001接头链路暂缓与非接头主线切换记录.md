# M2-Control-001 接头链路暂缓与非接头主线切换记录

## 结论

根据最新业务指令：

```text
接头这块先记录，暂不作为当前主线优先级。
```

大白话说：

```text
不是说接头以后不做，
而是现阶段先别让接头链路继续卡主整个复刻主线。
```

所以这轮只做路线纠偏，不改 `app` 业务代码。

## 本轮决策

### 1. 接头相关任务先降出当前 next

```text
TODO-050
  -> 保持 blocked
  -> 原因仍是旧图石现场运行条件 + 非空接头线样例

TODO-061
  -> 从 next 改为 pending
  -> 原因不是证据无效，而是业务优先级调整：
     领导要求现阶段先不考虑接头链路
```

### 2. 当前主线切回非接头方向

新增：

```text
TODO-062 / 生成工程图命令入口 / Ribbon 绑定静态补证 P0
```

并把它设为：

```text
status = next
```

原因：

```text
1. 它是非接头主线。
2. 它能继续服务 Qt6 UI 入口 1:1 复刻。
3. 它优先依赖 IDA MCP / 静态资源，不依赖旧图石现场运行。
4. 它和工程图输出主链直接相关，比继续卡在接头运行证据上更值钱。
```

## 这轮不变的东西

```text
1. Qt6 / OCCT / LegacyGeometryAdapter 的总路线不变。
2. 钢筋业务仍按旧图石证据 1:1 复刻，不改成 OCCT 直接重写。
3. 接头相关已有证据不回滚、不删除，只是暂不作为当前 next。
4. TODO-026 golden 仍保持 pending。
```

## 对后续执行的影响

后续继续 goal 模式时：

```text
先做 TODO-062，
不再继续追 TODO-061，
除非业务侧重新要求把接头链路提回主线。
```
