# 线配筋与弧形组专项初稿

## 目标

本文件记录 `线配筋`、`扇形筋`、`同心圆` 相关的 IDA 初步结论。

当前状态：

```text
已有 IDA 生成链证据。
还缺旧图石运行界面的选择流程和按钮对应关系。
```

因此本文件只能作为技术 spike 输入，不能直接当成完整业务复刻说明。

## 相关需求

### 线配筋

- Requirement ID：`REQ-REB-C-002`
- 新 CommandId：`Rebar.Create.LineGroup`
- 旧命令：`sgroupbarline`
- 入口函数：`sub_1404DE720`
- 当前闭合状态：`ReadyForSpike`
- 关联缺口：`GAP-IDA-002`、`GAP-IDA-007`

### 弧形组 / 扇形筋 / 同心圆

- Requirement ID：`REQ-REB-C-003`
- 新 CommandId：`Rebar.Create.ArcGroup`
- 旧命令：`sgroupbararc`
- 入口函数：`sub_1404DE110`
- 当前闭合状态：`ReadyForSpike`
- 关联缺口：`GAP-IDA-001`、`GAP-IDA-007`

## IDA 证据

### `sgroupbarline`

反编译摘要：

- 当前选择集必须只有 1 个对象。
- 会校验选中对象类型。
- 会校验内部结构数量，要求不少于 2。
- 会从内部结构中按奇数索引抽取实体。
- 会读取端点坐标。
- 会计算 4 组端点距离。
- 会选择最小距离和对应端点距离。
- 最终调用：

```text
sub_1404D10C0(entityList, v6, v4, minDistance, selectedDistance, flag)
```

工程含义：

- 线配筋不是纯参数弹窗。
- 它强依赖当前选择对象。
- 选择对象内部应能提供成对或成链的几何边。

### `sgroupbararc`

反编译摘要：

- 当前选择集必须只有 1 个对象。
- 会校验选中对象类型。
- 会校验内部结构数量，要求不少于 3。
- 会从链式实体关系中收集实体。
- 会调用 ACIS `api_entity_entity_distance` 计算实体距离。
- 默认距离参数初始值为 `0.1`。
- 最终调用：

```text
sub_1404D10C0(entityList, v4, v6, distance, 0.8, 1)
```

工程含义：

- 弧形组更像基于一组链式曲线/边界生成分段钢筋。
- 它可能服务于 `扇形筋`、`同心圆`，也可能是二者共用底层入口。

## 公共生成链

公共链路：

```text
sub_1404DE720 / sub_1404DE110
  -> sub_1404D10C0
  -> sub_140451730
  -> sub_1405D5670
```

`sub_1404D10C0`：

- 校验输入对象和距离下限。
- 距离小于 `0.002` 会失败。
- 启动 ACIS API block。
- 复制实体列表。
- 调用 `sub_140451730` 生成/挂接对象。
- 调用 `sub_1405D5670` 处理分段钢筋组。

`sub_140451730`：

- 复制或重建实体列表。
- 将实体挂接到模型/对象映射。
- 返回新生成对象。

`sub_1405D5670`：

- 命中源码路径：

```text
e:\tushi3d\dam\class\seg_steelbargroup.cpp
```

- 说明它属于旧图石分段钢筋组逻辑。
- 关键 ACIS 调用：

```text
api_edge
api_entity_entity_distance
api_split_curve
api_curve_spline
```

- 已观察到的几何处理：
  - 曲线分割。
  - 边长判断。
  - 端点裁剪。
  - 样条曲线生成。
  - 根据边长采样点。

已观察到的常量：

- 边长阈值：`0.01`。
- 端点距离阈值：`0.1`。
- 裁剪步长：`-0.03`。
- 采样数量：`max(5, EDGE::length * 50)`。

## 对 OCCT 的替代要求

这些旧 ACIS 能力在新系统中要用 OCCT 承担：

- curve / edge 表达。
- edge length。
- 点到曲线距离。
- curve split。
- curve trim。
- spline construction。
- entity list 的等价拓扑集合。

但业务规则仍由复刻层承担：

- 什么对象可作为线配筋输入。
- 何时按线配筋，何时按弧形组。
- 端部裁剪规则。
- 曲线采样规则。
- 分段钢筋组如何生成 `RebarGroup/Rebar/RebarSegment`。

## 新系统 spike 建议

### LineGroup Spike

输入：

- 一条或多条结构边。
- 间距/直径/保护层手动输入。

处理：

- 用 OCCT 读取边曲线。
- 计算端点距离。
- 根据距离和方向生成临时 `RebarGroup`。

输出：

- `RebarGroup.createCommand = Rebar.Create.LineGroup`
- `RebarSegment.shapeType = L`
- 可显示、可查询、可写首期可验证 Detail。

通过条件：

- 命令系统能从选择集拿到边。
- 生成对象进入领域模型，而不只是 Viewer 显示。
- dirty 和撤销可用。

### ArcGroup Spike

输入：

- 一组链式边或圆弧边。
- 间距/直径/保护层手动输入。

处理：

- 用 OCCT 计算曲线距离。
- 做曲线拆分或采样。
- 生成弧形 `RebarSegment`。

输出：

- `RebarGroup.createCommand = Rebar.Create.ArcGroup`
- `RebarSegment.shapeType = A`
- 可显示、可查询、可写首期可验证 Detail。

通过条件：

- 生成出的弧段能进入 `StbGeo`。
- 下料表能识别 `shapeType = A`。

## 待运行确认

必须确认：

1. 点击 `线配筋` 前是否需要预选线/边。
2. 点击 `线配筋` 后状态栏提示什么。
3. `线配筋` 是否复用 Dialog #265/#266/#268。
4. `扇形筋` 和 `同心圆` 是否分别弹不同窗口。
5. `sgroupbararc` 更接近 `扇形筋`、`同心圆`，还是两者共用。
6. 曲线组选择失败时旧图石提示什么。
7. 生成后模型树节点名称。

## 待 IDA 继续追踪

优先函数：

- `sub_1405BD0C0`
- `sub_1405C7260`
- `sub_1405E49D0`
- `sub_140580950`
- `sub_14058F160`
- `sub_14059B980`

目标：

- 给 `seg_steelbargroup` 字段命名。
- 找到写入 `steelbar` / `steelbargroup` / `seg_steelbargroup` 的位置。
- 找到刷新显示和 dirty 的位置。

当前字段地图初稿：

- 见 `16_seg_steelbargroup字段地图初稿.md`。

## 当前结论

线配筋和弧形组已经具备技术 spike 条件，但还不具备完整复刻条件。

下一步应并行做两件事：

- IDA 继续追 `seg_steelbargroup` 字段地图。
- 用户运行旧图石确认线配筋、扇形筋、同心圆的真实 UI 流程。
